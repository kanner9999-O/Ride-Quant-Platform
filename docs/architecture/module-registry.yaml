# Module Registry — Package 1.1 (Phase 1, System Decomposition & Module Registry)
#
# Evidence-fidelity correction (2026-08-20, vai trò: `Package 1.1 v1.3 Evidence-Fidelity
# Correction Executor`) -- mechanical, non-semantic. Corrects `P11-V13-A-MIN-01` (Review
# A/Independent Review B-confirmed Minor, non-blocking): the "v1.2 semantic amendment"
# banner below labels a rendering "Product Owner decision verbatim" that uses ASCII `--` in
# place of the actual em dash the Product Owner's decision text contains -- that specific
# historical rendering is NOT a literal-verbatim quotation and is superseded, for CURRENT
# evidence interpretation only, by the exact string below. The historical banner text is
# preserved byte-for-byte beneath this correction, unmodified -- this is an additive
# correction, not a rewrite; git history remains the authoritative historical record. No
# YAML module data (version/status/package_lifecycle/quality_tier, for this or any module)
# is touched by this correction -- verified byte-identical before/after.
#
# Authoritative exact decision strings (current evidence interpretation):
#   Tier decision:            APPROVE MARKET-DATA-INGESTION TIER 2 — SUPPORTING
#   Reconsolidation decision: APPROVE PACKAGE 1.1 V1.3 RECONSOLIDATION
# (Both reproduced here with the actual em dash and with no trailing punctuation inside the
# quotation, matching exactly what the Product Owner supplied in each governed transaction.)
#
# `P11-V13-A-MIN-01`: REMEDIATED_PENDING_DETERMINISTIC_VERIFICATION -- NOT self-certified
# CLOSED by this transaction; a separate deterministic verification decides closure.
# `P11-V13-REC-MIN-01` (new, same class -- a separate terminal-period-inside-quotation
# deviation found in docs/MANIFEST.md and docs/CHANGELOG.md's own reconsolidation-decision
# renderings, not in this registry file): REMEDIATED_PENDING_DETERMINISTIC_VERIFICATION --
# full detail in docs/MANIFEST.md's own additive correction section, not duplicated here.
# `P3-MDI-TIER-B-MIN-01` (separate, semantic, I-11 Scope wording in earlier candidate prose)
# is UNTOUCHED by this correction -- remains OPEN / non-blocking, NOT remediated here.
#
# CONSOLIDATED STABLE (package lifecycle, 2026-08-20T16:30+07:00 -- Product Owner decision
# timestamp supplied at MINUTE precision only, NOT padded with a fabricated seconds field,
# distinct from execution/commit time, vai trò: `Package 1.1 v1.3 Mechanical Reconsolidation
# Recorder`) -- status: Draft, NOT Approved/Locked. Product Owner decision verbatim: "APPROVE
# PACKAGE 1.1 V1.3 RECONSOLIDATION". Reviewed immutable semantic boundary:
# e8cfe378a458de30fe062b0f200d8a4090daf985 (the v1.3 semantic-amendment commit itself --
# market-data-ingestion.quality_tier Tier-2 pin -- same commit HEAD this transaction started
# from, verified via git rev-parse HEAD before any edit). Parent semantic baseline:
# b866bb8cf71a3df7b37b68badf227141c3082e90.
#
# Review evidence: bounded Review A (principal ChatGPT, role AI Technical Architect —
# Review A, reviewed boundary e8cfe378a458de30fe062b0f200d8a4090daf985) Blocker 0/Major
# 0/Minor 1 (`P11-V13-A-MIN-01` -- evidence-prose punctuation/typography fidelity issue in
# how the PO tier decision was labeled "verbatim" elsewhere; does NOT affect tier value,
# approval authority, approved_at, registry semantics, dependency graph, or reconsolidation
# eligibility), READY_FOR_INDEPENDENT_REVIEW_B. Independent Review B (principal ChatGPT, role
# AI Technical Architect, Mode B SAME_PRINCIPAL_DISTINCT_EXECUTION per ADR-031 §5, execution
# reference `P11-V13-B-e8cfe378-20260820T1618+0700`, isolation attestation SATISFIED,
# reviewed boundary e8cfe378a458de30fe062b0f200d8a4090daf985) Blocker 0/Major 0/Minor 1
# (`P11-V13-A-MIN-01` CONFIRMED -- Minor/non-blocking, independently re-verified NOT to
# require a separate remediation/re-review round before reconsolidation), final verdict
# READY_FOR_PRODUCT_OWNER_RECONSOLIDATION -- Review B independently determined the registry
# semantic delta is correct, the Tier-2 metadata is authoritative, the lifecycle reopening
# (Consolidated Stable -> candidate at the prior amendment) was correct, and no Blocker/Major
# exists. Independent-review requirement: SATISFIED.
#
# Mechanical lifecycle transaction -- `version: "1.3"` UNCHANGED (no content/architecture
# change, pure lifecycle-state reconsolidation), `status: Draft` UNCHANGED,
# `package_lifecycle: candidate -> Consolidated Stable`. **No semantic registry content
# altered by this transaction** -- `market-data-ingestion.quality_tier` (tier "Tier 2 --
# Supporting", approved_by "Product Owner", approved_at "2026-08-20T15:42+07:00") and
# `market-reference-service.quality_tier` (tier "Tier 2 -- Supporting", approved_by "Product
# Owner", approved_at "2026-08-19T19:27:00+07:00") both preserved byte-identical; every other
# module_id/module_type/responsibilities/implements_capabilities/serves_contexts/
# owns_authoritative_state/consumes/emits/depends_on/forbidden_dependencies/plugin_relation/
# security_classification/phase/status/notes field, for all 26 modules, preserved
# byte-identical; dependency graph preserved byte-equivalent (26 modules, 65 depends_on
# edges, zero edge additions/removals, verified acyclic). Authority boundary unchanged:
# market-reference-service remains sole authoritative owner of Instrument/Venue identity,
# precision/tick/lot metadata, calendar/session; market-data-ingestion remains
# query-consumer-only for those facts -- this reconsolidation grants NO new runtime
# authority. **`P3-MDI-TIER-B-MIN-01` -- OPEN / non-blocking, carried forward, NOT silently
# closed or erased by this transaction:** the earlier candidate's security-discussion prose
# inaccurately narrows I-11's declared Scope to "Execution Engine, Exchange Adapter" --
# Chapter 2's actual I-11 Scope is "toàn hệ thống, đặc biệt Execution Engine, Exchange
# Adapter" (the ENTIRE system, especially those two). Does not change Tier 2, does not change
# the current §13.12-D Security result, does not block this reconsolidation -- but must not
# later be used to exclude `market-data-ingestion` from applicable §13.12-A/I-11 conformance.
# NOT remediated by this mechanical transaction. **`P11-V13-A-MIN-01` -- Minor/non-blocking,
# NOT claimed CLOSED** (no authorized prior review actually closed it; Review B confirmed it
# non-blocking and not requiring a remediation round before reconsolidation, which is a
# distinct disposition from CLOSED) -- carried forward as non-blocking evidence-fidelity
# debt. This transaction does NOT: run or claim any Quality Gate result for
# `market-data-ingestion` (Tier 2 coverage floor -- line >= 80% AND branch >= 80%
# independently -- and all other applicable Chapter 13 dimensions, including §13.12-D data
# quality/numerical precision and Security per I-11's actual Chapter 2 Scope, remain to be
# formally evaluated in a separate future transaction); approve `market-data-ingestion` or
# any other module/package (Chapter 12 phase-approval is a separate, untouched authority);
# open Phase 3 Approval Gate; modify `go/**` or any other implementation code; modify
# ADR-032; modify any Domain Contract; modify Chapter 13; authorize LIVE (unchanged,
# NOT_AUTHORIZED).
#
# v1.3 semantic amendment (2026-08-20T15:42+07:00 -- Product Owner decision timestamp
# supplied at MINUTE precision only, per the governed transaction request; NOT normalized/
# padded with a fabricated ":00" seconds field -- written verbatim as
# "2026-08-20T15:42+07:00", a valid ISO-8601 representation omitting the seconds component,
# distinct from execution/commit time, vai trò: `market-data-ingestion Tier-2 Registry
# Recorder`) -- Product Owner decision verbatim: "APPROVE MARKET-DATA-INGESTION TIER 2 --
# SUPPORTING". Adds ONE new field, `quality_tier`, to the `market-data-ingestion` entry ONLY
# -- pins its Chapter 13 §13.4 branch-1 quality-tier classification (Tier 2 -- Supporting,
# coverage floor >= 80% line AND >= 80% branch independently per Chapter 13 §13.3, no
# additional tier-triggered requirement -- Chaos Test is Tier 0-only, Parity Test is Tier
# 1-only). Classification candidate (HEAD 7858b175365032aa0304158f819c801e4fb7d774) + full
# rejected-tier reasoning + ADR Scope Rule assessment (ADR NOT REQUIRED) authored in a prior
# transaction; Review A found `REVISION_REQUIRED` (`P3-MDI-TIER-A-MAJ-01` Major,
# `P3-MDI-TIER-A-MIN-02` Minor); both closed by a bounded correction (HEAD
# b866bb8cf71a3df7b37b68badf227141c3082e90) distinguishing Chapter 13 §13.12-A (I-9's own
# invariant-conformance Scope, correctly unwidened) from §13.12-D (Data quality/numerical
# precision, APPLICABLE -- this module owns authoritative Candle state and processes
# authoritative OHLCV values) and correcting the candidate's dependency-direction prose
# (structure-engine/raw-regime-engine both directly depends_on this module -- direct
# dependency BY a Decision-Pipeline engine != membership IN the Decision Pipeline, does NOT
# trigger Tier 1); bounded Review A re-review CLOSED both findings,
# `READY_FOR_INDEPENDENT_REVIEW_B`; Independent Review B (Mode B
# `SAME_PRINCIPAL_DISTINCT_EXECUTION`, principal ChatGPT, execution reference
# `P3-MDI-TIER-B-b866bb8-20260820T1534+0700`, ADR-031 §5 isolation attestation) independently
# supported Tier 2 -- Supporting, independently confirmed both prior findings CLOSED, Blocker
# 0/Major 0/Minor 1 (`P3-MDI-TIER-B-MIN-01`, non-blocking -- see below), verdict
# `READY_FOR_PRODUCT_OWNER_TIER_DECISION`. Full evidence detail recorded in
# `docs/MANIFEST.md`/`docs/CHANGELOG.md`; this transaction only records the resulting Product
# Owner approval into this registry, per Chapter 13 §13.4 branch 1's own authority path
# ("Runtime module -> resolve tier từ module-registry.yaml, Chapter 7 §7.5") -- no separate
# canonical-tier-designation-authority chain needed (§13.4.1 is for standalone artifacts
# without a canonical owning module; this module already has one). `version: "1.2" -> "1.3"`;
# genuine semantic field addition (a new authoritative fact, not mechanical/wording-only), so
# `package_lifecycle: Consolidated Stable -> candidate` -- same precedent this file already
# established for `market-reference-service.quality_tier`'s own v1.1 -> v1.2 amendment below:
# a genuine content change reopens the consolidated baseline pending a future
# re-consolidation transaction, it is not silently absorbed into the existing Consolidated
# Stable state. **`P3-MDI-TIER-B-MIN-01` — OPEN / non-blocking, carried forward, NOT silently
# closed by this transaction:** Review B found the (preserved, unmodified by this transaction)
# candidate's security-discussion prose inaccurately narrows I-11's declared Scope to
# "Execution Engine, Exchange Adapter" -- Chapter 2's actual I-11 Scope text is "Toàn hệ
# thống, đặc biệt Execution Engine, Exchange Adapter" (the ENTIRE system, especially those
# two) -- a narrower paraphrase than the invariant's own declared Scope. Review B determined
# this does NOT change Tier 2, does NOT change the current §13.12-D Security result (still
# NOT APPLICABLE in substance on the current implemented boundary, per the bounded
# correction's own preserved reasoning), and does NOT block this Product Owner tier decision
# -- but must NOT be used later to exclude `market-data-ingestion` from applicable
# §13.12-A/I-11 conformance once a real venue/credential boundary exists. This mechanical
# registry-recording transaction does NOT perform this Minor's semantic remediation (no
# repository rule requires correction as part of recording) -- it remains OPEN, tracked in
# `docs/MANIFEST.md`, for a future bounded correction. Preserved BYTE-IDENTICAL:
# `market-reference-service.quality_tier` (unchanged); module_id, module_type,
# responsibilities, owns_authoritative_state, consumes, emits, depends_on,
# forbidden_dependencies, security_classification, phase, status, notes -- of
# `market-data-ingestion` AND of every other module (all 25 other modules untouched,
# dependency graph byte-equivalent, 26 modules, 65 depends_on edges, zero edge
# additions/removals/forbidden_dependencies violations). This transaction does NOT: run or
# claim any Quality Gate result (coverage floor is now applicable once a formal gate
# evaluation happens, but none has run at this new tier boundary); approve
# `market-data-ingestion` as a module/package (Chapter 12 phase-approval is a separate,
# untouched authority); modify `go/market-data-ingestion/**` or any other implementation
# code; modify ADR-032; modify any Domain Contract; modify Chapter 13; authorize LIVE
# (unchanged, NOT_AUTHORIZED).
#
# CONSOLIDATED STABLE (package lifecycle, 2026-08-19T22:02:00+07:00, Product Owner decision,
# vai trò: `Package 1.1 v1.2 Mechanical Reconsolidation Recorder`) -- status: Draft, NOT
# Approved/Locked. Product Owner decision verbatim: "APPROVE PACKAGE 1.1 V1.2
# RECONSOLIDATION". Reviewed immutable boundary: 84a95eed108b3062d0521b7b453ee6b270d7e0ed
# (the v1.2 semantic-amendment commit itself, see banner below -- same commit HEAD this
# transaction started from, verified via git rev-parse HEAD before any edit). Review
# evidence: Review A COMPLETE/ELIGIBLE/CLEAN, Blocker 0/Major 0/Minor 0. Independent Review
# B COMPLETE/ELIGIBLE/CLEAN, Blocker 0/Major 0/Minor 0, Mode B
# (SAME_PRINCIPAL_DISTINCT_EXECUTION, ADR-031 §5) -- execution reference
# `P11-TIER-B-84a95eed-20260819T2126+0700` (deterministic workflow-generated ID, no
# provider-native session ID supplied, ADR-031 §5's explicit fallback model), isolation
# attestation SATISFIED -- self-attested per ADR-031 §5 conditions 1-4: executed in a
# separate execution/session context from Review A, not nested within it; independently
# resolved repository authority/evidence for this exact boundary, did not accept Review A's
# conclusions as ground truth; execution identifier distinct from Review A's. Independent-
# review requirement: SATISFIED. Mechanical lifecycle transaction -- `version: "1.2"`
# UNCHANGED (no content/architecture change, pure lifecycle-state reconsolidation),
# `status: Draft` UNCHANGED, `package_lifecycle: candidate -> Consolidated Stable`. **No
# semantic registry content altered by this transaction** -- `market-reference-service.
# quality_tier` (tier "Tier 2 -- Supporting", approved_by "Product Owner", approved_at
# "2026-08-19T19:27:00+07:00") preserved byte-identical; every other module_id/module_type/
# responsibilities/implements_capabilities/serves_contexts/owns_authoritative_state/
# consumes/emits/depends_on/forbidden_dependencies/plugin_relation/security_classification/
# phase/status/notes field, for all 26 modules, preserved byte-identical; dependency graph
# preserved byte-equivalent (26 modules, 65 depends_on edges, zero edge additions/removals).
# This transaction does NOT: run or claim any Quality Gate result; approve any
# implementation module/package (Chapter 12 phase-approval is a separate, untouched
# authority); modify `go/**` or any other implementation code; modify ADR-032; modify any
# Domain Contract; modify Chapter 13; authorize LIVE (unchanged, NOT_AUTHORIZED).
#
# v1.2 semantic amendment (2026-08-19T19:27:00+07:00, Product Owner decision, vai trò:
# `market-reference-service Tier-2 Registry Recorder`) -- Product Owner decision verbatim:
# "APPROVE MARKET-REFERENCE-SERVICE TIER 2". Adds ONE new field, `quality_tier`, to the
# `market-reference-service` entry ONLY -- pins its Chapter 13 §13.4 branch-1 quality-tier
# classification (Tier 2 -- Supporting, coverage floor >= 80% line AND >= 80% branch
# independently per Chapter 13 §13.3, no additional tier-triggered requirement -- Chaos
# Test is Tier 0-only, Parity Test is Tier 1-only). Classification candidate + full
# rejected-tier reasoning + ADR Scope Rule assessment (ADR NOT REQUIRED, Chapter 0 §4b --
# neither the ">1 module" nor "khó đảo ngược" clause triggers for one module's own tier
# field) authored in a prior transaction, recorded in MANIFEST/CHANGELOG; this transaction
# only records the resulting Product Owner approval into this registry, per Chapter 13
# §13.4 branch 1's own authority path ("Runtime module -> resolve tier từ
# module-registry.yaml, Chapter 7 §7.5") -- no separate canonical-tier-designation-authority
# chain needed (that machinery, §13.4.1, is for standalone artifacts without a canonical
# owning module; this module already has one). `version: "1.1" -> "1.2"`; genuine semantic
# field addition (a new authoritative fact, not mechanical/wording-only), so
# `package_lifecycle: Consolidated Stable -> candidate` -- same precedent this file already
# established for `custody-signing-service.phase.elaborated_by: null -> "1.2"` (v0.6 ->
# v0.7 below): a genuine content change reopens the consolidated baseline pending a future
# re-consolidation transaction, it is not silently absorbed into the existing Consolidated
# Stable state. Preserved BYTE-IDENTICAL: module_id, module_type, responsibilities,
# owns_authoritative_state, consumes, emits, depends_on, forbidden_dependencies,
# security_classification, phase, status, notes -- of `market-reference-service` AND of
# every other module (all 25 other modules untouched, dependency graph byte-equivalent, 65
# depends_on edges unchanged, zero forbidden_dependencies violation). This transaction does
# NOT: run or claim any Quality Gate result (coverage floor is now applicable once a formal
# gate evaluation happens, but none has run); approve `market-reference-service` as a
# module/package (Chapter 12 phase-approval is a separate authority); modify
# `go/market-reference-service/**` or any other implementation code; modify ADR-032; modify
# any Domain Contract; modify Chapter 13; authorize LIVE (unchanged, NOT_AUTHORIZED).
#
# CONSOLIDATED STABLE (package lifecycle, 2026-08-10, Product Owner decision,
# vai trò: `Package 1.1 ADR-023 Alignment Mechanical Reconsolidation Executor`)
# -- status: Draft, NOT Approved/Locked. Product Owner decision (verbatim, as
# supplied in the transaction request): "APPROVE PACKAGE 1.1 ADR-023 ALIGNMENT
# RECONSOLIDATION." Decision date: 2026-08-10 (exact clock time not supplied
# -- date-only metadata used, no timestamp invented). Review evidence: Review
# A CLEAN; Independent Review B initial alignment review Blocker 0/Major 0/
# Minor 1 (`P11-ADR023-B-MIN-01`), closed by the bounded parity correction
# (`system-decomposition.md` v1.2 -> v1.3); Review A bounded re-review CLEAN;
# Independent Review B bounded re-review CLEAN, `P11-ADR023-B-MIN-01` CLOSED;
# final readiness Blocker 0/Major 0/Minor 0, READY FOR PRODUCT OWNER
# RECONSOLIDATION. Mechanical lifecycle transaction -- `version: "1.1"`
# UNCHANGED (no content/architecture change), `package_lifecycle: candidate
# -> Consolidated Stable`. `system-decomposition.md` consolidates in parallel
# at its own reviewed version, `"1.3"` -- the two artifact versions are NOT
# normalized to a shared number, each is recorded exactly as reviewed.
# Preserved unchanged (byte-identical, this consolidation changes lifecycle
# prose/fields ONLY): `contract-compatibility-authority` (module_type:
# compute_engine, owns_authoritative_state: true, depends_on: [],
# implements_capabilities: [], serves_contexts: [], consumes: [], emits:
# [event]); all 25 prior modules; 26-module inventory; 65 depends_on edges;
# acyclic graph; zero forbidden_dependencies violation; A+B responsibility
# colocation; Part C exclusion; Declaration-tier registration only --
# `module identity != evaluator grant`, `Declaration -> Grant -> Enforcement
# -> Verification`, `granted subset declared`. No evaluator Grant issued, no
# Compatibility Result created, Package 1.4 (`api-architecture.md`) not
# modified, ADR-023/ADR-022 not modified, ADR-024 not created.
# `QG-P14-E-EVID-01`/`G2-RDY-BLK-03` remain OPEN, Phase 1 Quality Gate
# remains `FAIL -- evidence`, Gate 2 remains CLOSED, Phase 2 remains NOT
# AUTHORIZED -- reconsolidation does not create evaluator authorization/
# evidence. `status: Draft`, `approved_by: null`, `approved_at: null`
# unchanged -- `Consolidated Stable` is a package lifecycle/readiness state,
# not artifact `Approved`/`Locked` (Chapter 0 §7.1). No implementation/
# Gate 2/Phase 2/LIVE authorization introduced.
#
# CANDIDATE (package lifecycle, reverted from Consolidated Stable, 2026-08-10,
# vai trò: `Package 1.1 ADR-023 Alignment Executor`) -- status: Draft, NOT
# Approved/Locked. v1.0 -> v1.1: genuine semantic registry change -- EXACT
# mechanical transcription of Approved ADR-023 (v0.5, Approved 2026-08-10,
# approved_by: Product Owner, approved_at: 2026-08-10) -- NOT a new
# architecture decision, NOT a new ADR. Exactly one module added:
# `contract-compatibility-authority` (module_type: compute_engine,
# owns_authoritative_state: true, depends_on: [], forbidden_dependencies: [],
# implements_capabilities: [], serves_contexts: [] -- no existing context-
# map.yaml capability/Domain Context invented, same empty-field convention as
# command-query-api-surface). ZERO dependency edges added or removed on ANY
# existing module -- reading the Package 1.4 published-contract artifact and
# the pinned compatibility-policy root occurs via MANIFEST identity/
# applicability authority (ADR-022 §5.2, same pattern as ADR-022 §8/ADR-023
# §8), NOT a runtime module dependency edge. Independently graph-verified
# (full 26-module DFS): module count 25 -> 26, zero edges added, zero edges
# removed, zero duplicate edges, full depends_on graph remains acyclic, zero
# forbidden_dependencies violations. No existing module's module_type,
# owns_authoritative_state, responsibilities, consumes, emits, depends_on, or
# forbidden_dependencies changed. This registration is Declaration-tier
# architecture only (Chapter 9 §9.6) -- module identity != evaluator grant,
# `Declaration -> Grant -> Enforcement -> Verification`, granted subset
# declared -- NO evaluator Grant issued by this transaction, NO Compatibility
# Result created, Package 1.4 (`api-architecture.md`) NOT modified,
# `QG-P14-E-EVID-01`/`G2-RDY-BLK-03` remain OPEN, Phase 1 Quality Gate remains
# `FAIL -- evidence`, Gate 2 remains CLOSED, Phase 2 remains NOT AUTHORIZED.
# `package_lifecycle: Consolidated Stable -> candidate` (genuine semantic
# change, this consolidation is NOT re-run by this transaction).
# `system-decomposition.md` mirrors this module in parallel at its own
# reviewed version stream, NOT normalized to a shared version number with this
# file. ADR-023 is NOT modified by this transaction. ADR-024 is NOT created.
#
# CONSOLIDATED STABLE (package lifecycle, 2026-08-07, Product Owner decision,
# vai trò: `Package 1.1 ADR-021 Mechanical Consolidation Executor`) — status:
# Draft, NOT Approved/Locked. Package lifecycle (Consolidated Stable) and
# artifact status (Draft) are separate dimensions (Chapter 0 §7.1). Product
# Owner decision (verbatim, as supplied in the transaction request): "APPROVE
# PACKAGE 1.1 V1.0/V1.1 CONSOLIDATION." Decision date: 2026-08-07 (exact clock
# time not supplied -- date-only metadata used, no timestamp invented). Review
# evidence on the v1.0 ADR-021 alignment (this file) and system-decomposition.md
# v1.1 (parity mirror): Review A CLEAN, Independent Review B CLEAN,
# consolidation readiness: READY, Blocker 0/Major 0/Minor 0. Mechanical
# lifecycle transaction -- `version: "1.0"` UNCHANGED (no content/architecture
# change), `package_lifecycle: candidate -> Consolidated Stable`.
# system-decomposition.md consolidates in parallel at its own reviewed version,
# `"1.1"` -- the two artifact versions are NOT normalized to a shared number;
# each is recorded exactly as reviewed. Preserved unchanged (byte-identical,
# this consolidation changes lifecycle prose/fields ONLY): the added
# review-evidence-service.depends_on edge to decision-evaluation-engine
# (ADR-021 §4/§10) -- recomputation delegation ONLY; the VIEW-003
# responsibility text (recorded Decision via decision-authority-service,
# deterministic recomputation delegated to decision-evaluation-engine, final
# structured comparison staying at review-evidence-service,
# review-evidence-service does NOT perform canonical Decision evaluation
# itself); 25-module inventory; every other dependency/forbidden_dependencies
# list; review-evidence-service.consumes (`[event]`), .emits (`[query]`),
# module_type (`projection`), owns_authoritative_state (`false`), hybrid
# (`null`), forbidden_dependencies; decision-evaluation-engine's every field
# (module_type/consumes/emits/depends_on/forbidden_dependencies/
# owns_authoritative_state/phase, unchanged -- remains recomputation delegate
# ONLY, gains no authority, performs no comparison, exposes no query result
# directly); Decision authority (decision-authority-service, sole authority);
# RiskEvaluation/Execution Intent authority (risk-gateway); Order authority
# (execution-engine); ExecutionResult authority (execution-result-processor);
# API Surface routing/exposure-only boundary; UX Shell non-authority;
# `decision.md` §9a semantics (not redefined, only cited). The
# recomputation-delegation interaction mechanism (request representation,
# response/event correlation, synchronous-vs-asynchronous interaction, timeout
# behavior, concrete failure codes, implementation transport) remains
# EXPLICITLY unresolved -- this consolidation does NOT resolve it, does NOT
# represent it as resolved, and does NOT introduce any consumes/emits change to
# address it; it remains carried forward as an interaction-mechanism gap, same
# pattern as the Package 1.5 contract-category interaction gap. ADR-021 is NOT
# modified by this transaction. `DD-001` and any Package 1.5 interaction gap
# remain unresolved. Package 1.4 (`api-architecture.md`) and Package 1.6
# (`ux-architecture.md`) are NOT modified -- the separate Package 1.4
# parity-description transaction and the separate Package 1.6 correction
# transaction (closing `P16-A-MAJ-02` fully) remain pending, unaffected by this
# consolidation. No implementation, Gate 2, Phase 2, or LIVE authorization
# introduced.
#
# CANDIDATE (package lifecycle, reverted from Consolidated Stable, 2026-08-07,
# HISTORICAL — superseded by the Consolidated Stable decision above, vai trò:
# `Package 1.1 ADR-021 Alignment Executor`) — status: Draft, NOT
# Approved/Locked. Package 1.1 v0.9 -> v1.0: genuine semantic registry change —
# EXACT mechanical transcription of Approved ADR-021 (v0.1, Approved 2026-08-07,
# approved_at "2026-08-07") — NOT a new architecture decision, NOT a new ADR.
# Exactly one dependency edge added: review-evidence-service.depends_on +=
# decision-evaluation-engine (VIEW-003/UC-005 Decision replay-parity
# recomputation delegation, ADR-021 §4/§10). The recorded-side edge
# (review-evidence-service -> decision-authority-service) was already present
# -- ZERO new edge needed for that side. `command-query-api-surface`'s edge to
# review-evidence-service was already present -- ZERO new edge at the API
# Surface layer. Independently graph-verified (full 25-module DFS): exactly one
# edge added, zero edges removed, zero duplicate edges, the full depends_on
# graph remains acyclic, and the direct cycle decision-authority-service <->
# decision-evaluation-engine is NOT introduced (decision-authority-service
# already depends_on decision-evaluation-engine; the new edge runs the other
# direction, review-evidence-service -> decision-evaluation-engine, with no
# path back). No forbidden_dependencies violated. Module count unchanged (25).
# `review-evidence-service.responsibilities` gains one new entry describing the
# VIEW-003 comparison-orchestration boundary (recorded-side via existing edge,
# recomputation delegated to decision-evaluation-engine, final comparison and
# query exposure staying at review-evidence-service) -- review-evidence-service
# does NOT perform canonical Decision evaluation itself. `review-evidence-
# service.consumes` (`[event]`), `.emits` (`[query]`), `module_type`
# (`projection`), `owns_authoritative_state` (`false`), `hybrid` (`null`) are
# ALL unchanged -- NO contract-category change introduced to address the
# interaction-mechanism gap (§ below). `decision-evaluation-engine` (every
# field: module_type, consumes, emits, depends_on, forbidden_dependencies,
# owns_authoritative_state, phase) is completely unchanged -- it remains a
# non-authoritative recomputation delegate ONLY, gains no new authority, and is
# not itself modified by this transaction. Interaction-mechanism gap (bắt buộc
# ghi nhận, KHÔNG resolve tại transaction này): the exact recomputation request
# representation, response/event correlation, synchronous-vs-asynchronous
# interaction, timeout behavior, concrete failure codes, and implementation
# transport between review-evidence-service and decision-evaluation-engine are
# NOT defined by this depends_on edge alone -- carried forward as an unresolved
# implementation/interaction-mechanism gap, same pattern as the Package 1.5
# contract-category interaction gap (database-architecture.md §11, ADR-019
# §1.1's own citation of that precedent). Same precedent as v0.8 -> v0.9
# (ADR-020 alignment) — a genuine architectural/semantic change, NOT a bounded
# wording-only correction, reverts `package_lifecycle` from `Consolidated
# Stable` to `candidate`. This transaction does NOT reconsolidate — a separate
# Review A + Independent Review B + Product Owner consolidation decision MUST
# complete before this baseline returns to `Consolidated Stable`. Preserved
# unchanged: every other module's identity/taxonomy/dependency/
# forbidden_dependencies/contract-category/authority/phase assignment; module
# inventory (STILL 25 modules — no module added); Decision authority
# (decision-authority-service, sole authority, unchanged); RiskEvaluation/
# Execution Intent authority (risk-gateway); Order authority
# (execution-engine); ExecutionResult authority (execution-result-processor);
# API Surface routing/exposure-only boundary; UX Shell non-authority;
# `decision.md` §9a semantics (not redefined, only cited by ADR-021). `DD-001`
# and any Package 1.5 interaction gap remain unresolved — untouched by this
# transaction. Package 1.4 (`api-architecture.md`) and Package 1.6
# (`ux-architecture.md`) are NOT modified by this transaction — a separate
# Package 1.4 parity-description transaction and a separate Package 1.6
# correction transaction (closing `P16-A-MAJ-02` fully) remain pending. No
# implementation, Gate 2, Phase 2, or LIVE authorization introduced.
#
# CONSOLIDATED STABLE (package lifecycle, 2026-08-06, Product Owner decision,
# HISTORICAL — superseded by the candidate decision above, vai trò:
# `Package 1.1 ADR-020 Alignment Mechanical Consolidation Executor`) —
# status: Draft, NOT Approved/Locked. Package lifecycle (Consolidated Stable)
# and artifact status (Draft) are separate dimensions (Chapter 0 §7.1). Product
# Owner decision (verbatim, as supplied in the transaction request): "APPROVE
# PACKAGE 1.1 ADR-020 ALIGNMENT CONSOLIDATION." Decision date: 2026-08-06
# (exact clock time not supplied -- date-only metadata used, no timestamp
# invented). Review evidence on the v0.9 ADR-020 alignment (this file) and
# system-decomposition.md v1.0 (parity mirror): Review A CLEAN, Independent
# Review B CLEAN, Blocker 0/Major 0/Minor 0. Mechanical lifecycle transaction —
# `version: "0.9"` UNCHANGED (no content/architecture change),
# `package_lifecycle: candidate -> Consolidated Stable`. system-decomposition.md
# consolidates in parallel at its own reviewed version, `"1.0"` -- the two
# artifact versions are NOT normalized to a shared number; each is recorded
# exactly as reviewed. Preserved unchanged (byte-identical, this consolidation
# changes lifecycle prose/fields ONLY): the added review-evidence-service
# VIEW-002/UC-003 responsibility (ADR-020 §5/§7/§9); 25-module inventory; every
# dependency/forbidden_dependencies list; review-evidence-service.consumes
# (`[event]`), .emits (`[query]`), module_type (`projection`),
# owns_authoritative_state (`false`), hybrid (`null`), phase assignment;
# Decision/RiskEvaluation/Execution Intent/Order/ExecutionResult authority; API
# Surface routing/exposure-only boundary; UX Shell non-authority; no-recompute
# and correction-lineage requirements. ADR-020 is NOT modified by this
# transaction. VIEW-003 computation ownership, `canonical semantic-decision
# hash`, UC-003's three Product-level mechanism gaps, `DD-001`, and any Package
# 1.5 interaction gap remain unresolved. Package 1.4 (`api-architecture.md`),
# Package 1.5 (`database-architecture.md`), and Package 1.6
# (`ux-architecture.md`) are NOT modified -- the separate Package 1.4
# parity-description transaction and the separate Package 1.6 correction
# transaction remain pending, unaffected by this consolidation. No
# implementation, Gate 2, Phase 2, or LIVE authorization introduced.
#
# CANDIDATE (package lifecycle, reverted from Consolidated Stable, 2026-08-06,
# HISTORICAL — superseded by the Consolidated Stable decision above, vai trò:
# `Package 1.1 ADR-020 Alignment Executor`) — status: Draft, NOT
# Approved/Locked. Package 1.1 v0.8 -> v0.9: genuine semantic registry change —
# EXACT mechanical transcription of Approved ADR-020 (v0.1, Approved 2026-08-06,
# approved_at "2026-08-06") — NOT a new architecture decision, NOT a new ADR.
# Exactly one change: review-evidence-service.responsibilities gains one new
# entry — the non-authoritative, interval-bounded VIEW-002 / UC-003 Research
# verification existence check over existing Decision, RiskEvaluation, Execution
# Intent, Order and ExecutionResult facts (ADR-020 §5/§7/§9). The four source
# modules (decision-authority-service, risk-gateway, execution-engine,
# execution-result-processor) were already present in
# review-evidence-service.depends_on, and command-query-api-surface already
# depends_on review-evidence-service — ZERO new dependency edge at any layer.
# `review-evidence-service.consumes` (`[event]`), `.emits` (`[query]`),
# `module_type` (`projection`), `hybrid` (`null`), `owns_authoritative_state`
# (`false`), `depends_on`, `forbidden_dependencies`, and `phase` are ALL
# unchanged. Same precedent as v0.7 -> v0.8 (ADR-019 alignment) — a genuine
# architectural/semantic change, NOT a bounded wording-only correction, reverts
# `package_lifecycle` from `Consolidated Stable` to `candidate`. This
# transaction does NOT reconsolidate — a separate Review A + Independent Review
# B + Product Owner consolidation decision MUST complete before this baseline
# returns to `Consolidated Stable`. Preserved unchanged: every other module's
# identity/taxonomy/dependency/forbidden_dependencies/contract-category/
# authority/phase assignment; module inventory (STILL 25 modules — no module
# added); Decision authority (decision-authority-service); RiskEvaluation/
# Execution Intent authority (risk-gateway); Order authority (execution-engine);
# ExecutionResult authority (execution-result-processor); API Surface
# routing/exposure-only boundary; UX Shell non-authority. VIEW-003 computation
# ownership, `canonical semantic-decision hash`, the VIEW-003
# INDETERMINATE-equivalent outcome, UC-003's three Product-level mechanism gaps
# (session-interval, evidence-completeness, correction-arrival), `DD-001`, and
# any Package 1.5 interaction gap remain unresolved — untouched by this
# transaction. Package 1.4 (`api-architecture.md`), Package 1.5
# (`database-architecture.md`), and Package 1.6 (`ux-architecture.md`) are NOT
# modified by this transaction — a separate Package 1.4 parity-description
# transaction and a separate Package 1.6 correction transaction remain pending.
# No implementation, Gate 2, Phase 2, or LIVE authorization introduced.
#
# CONSOLIDATED STABLE (package lifecycle, 2026-08-06, Product Owner decision,
# HISTORICAL — superseded by v0.9 above) — status:
# Draft, NOT Approved/Locked. Package lifecycle (Consolidated Stable) and artifact status
# (Draft) are separate dimensions (Chapter 0 §7.1). Product Owner decision (verbatim, as
# supplied in the transaction request): "APPROVE PACKAGE 1.1 ADR-019 ALIGNMENT
# CONSOLIDATION." Decision date: 2026-08-06 (exact clock time not supplied -- date-only
# metadata used, no timestamp invented). Review evidence on the v0.8 ADR-019 alignment
# (this file) and system-decomposition.md v0.9 (parity mirror, post P11V08-A-MIN-01
# correction): Review A CLEAN, Independent Review B CLEAN, Blocker 0/Major 0/Minor 0.
# Mechanical lifecycle transaction -- `version: "0.8"` UNCHANGED (no content/architecture
# change), `package_lifecycle: candidate -> Consolidated Stable`. system-decomposition.md
# consolidates in parallel at its own reviewed version, `"0.9"` -- the two artifact
# versions are NOT normalized to a shared number; each is recorded exactly as reviewed.
# Preserved unchanged (byte-identical, this consolidation changes lifecycle prose/fields
# ONLY): 25-module inventory; the 17-module command-query-api-surface.depends_on list
# (including backtest-orchestrator); backtest-orchestrator.consumes (`[event, query]`),
# .emits (`[event]`), `module_type` (`runtime_service`), `hybrid` (`null`),
# `owns_authoritative_state` (`deferred`); decision-authority-service/risk-gateway's
# `owns_authoritative_state` (`true`/`true`, Decision/RiskEvaluation authority unchanged);
# every other module's dependencies, forbidden_dependencies, and contract categories.
# ADR-018 and ADR-019 are NOT modified by this transaction. `DD-001` and VIEW-002 remain
# unresolved. Package 1.4 (`api-architecture.md`) and Package 1.6 (`ux-architecture.md`)
# are NOT modified -- the separate Package 1.4 parity-transcription transaction and the
# separate Package 1.6 correction transaction remain pending, unaffected by this
# consolidation. No implementation, Gate 2, Phase 2, or LIVE authorization introduced.
#
# CANDIDATE (package lifecycle, reverted from Consolidated Stable, 2026-08-06,
# HISTORICAL — superseded by the Consolidated Stable decision above) — status:
# Draft, NOT Approved/Locked. Package 1.1 v0.7 -> v0.8: genuine semantic registry change —
# EXACT mechanical transcription of Approved ADR-019 (NAV-003 Gap A, v0.2, Approved
# 2026-08-06, approved_at "2026-08-06") — NOT a new architecture decision, NOT a new ADR.
# Controlled by Approved ADR-018 (v0.2, NAV-003 Gap B classification, unchanged) and
# ADR-019 (v0.2, unchanged). Exactly two changes: (1) command-query-api-surface.depends_on
# += backtest-orchestrator (ONE new dependency edge — the query-exposure route ADR-019 §2
# decided); (2) backtest-orchestrator.consumes: [event] -> [event, query] (the
# contract-category expansion ADR-019 §2 decided as required, precedent
# decision-authority-service). Same precedent as v0.4 -> v0.5 (ADR-017 alignment) and
# v0.6 -> v0.7 (ADR-016/Package 1.2 alignment) — a genuine architectural/semantic change,
# NOT a bounded parity/wording correction, reverts `package_lifecycle` from
# `Consolidated Stable` to `candidate`. This transaction does NOT reconsolidate — a
# separate Review A + Independent Review B + Product Owner consolidation decision MUST
# complete before this baseline returns to `Consolidated Stable`. Preserved unchanged:
# backtest-orchestrator.emits (`[event]`), `module_type` (`runtime_service`), `hybrid`
# (`null`), `owns_authoritative_state` (`deferred`); decision-authority-service/
# risk-gateway's `owns_authoritative_state` (`true`/`true`, Decision/RiskEvaluation
# authority unchanged); module inventory (STILL 25 modules — no module added); every
# other module's `depends_on`/`forbidden_dependencies`/`consumes`/`emits`/taxonomy/
# authority/phase assignment. No Backtest Domain entity/event/schema/authoritative fact
# introduced. `DD-001`, `backtest-orchestrator.owns_authoritative_state` (specific
# true/false), and VIEW-002 remain unresolved — untouched by this transaction. Package
# 1.4 (`api-architecture.md`) and Package 1.6 (`ux-architecture.md`) are NOT modified by
# this transaction — a separate Package 1.4 parity-transcription transaction and a
# separate Package 1.6 correction transaction remain pending.
#
# CONSOLIDATED STABLE (package lifecycle, 2026-08-05T13:51:00+07:00, Product Owner
# decision, HISTORICAL — superseded by v0.8 above) — status: Draft, NOT Approved/Locked. Package lifecycle (Consolidated Stable)
# and artifact status (Draft) are separate dimensions (Chapter 0 §7.1) — Consolidated
# Stable does NOT mean this file is Approved/Locked; it means the reviewed package
# baseline (v0.7, 25 modules, custody-signing-service.phase.elaborated_by: "1.2") is
# internally coherent and stable enough to be a dependency baseline for the next package.
# Product Owner decision (verbatim): "I approve consolidation of Package 1.1 v0.7 as the
# current Consolidated Stable module-registry and system-decomposition baseline,
# including the assignment of custody-signing-service.phase.elaborated_by to Package
# 1.2, while preserving exchange-adapter.phase.elaborated_by as null, the PAPER-only
# execution path, and LIVE Unauthorized." Mechanical lifecycle transaction — `version:
# "0.7"` UNCHANGED (no content/architecture change), `package_lifecycle: candidate ->
# Consolidated Stable`.
#
# v0.7 semantic alignment transaction (2026-08-05, HISTORICAL — candidate v0.7 review
# scope preceding the consolidation decision above): reverted package_lifecycle from
# `Consolidated Stable` (v0.6) to `candidate`: `custody-signing-service.phase.
# elaborated_by` changed `null -> "1.2"` after Package 1.2 v0.4 (security-custody-
# baseline.md §4a) passed Review A CLEAN + Independent Review B CLEAN (Blocker 0/Major
# 0/Minor 0) — a genuine semantic phase-assignment change, NOT a mechanical/wording-only
# transaction, same precedent as v0.5's original registration and the v0.2 -> v0.3
# ADR-016 alignment. v0.6's Consolidated Stable consolidation decision
# (2026-08-05T08:40:00+07:00, Product Owner) was NOT falsified — it remained the
# historical record for candidate v0.6's own review-clean state at that time.
# `exchange-adapter.phase.elaborated_by` remains `null`, unchanged. `version: "0.6" ->
# "0.7"`.
#
# CONSOLIDATED STABLE (package lifecycle, 2026-08-05T08:40:00+07:00, Product Owner
# decision, HISTORICAL — superseded by v0.7 above) — status: Draft, NOT Approved/Locked.
# Package lifecycle (Consolidated Stable) and artifact status (Draft) are separate
# dimensions (Chapter 0 §7.1) — Consolidated Stable does NOT mean this file is
# Approved/Locked; it meant the reviewed package baseline (v0.6, 25 modules) was
# internally coherent and stable enough to be a dependency baseline for the next
# package. Product Owner decision: "I approve consolidation of Package 1.1 v0.6 as the
# current Consolidated Stable module-registry and system-decomposition baseline."
# Mechanical lifecycle transaction — `version: "0.6"` UNCHANGED (no content/architecture
# change), `package_lifecycle: candidate -> Consolidated Stable`.
#
# History: v0.5 registered two new modules (custody-signing-service, exchange-adapter)
# authorized by Approved ADR-017 v0.2 (2026-08-04T20:08:00+07:00) — a genuine
# architectural/semantic change, NOT a bounded parity/wording correction — which reverted
# package_lifecycle to `candidate` (same precedent as v0.2 -> v0.3 ADR-016 alignment).
# v0.6 — bounded correction (2026-08-05), đóng `P11V05-A-MAJ-01`/`P11-v0.5-IRB-MAJ-01`:
# v0.5's `custody-signing-service.phase.elaborated_by: "1.2"` was a false package-coverage
# claim — security-custody-baseline.md v0.2 was authored/reviewed BEFORE this module
# existed. Corrected to `elaborated_by: null` (same deferred representation already used
# for `exchange-adapter`) — module registered, elaborating package NOT yet assigned. This
# consolidation transaction reviews v0.6 (post-correction) — Review A CLEAN + Independent
# Review B CLEAN, Blocker 0/Major 0/Minor 0 — and does NOT reopen or resolve either
# module's elaborating-package assignment, capability/context mapping, LIVE-path
# activation, or any other preserved gap.
# Authority: Chapter 7 §7.5 (Locked) — module identity/taxonomy/mapping ONLY.
# This registry does NOT own: Business Capability/Domain Context identity (context-map.yaml,
# Chapter 4), domain concept semantics (Domain Contract, /docs/domain/), Product/UC/UX
# semantics (docs/product/), or runtime facts (event log).
#
# P11-A-MAJ-02 correction (canonical authority boundary, Chapter 9 §9.1 / I-12): THIS FILE
# is the SOLE authority for Plugin Definition identity, module existence, primary taxonomy,
# and architecture responsibility of every module including plugin-hosting modules. No
# runtime module (e.g. `plugin-release-manager` below) may own or mutate these architecture
# facts — a runtime module may only own OPERATIONAL facts (Plugin Version -> exact artifact
# resolution, runtime compatibility/availability status, activation coordination) that do
# not overlap Plugin Definition identity/taxonomy. There is no second plugin registry.
#
# schema_version: 1 (Constitution does not mandate a filename/schema — this repository's
# own convention, storage/format deferred per Chapter 13 §13.14 / Chapter 7 §7.5).
#
# Field reference:
#   module_id                  stable machine-readable ID, snake/kebab-case, never reused
#   name                       display name
#   module_type                exactly one of: compute_engine | projection | runtime_service
#                               (Chapter 7 §7.1 — exhaustive, no invented fourth type)
#   hybrid                     null, or {secondary_responsibility, condition, adr_status}
#                               when module carries a secondary core responsibility of
#                               another taxonomy type (Chapter 7 §7.1, requires ALL 4
#                               conditions incl. ADR)
#   responsibilities           bounded list of what this module owns/does
#   implements_capabilities    capability_id(s) from context-map.yaml (must already exist there)
#   serves_contexts            domain_context_id(s) from context-map.yaml (must already exist there)
#   owns_authoritative_state   true | false | deferred — per I-12, only true if this module
#                               is the sole authoritative source for the state it emits
#   consumes                   contract categories consumed (event | query | command), not
#                               field-level schema
#   emits                      contract categories emitted (event | query | command), not
#                               field-level schema
#   depends_on                 module_id(s) this module requires (dependency direction)
#   forbidden_dependencies     module_id(s) this module MUST NOT depend on (boundary enforcement)
#   plugin_relation            none | hosts | is_plugin_definition | manages_release
#                               ("manages_release" = operational Plugin Version/artifact/
#                               runtime-compatibility/activation tracking ONLY — never
#                               Plugin Definition identity/taxonomy ownership, P11-A-MAJ-02)
#   security_classification    none | trust_boundary_candidate | custody_adjacent |
#                               secret_consuming — IDENTIFICATION ONLY, no implementation
#                               design (Package 1.2 scope)
#   phase                      Phase 1 sub-package this module is identified under (1.1)
#                               and which later package (1.2-1.6) elaborates its detailed
#                               architecture
#   status                     candidate (this document authors no module beyond `candidate`)
#   notes                      deferred items / authority gaps / rationale, when applicable
#   quality_tier                OPTIONAL, present only once a module's Chapter 13 §13.4
#                               criticality classification has been Product-Owner-approved
#                               (branch 1: "Runtime module -> resolve tier từ
#                               module-registry.yaml", Chapter 7 §7.5) -- {tier,
#                               approved_by, approved_at}. `tier` is one of the four Chapter
#                               13 §13.4 tier names (exact string, e.g. "Tier 2 —
#                               Supporting"); coverage floor and any additional
#                               tier-triggered requirement (Chaos/Parity Test) are NOT
#                               duplicated here -- both derive from Chapter 13 §13.4's own
#                               Locked table for the pinned tier value (I-12: one
#                               authoritative source per concept). Absence of this field on
#                               a module means its tier is UNRESOLVED (Chapter 13 §13.4
#                               point 4, fail-closed) -- absence is not "no tier"/"Tier 3
#                               default," it is "not yet classified."

schema_version: 1
registry_id: phase-1-module-registry
version: "1.6"
status: Draft
package: "1.1"
package_lifecycle: candidate
generated_at: "2026-08-10"

modules:

  # ---------------------------------------------------------------------
  # Data Layer
  # ---------------------------------------------------------------------

  - module_id: market-reference-service
    name: Market Reference Service
    module_type: runtime_service
    hybrid: null
    responsibilities:
      - "Sở hữu authoritative Instrument/Venue identity, precision/tick/lot metadata, trading calendar/session."
    implements_capabilities: [market-reference]
    serves_contexts: [instrument-venue-reference]
    owns_authoritative_state: true
    consumes: [command]
    emits: [event, query]
    depends_on: []
    forbidden_dependencies: []
    plugin_relation: none
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.3-A" }
    status: candidate
    notes: "Root module — no Phase 1 dependency."
    quality_tier: { tier: "Tier 2 — Supporting", approved_by: "Product Owner", approved_at: "2026-08-19T19:27:00+07:00" }

  - module_id: market-data-ingestion
    name: Market Data Ingestion
    module_type: runtime_service
    hybrid: null
    responsibilities:
      - "Quan sát và ghi nhận Candle observed/closed/corrected từ venue theo bitemporal semantics (Chapter 5)."
      - "Chuẩn hóa venue-specific schema về canonical domain language."
    implements_capabilities: [market-data]
    serves_contexts: [market-data-observation]
    owns_authoritative_state: true
    consumes: [query]
    emits: [event]
    depends_on: [market-reference-service]
    forbidden_dependencies: []
    plugin_relation: none
    security_classification: trust_boundary_candidate
    phase: { identified_in: "1.1", elaborated_by: "1.3-A" }
    status: candidate
    notes: "External venue connection boundary — security classification identification only, no auth/isolation design (Package 1.2)."
    quality_tier: { tier: "Tier 2 — Supporting", approved_by: "Product Owner", approved_at: "2026-08-20T15:42+07:00" }

  # ---------------------------------------------------------------------
  # Structure / Regime (independent, per ADR-003/ADR-014)
  # ---------------------------------------------------------------------

  - module_id: structure-engine
    name: Structure Engine
    module_type: compute_engine
    hybrid: null
    responsibilities:
      - "Suy diễn Swing/BOS/CHoCH và structure semantics từ Candle — analytical output, không external side effect."
    implements_capabilities: [market-structure]
    serves_contexts: [market-structure-analysis]
    owns_authoritative_state: true
    consumes: [event]
    emits: [event]
    depends_on: [market-data-ingestion]
    forbidden_dependencies: []
    plugin_relation: none
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.3-A" }
    status: candidate
    notes: null
    quality_tier: { tier: "Tier 1 — Core Logic", approved_by: "Product Owner", approved_at: "2026-08-22" }

  - module_id: raw-regime-engine
    name: Raw Regime Engine
    module_type: compute_engine
    hybrid: null
    responsibilities:
      - "Suy diễn Regime (trending/ranging/volatile) trực tiếp từ raw market data, ĐỘC LẬP HOÀN TOÀN với Structure Engine (ADR-003, hiệu lực qua ADR-014)."
    implements_capabilities: [market-regime]
    serves_contexts: [raw-regime-analysis]
    owns_authoritative_state: true
    consumes: [event]
    emits: [event]
    depends_on: [market-data-ingestion]
    forbidden_dependencies: [structure-engine]
    plugin_relation: none
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.3-A" }
    status: candidate
    notes: >
      DEFERRED COVERAGE — "Structure-aware Regime" (phase-1-plan.md §5.1, docs/architecture/README.md
      reference pipeline): no capability_id/domain_context_id for this responsibility exists in
      context-map.yaml today (Chapter 4 §4.2 authority — Domain Context identity is NOT
      module-registry's to invent). Package 1.1 CANNOT bind a module to this responsibility without
      first registering the missing Domain Context/Capability. Blocked on: Domain Context/Capability
      registration (Chapter 4 §4.2, Product Owner + Domain Contract authoring decision), out of
      Package 1.1 scope. Raw Regime Engine module above covers ONLY the already-registered
      raw-regime-analysis context.
    quality_tier: { tier: "Tier 1 — Core Logic", approved_by: "Product Owner", approved_at: "2026-08-23T08:57:00+07:00" }

  # P13B-IRB-MAJ-01/MAJ-02/MAJ-03 bounded correction (2026-08-04, v0.3 -> v0.4): confirmed
  # Package 1.3-B Review A/Independent Review B findings against feature.md/context.md
  # (Package 0.2-B3/B4, Consolidated Stable, unchanged). feature-engine and
  # context-aggregator both directly consume candle-closed/candle-corrected already, so
  # both now declare market-data-ingestion in depends_on (previously omitted — a registry
  # gap, not a new fan-in). context-aggregator emits both event (MarketContextSnapshot /
  # MarketContextFactInvalidated, context.md §3/§4 — append-only projection snapshot/
  # invalidation records) and query — previously declared query only. module_type,
  # owns_authoritative_state, responsibilities, and forbidden_dependencies are unchanged
  # for both modules; no module added/removed, no other module touched.
  - module_id: feature-engine
    name: Feature Engine
    module_type: compute_engine
    hybrid: null
    responsibilities:
      - "Fan-in CÓ CHỌN LỌC (Definition-pinned direct fan-in, ADR-014) từ Structure và Regime output thành Feature."
    implements_capabilities: [feature-engineering]
    serves_contexts: [feature-engineering]
    owns_authoritative_state: true
    consumes: [event]
    emits: [event]
    depends_on: [market-data-ingestion, structure-engine, raw-regime-engine]
    forbidden_dependencies: []
    plugin_relation: none
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.3-B" }
    status: candidate
    notes: null

  - module_id: context-aggregator
    name: Context Aggregator
    module_type: projection
    hybrid: null
    responsibilities:
      - "CQRS read-model tổng hợp Structure+Regime+Feature+market state cho Strategy tiêu thụ."
    implements_capabilities: [context-aggregation]
    serves_contexts: [context-projection]
    owns_authoritative_state: false
    consumes: [event]
    emits: [event, query]
    depends_on: [market-data-ingestion, structure-engine, raw-regime-engine, feature-engine]
    forbidden_dependencies: []
    plugin_relation: none
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.3-B" }
    status: candidate
    notes: "Type 2 Projection (Chapter 7 §7.4) — KHÔNG BAO GIỜ sở hữu business decision; cấm phát sinh authoritative domain fact. `emits: [event, query]` — event là append-only projection snapshot/invalidation record (MarketContextSnapshot/MarketContextFactInvalidated), KHÔNG phải authoritative domain fact theo nghĩa Chapter 7 §7.4 cấm; ranh giới immutable-record-integrity vs authoritative-domain-ownership, xem docs/architecture/engine/feature-context-architecture.md."

  # ---------------------------------------------------------------------
  # Account
  # ---------------------------------------------------------------------

  - module_id: account-service
    name: Account Service
    module_type: runtime_service
    hybrid: null
    responsibilities:
      - "Sở hữu Trading Account identity, account boundary binding (ADR-012), environment distinction (PAPER/LIVE), lifecycle tối thiểu."
      - "KHÔNG sở hữu credential/secret material (I-11)."
    implements_capabilities: [account-management]
    serves_contexts: [account-reference]
    owns_authoritative_state: true
    consumes: [command]
    emits: [event, query]
    depends_on: []
    forbidden_dependencies: []
    plugin_relation: none
    security_classification: custody_adjacent
    phase: { identified_in: "1.1", elaborated_by: "1.2" }
    status: candidate
    notes: "Custody-adjacent identification only — no credential/key-management design (Package 1.2)."

  # ---------------------------------------------------------------------
  # Custody & Signing (ADR-017 v0.2, Approved 2026-08-04T20:08:00+07:00 — Option C split)
  # ---------------------------------------------------------------------

  - module_id: custody-signing-service
    name: Custody & Signing Service
    module_type: runtime_service
    hybrid: null
    responsibilities:
      - "Module DUY NHẤT trong toàn platform được phép sử dụng exchange credential trực tiếp (I-11; ADR-017 §3.1)."
      - "Resolve account-service's opaque `credential_reference` thành credential-binding/signing-eligibility state, dưới đúng Account Boundary/environment scope."
      - "Nhận bounded signing request, trả về signature/signed-payload evidence — KHÔNG BAO GIỜ trả raw secret material cho caller."
      - "Fail closed khi credential_reference absent/revoked/stale/unauthorized/environment-mismatched/boundary-mismatched (I-6; ADR-017 §11)."
    implements_capabilities: []
    serves_contexts: []
    owns_authoritative_state: true
    consumes: [command, query]
    emits: [event, query]
    depends_on: [account-service]
    forbidden_dependencies: [decision-authority-service, risk-gateway, execution-engine, exchange-adapter, strategy-engine, strategy-plugin-host, decision-evaluation-engine, context-aggregator, position-projection, command-query-api-surface]
    plugin_relation: none
    security_classification: secret_consuming
    phase: { identified_in: "1.1", elaborated_by: "1.2" }
    status: candidate
    notes: >
      ADR-017 v0.2 (Approved, 2026-08-04T20:08:00+07:00, Product Owner — Option C split
      selected). Stage 1 architecture-boundary registration ONLY (ADR-017 §9a) — does NOT
      activate any LIVE execution path. owns_authoritative_state: true scoped strictly to
      credential-binding/eligibility/revocation state and signing-attempt/signing-outcome
      operational facts — NOT raw secret material itself (outside Domain Contract scope,
      forbidden per ADR-017 §3.3), NOT Account identity, NOT Decision/RiskEvaluation/
      Execution Intent/Order/ExecutionResult/Fill/Position, NOT kill-switch state. Kill-
      switch: future fail-safe participant SELECTED by ADR-017 as a consequence of
      I-6/I-8/I-11 combined (ADR-017 §10) — I-8 does not literally name this module;
      authoritative kill-switch-state ownership remains UNRESOLVED, not claimed here.
      Published authorization evidence carried within a bounded signing request does NOT
      create a dependency on decision-authority-service or risk-gateway (ADR-017 §8.4) —
      forbidden_dependencies enforces this at module-boundary level. `security_classification:
      secret_consuming` is a pre-existing enum value from this file's own field-reference
      comment (line 48-50), first assigned here — not a new value. Implementation (Vault/
      KMS/HSM binding, signing algorithm, credential rotation protocol) is forbidden scope
      (ADR-017 §3.3) — NOT authored here.
      `phase.elaborated_by` corrected v0.5 -> v0.6 (bounded correction, đóng
      `P11V05-A-MAJ-01`/`P11-v0.5-IRB-MAJ-01`, HISTORICAL): v0.5's `elaborated_by: "1.2"`
      was a false package-coverage/readiness claim — [security-custody-baseline.md](../security-custody-baseline.md)
      v0.2 was authored and reviewed BEFORE this module existed (it registers only at
      Package 1.1 v0.5, AFTER Package 1.2 v0.2's review-clean state), so Package 1.2 had
      NOT actually elaborated it at that time. Corrected to `null` in v0.6 pending genuine
      elaboration and review.
      `phase.elaborated_by` assigned v0.6 -> v0.7 (semantic Package 1.1 alignment
      transaction, 2026-08-05, `Package 1.1 Custody Assignment Alignment Executor`):
      `null -> "1.2"`. This assignment follows, rather than precedes, genuine elaboration —
      [security-custody-baseline.md](../security-custody-baseline.md) v0.4 §4a fully
      elaborates this module's architecture (authority model, credential-reference model,
      signing-request lifecycle, caller-authorization boundary, fail-closed rules, kill-
      switch participation, audit/provenance) and passed Review A CLEAN + Independent
      Review B CLEAN (Blocker 0/Major 0/Minor 0) after its own v0.4 bounded correction —
      the exact precondition v0.6's correction said was missing. Package 1.2 v0.4 remains
      the elaborating artifact; this transaction does NOT modify Package 1.2 semantics,
      does NOT consolidate Package 1.2, and does NOT activate LIVE. `exchange-adapter`
      below is unaffected — same deferred/null representation, no elaborating package
      exists for it.

  - module_id: exchange-adapter
    name: Exchange Adapter
    module_type: runtime_service
    hybrid: null
    responsibilities:
      - "Venue protocol translation và external venue transport."
      - "Bounded signing-request coordination với custody-signing-service — nhận signature/signed payload, KHÔNG BAO GIỜ raw secret."
      - "Ghi nhận raw venue-interaction evidence trực tiếp chứng kiến tại external venue boundary (ADR-017 §3.2a) — KHÔNG platform ExecutionObservation/ExecutionResult."
    implements_capabilities: []
    serves_contexts: []
    owns_authoritative_state: true
    consumes: [command]
    emits: [event]
    depends_on: [custody-signing-service]
    forbidden_dependencies: [decision-authority-service, risk-gateway, strategy-engine, strategy-plugin-host, decision-evaluation-engine, context-aggregator, position-projection, account-service, command-query-api-surface]
    plugin_relation: none
    security_classification: trust_boundary_candidate
    phase: { identified_in: "1.1", elaborated_by: null }
    status: candidate
    notes: >
      ADR-017 v0.2 (Approved, 2026-08-04T20:08:00+07:00, Product Owner — Option C split
      selected). Stage 1 architecture-boundary registration ONLY (ADR-017 §9a) — LIVE
      execution path NOT activated by this registration: execution-engine.depends_on does
      NOT include this module (see execution-engine notes below); an active
      execution-engine -> exchange-adapter edge is a Stage 2 concern requiring a separate
      future LIVE architecture/governance authorization (ADR-017 §9a), NOT added here.
      owns_authoritative_state: true scoped STRICTLY to raw venue-interaction evidence
      directly witnessed at the external venue boundary (venue interaction attempt,
      transport transmission outcome, transport failure/timeout evidence, venue
      acceptance/rejection acknowledgment, venue-side external-reference evidence,
      venue-originating order/execution evidence, protocol response evidence — ADR-017
      §3.2a) — NOT platform ExecutionObservation, ExecutionResult, executed/not-executed
      business interpretation, Order, Fill, execution economics, Position, Decision,
      RiskEvaluation, Execution Intent, or kill-switch state; execution-result-processor
      (unchanged) remains sole authority interpreting this evidence into execution-
      observation/ExecutionResult under a future LIVE Domain Contract (not yet authored).
      Kill-switch: future execution-suspension participant expressly ANTICIPATED by I-8
      Scope ("Risk Gateway, Execution Engine, mọi Exchange Adapter") — authoritative
      kill-switch-state ownership remains UNRESOLVED, not claimed here. `phase.
      elaborated_by` intentionally `null` — Exchange Adapter's full functional
      architecture is a LIVE-prerequisite outside all nine current Phase 1 package scopes
      (1.1/1.2/1.3-A-D/1.4/1.5/1.6, all PAPER-focused per phase-1-plan.md v0.4); no future
      package ID is invented per ADR-017's own instruction — assignment deferred to a
      future Phase 1 amendment or new package, Product Owner decision, out of this
      transaction's scope.

  # ---------------------------------------------------------------------
  # Strategy / Plugin
  # ---------------------------------------------------------------------

  - module_id: strategy-engine
    name: Strategy Engine
    module_type: runtime_service
    hybrid: null
    responsibilities:
      - "Sở hữu Strategy Definition Version identity (ADR-013) và Strategy Instance identity (bốn trục evidence độc lập + account_id + instrument_selection)."
    implements_capabilities: [strategy-management]
    serves_contexts: [strategy-definition]
    owns_authoritative_state: true
    consumes: [command, query]
    emits: [event, query]
    depends_on: [account-service]
    forbidden_dependencies: []
    plugin_relation: none
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.3-C" }
    status: candidate
    notes: null

  - module_id: plugin-release-manager
    name: Plugin Release Manager
    module_type: runtime_service
    hybrid: null
    responsibilities:
      - "Resolve Plugin Version → exact Package/Build Artifact content identity (content hash, target platform, build identity) HOẶC immutable release manifest (Chapter 9 §9.1, mô hình A/B) — operational resolution, KHÔNG architecture identity."
      - "Theo dõi runtime compatibility/availability status của Plugin Runtime replica đã deploy."
      - "Điều phối activation/deactivation boundary (Chapter 9 §9.5 — validated compatibility set, atomic activation) — operational coordination fact, KHÔNG Plugin Definition identity."
      - "KHÔNG sở hữu Plugin Definition identity, taxonomy type, hay bất kỳ architecture responsibility nào — authority đó thuộc DUY NHẤT `module-registry.yaml` (Chapter 7 §7.5, Chapter 9 §9.1). Module này KHÔNG phải, và KHÔNG tạo, một plugin registry thứ hai."
    implements_capabilities: [strategy-management]
    serves_contexts: [strategy-definition]
    owns_authoritative_state: true
    consumes: [command]
    emits: [event, query]
    depends_on: []
    forbidden_dependencies: []
    plugin_relation: manages_release
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.3-C" }
    status: candidate
    notes: >
      P11-A-MAJ-02 correction — renamed từ "Plugin Registry Service" (đóng, xem
      system-decomposition.md §12b). Runtime service này CHỈ owns_authoritative_state
      cho operational fact (Package/Build Artifact resolution record, runtime
      compatibility/availability status, activation coordination record) KHÔNG overlap
      với architecture identity (Plugin Definition/taxonomy) — architecture identity đó
      resolve DUY NHẤT từ chính entry `strategy-plugin-host` (và mọi entry khác) trong
      `module-registry.yaml` này. Distinct khỏi `strategy-plugin-host` (EXECUTES plugin
      logic).

  - module_id: strategy-plugin-host
    name: Strategy Plugin Host
    module_type: compute_engine
    hybrid: null
    responsibilities:
      - "Host/execute Strategy Plugin (Decision Advisor) logic — Compute Engine điển hình theo Chapter 9 §9.1."
      - "Tuân Decision-time visibility (Chapter 9 §9.5) — mọi input cursor-bounded, cấm đọc ambient/current state."
    implements_capabilities: [strategy-management]
    serves_contexts: [strategy-definition]
    owns_authoritative_state: false
    consumes: [query]
    emits: [event]
    depends_on: [strategy-engine, context-aggregator, plugin-release-manager]
    forbidden_dependencies: [execution-engine, risk-gateway, paper-execution-boundary, decision-authority-service]
    plugin_relation: hosts
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.3-C" }
    status: candidate
    notes: >
      Strategy Plugin cannot bypass Decision or Risk Gateway (I-4, I-7) — forbidden_dependencies
      enforces this at module-boundary level. Output is candidate analytical signal, NOT the
      authoritative Decision fact — `decision-authority-service` owns the authoritative record
      (ADR-016 v0.8, Candidate B; `decision-evaluation-engine` performs non-authoritative
      evaluation upstream of it). Explicit forbidden_dependency on `decision-authority-service`
      added (ADR-016 v0.8 correction) — Plugin must never directly reach authority append.

  # ---------------------------------------------------------------------
  # Decision / Risk / Execution (C7 chain)
  # ---------------------------------------------------------------------

  - module_id: decision-evaluation-engine
    name: Decision Evaluation Engine
    module_type: compute_engine
    hybrid: null
    responsibilities:
      - "Platform-owned deterministic Decision evaluation của Strategy Instance tại decision_context_cursor (ADR-010, Chapter 8 §8.4-§8.5) — sinh non-authoritative evaluation output/proposal."
      - "KHÔNG sở hữu Decision identity, KHÔNG Decision append authority, KHÔNG Trade Intent identity, KHÔNG Risk approval authority, KHÔNG Execution authority (ADR-016 v0.8, Candidate B)."
    implements_capabilities: [decision-management]
    serves_contexts: [strategy-decision]
    owns_authoritative_state: false
    consumes: [event, query]
    emits: [event]
    depends_on: [strategy-engine, strategy-plugin-host, context-aggregator]
    forbidden_dependencies: [execution-engine, risk-gateway, paper-execution-boundary]
    plugin_relation: none
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.3-C" }
    status: candidate
    notes: >
      ADR-016 v0.8 (Approved, 2026-08-04, Product Owner — Candidate B/split selected under
      Mechanism A) — tách từ `decision-engine` hybrid cũ (Candidate A, KHÔNG selected;
      superseded, xem "Alternatives considered" trong ADR-016 cho phân tích lịch sử). Output
      là non-authoritative evaluation proposal — `decision-authority-service` (dưới) sở hữu
      validate/append. KHÔNG modeled như second Strategy Plugin Host, KHÔNG authoritative
      Decision/Trade Intent writer, KHÔNG projection, KHÔNG runtime side-effect authority.
      Forbidden dependency enforces non-bypass của Risk Gateway/Execution (cùng nguyên tắc
      `strategy-plugin-host`). Ranh giới CHÍNH XÁC với `strategy-plugin-host` CHƯA resolve
      (residual risk #1, ADR-016 v0.8 Accepted risks) — CẦN further contract work, KHÔNG
      resolve tại transaction này.

  - module_id: decision-authority-service
    name: Decision Authority Service
    module_type: runtime_service
    hybrid: null
    responsibilities:
      - "Sole invariant-validation authority; sole authoritative Decision append authority; sole Decision identity authority (ADR-010, Chapter 8 §8.4/§8.5) và Trade Intent identity."
      - "Final acceptance/rejection tại authority boundary — validate evaluation proposal identity/version/cursor/eligibility từ `decision-evaluation-engine` trước khi append."
    implements_capabilities: [decision-management]
    serves_contexts: [strategy-decision]
    owns_authoritative_state: true
    consumes: [event, query]
    emits: [event]
    depends_on: [decision-evaluation-engine, strategy-engine]
    forbidden_dependencies: [execution-engine, paper-execution-boundary]
    plugin_relation: none
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.3-C" }
    status: candidate
    notes: >
      ADR-016 v0.8 (Approved, 2026-08-04, Product Owner — Candidate B/split selected under
      Mechanism A) — thay thế `decision-engine` hybrid cũ. Decision TRƯỚC Trade Intent
      (preserved). Forbidden dependency enforces Decision cannot bypass Risk Gateway
      downstream. KHÔNG sở hữu Strategy Plugin advisory computation, Context Aggregator
      responsibility, Risk approval, hay Execution side effect nào. Mechanism A: ADR-016
      v0.8 tự nó amend hiệu lực kiểm soát của ADR-015 SCOPED cho module này + dependency
      edge liên quan — ADR-015 vẫn controlling cho 21/22 module còn lại (không đổi).

  - module_id: risk-gateway
    name: Risk Gateway
    module_type: runtime_service
    hybrid: null
    responsibilities:
      - "Sở hữu RiskEvaluation identity (bốn trục risk evidence độc lập) và Execution Intent identity."
      - "Risk Policy logic (exposure limit, approve/reject, risk-increasing detection, kill switch) — Chapter 3 §3.1 — LÀ business logic hợp lệ của đúng responsibility này (Chapter 7 §7.3: Type 3 Runtime Service KHÔNG có nghĩa chỉ làm I/O thuần túy), KHÔNG phải secondary taxonomy type/hybrid. Runtime Service taxonomy CHO PHÉP authoritative business policy/control logic như một phần bình thường của primary responsibility — không cần khai báo hybrid, không cần ADR."
    implements_capabilities: [risk-management]
    serves_contexts: [risk-gateway]
    owns_authoritative_state: true
    consumes: [event]
    emits: [event]
    depends_on: [decision-authority-service, account-service]
    forbidden_dependencies: []
    plugin_relation: none
    security_classification: trust_boundary_candidate
    phase: { identified_in: "1.1", elaborated_by: "1.3-D" }
    status: candidate
    notes: "Trade Intent TRƯỚC RiskEvaluation TRƯỚC Execution Intent (preserved). Risk Gateway consumes authoritative Decision output từ `decision-authority-service` (ADR-016 v0.8) — KHÔNG trực tiếp từ `decision-evaluation-engine` (non-authoritative)."

  - module_id: execution-engine
    name: Execution Engine
    module_type: runtime_service
    hybrid: null
    responsibilities:
      - "Sở hữu Order identity (bất biến), OrderCreationAttempt, OrderSubmissionRequest."
    implements_capabilities: [execution-management]
    serves_contexts: [order-management]
    owns_authoritative_state: true
    consumes: [event]
    emits: [event, command]
    depends_on: [risk-gateway, paper-execution-boundary]
    forbidden_dependencies: [strategy-engine, strategy-plugin-host, context-aggregator]
    plugin_relation: none
    security_classification: trust_boundary_candidate
    phase: { identified_in: "1.1", elaborated_by: "1.3-D" }
    status: candidate
    notes: >
      Execution cannot accept unapproved Trade Intent directly (preserved) —
      forbidden_dependencies blocks any path bypassing Risk Gateway. depends_on
      intentionally does NOT include `exchange-adapter` (ADR-017 v0.2, Approved,
      2026-08-04T20:08:00+07:00) — a future LIVE venue-submission dependency is
      authorized in principle (ADR-017 §8/§9a) but registering it as an active edge is a
      Stage 2 concern requiring separate future LIVE architecture/governance
      authorization; the current `paper-execution-boundary` PAPER dependency is
      unchanged.

  - module_id: execution-result-processor
    name: Execution Result Processor
    module_type: runtime_service
    hybrid: null
    responsibilities:
      - "Sở hữu ExecutionResult identity — observation trả về từ bounded PAPER execution boundary."
      - "ExecutionResultProcessingAttempt."
    implements_capabilities: [execution-management]
    serves_contexts: [execution-result-management]
    owns_authoritative_state: true
    consumes: [event]
    emits: [event]
    depends_on: [execution-engine, paper-execution-boundary]
    forbidden_dependencies: []
    plugin_relation: none
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.3-D" }
    status: candidate
    notes: null

  - module_id: fill-processor
    name: Fill Processor
    module_type: runtime_service
    hybrid: null
    responsibilities:
      - "Sở hữu Fill identity (bất biến) — quantity/price đã executed cho ExecutionResult result_type=EXECUTED."
    implements_capabilities: [execution-management]
    serves_contexts: [fill-management]
    owns_authoritative_state: true
    consumes: [event]
    emits: [event]
    depends_on: [execution-result-processor]
    forbidden_dependencies: []
    plugin_relation: none
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.3-D" }
    status: candidate
    notes: null

  - module_id: position-projection
    name: Position Projection
    module_type: projection
    hybrid: null
    responsibilities:
      - "Deterministic, NON-authoritative projection dẫn xuất từ visible-valid Fill history (Chapter 7 §7.4 Type 2)."
    implements_capabilities: [execution-management]
    serves_contexts: [position-management]
    owns_authoritative_state: false
    consumes: [event]
    emits: [query]
    depends_on: [fill-processor]
    forbidden_dependencies: []
    plugin_relation: none
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.3-D" }
    status: candidate
    notes: "Projection/read model KHÔNG BAO GIỜ authoritative source (preserved, I-12)."

  - module_id: replay-integration-service
    name: Replay Integration Service
    module_type: projection
    hybrid: null
    responsibilities:
      - "ReplayStateProjection — select + fold TOÀN BỘ authoritative fact (Decision→Position) tại canonical Replay Cursor. KHÔNG author fact mới."
    implements_capabilities: [execution-management]
    serves_contexts: [replay-integration]
    owns_authoritative_state: false
    consumes: [event]
    emits: [query]
    depends_on: [decision-authority-service, risk-gateway, execution-engine, execution-result-processor, fill-processor, position-projection]
    forbidden_dependencies: []
    plugin_relation: none
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.3-A" }
    status: candidate
    notes: "Chapter 8 §8.5 canonical Replay Cursor authority — no ReplayDecision (preserved). Folds authoritative fact only — `decision-authority-service` (ADR-016 v0.8), KHÔNG `decision-evaluation-engine` (non-authoritative evaluation proposal, ngoài phạm vi Replay authoritative-fact scope)."

  # ---------------------------------------------------------------------
  # Backtest / Paper orchestration boundaries
  # ---------------------------------------------------------------------

  - module_id: backtest-orchestrator
    name: Backtest Orchestrator
    module_type: runtime_service
    hybrid: null
    responsibilities:
      - "Điều phối một bounded Backtest run gắn Strategy/version/configuration + stable run identity."
      - "Non-PAPER simulated authority — KHÔNG tái sử dụng PAPER Order/ExecutionResult/Fill/Position."
    implements_capabilities: [decision-management, risk-management]
    serves_contexts: [strategy-decision, risk-gateway]
    owns_authoritative_state: deferred
    consumes: [event, query]
    emits: [event]
    depends_on: [strategy-engine, decision-evaluation-engine, decision-authority-service, risk-gateway]
    forbidden_dependencies: [execution-engine, paper-execution-boundary, execution-result-processor, fill-processor, position-projection]
    plugin_relation: none
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.3-A" }
    status: candidate
    notes: >
      DEFERRED COVERAGE — DD-001 (Backtest Domain Contract/entity/event/schema): no Domain
      Contract exists yet for Backtest-specific facts. owns_authoritative_state cannot be
      resolved to true/false until DD-001 is addressed (Product Owner decision, out of
      Package 1.1 scope). This module identifies the ORCHESTRATION boundary only — does NOT
      invent Backtest entities (BacktestOrder/BacktestFill/BacktestPosition/
      BacktestExecutionResult or equivalent remain forbidden per DD-001's own constraint).
      ADR-016 v0.8 correction: `decision-engine` dependency split into
      `decision-evaluation-engine` + `decision-authority-service` — bounded Backtest run
      cần cả evaluation VÀ authority append, không chỉ authoritative record access.
      ADR-019 v0.2 (Approved, 2026-08-06) alignment: `consumes` mở rộng `[event]` →
      `[event, query]` — module nay directly query-answerable qua `command-query-api-
      surface` (edge mới, cùng transaction), composing một non-authoritative correlation
      view của Decision (`decision-authority-service`)/RiskEvaluation (`risk-gateway`)
      fact ĐÃ tồn tại bằng run identity (ADR-018) — KHÔNG một fact/entity/event mới,
      KHÔNG authority transfer nào. `emits`/`module_type`/`hybrid`/`owns_authoritative_
      state` KHÔNG đổi (precedent: `decision-authority-service`, cùng shape).

  - module_id: paper-execution-boundary
    name: Paper Execution Boundary
    module_type: runtime_service
    hybrid: null
    responsibilities:
      - "Bounded PAPER execution boundary mà OrderSubmissionRequest/ExecutionResult tham chiếu — chưa chứng minh venue acceptance/execution thật."
    implements_capabilities: [execution-management]
    serves_contexts: [order-management, execution-result-management]
    owns_authoritative_state: false
    consumes: [command]
    emits: [event]
    depends_on: []
    forbidden_dependencies: []
    plugin_relation: none
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.3-D" }
    status: candidate
    notes: >
      DEFERRED COVERAGE — DD-003 (PAPER-context authoritative Decision establishment
      mechanism): the exact trigger/mechanism for establishing an eligible PAPER-context
      Decision lineage is NOT decided (mandatory before UC-011 runtime design, per MANIFEST
      Deferred Decisions register). This module identifies the BOUNDARY only, does not invent
      the establishment mechanism.

  # ---------------------------------------------------------------------
  # Cross-cutting: API surface, Review evidence, UX shell
  # ---------------------------------------------------------------------

  - module_id: command-query-api-surface
    name: Command/Query/Event API Surface
    module_type: runtime_service
    hybrid: null
    responsibilities:
      - "Expose command/query/event contract surface cho mọi module authoritative/projection — routing/exposure, KHÔNG business logic riêng."
    implements_capabilities: []
    serves_contexts: []
    owns_authoritative_state: false
    consumes: [event, query, command]
    emits: [query, command]
    depends_on: [market-reference-service, market-data-ingestion, structure-engine, raw-regime-engine, feature-engine, context-aggregator, account-service, strategy-engine, decision-authority-service, risk-gateway, execution-engine, execution-result-processor, fill-processor, position-projection, replay-integration-service, review-evidence-service, backtest-orchestrator]
    forbidden_dependencies: []
    plugin_relation: none
    security_classification: trust_boundary_candidate
    phase: { identified_in: "1.1", elaborated_by: "1.4" }
    status: candidate
    notes: "implements_capabilities/serves_contexts intentionally empty — this module is a routing/exposure layer, not a capability owner (avoids inventing a competing capability identity outside context-map.yaml). ADR-016 v0.8 correction: `decision-engine` → `decision-authority-service` only (authoritative) — `decision-evaluation-engine` intentionally excluded, same pattern as `strategy-plugin-host` (compute_engine, non-authoritative modules are not exposed via this routing layer). ADR-019 v0.2 (Approved, 2026-08-06) alignment: `backtest-orchestrator` added to `depends_on` — the NAV-003 Gap A query-exposure route, mechanical transcription only; `command-query-api-surface` itself gains no new authority, remains routing/exposure-only, `owns_authoritative_state: false` unchanged."

  - module_id: review-evidence-service
    name: Review Evidence Service
    module_type: projection
    hybrid: null
    responsibilities:
      - "Read-only aggregation/trace query surface phục vụ Decision→Position lineage trace, historical-state comparison, correction inspection (UC-016–UC-018)."
      - "Performs the non-authoritative, interval-bounded VIEW-002 / UC-003 Research verification existence check over existing Decision, RiskEvaluation, Execution Intent, Order and ExecutionResult facts, producing a workflow-visible PASSED / FAILED / INDETERMINATE query result without recomputing or replacing source authority."
      - "Owns the non-authoritative VIEW-003 / UC-005 Decision replay-parity comparison orchestration: obtains the recorded Decision via the existing decision-authority-service dependency, delegates deterministic pinned recomputation to decision-evaluation-engine (review-evidence-service does not perform canonical Decision evaluation itself), performs the final structured Canonical Decision Semantic Representation comparison, and exposes a workflow-visible MATCH / MISMATCH / INDETERMINATE query result without appending, replacing, or authorizing any Decision."
    implements_capabilities: []
    serves_contexts: []
    owns_authoritative_state: false
    consumes: [event]
    emits: [query]
    depends_on: [decision-authority-service, risk-gateway, execution-engine, execution-result-processor, fill-processor, position-projection, replay-integration-service, decision-evaluation-engine]
    forbidden_dependencies: []
    plugin_relation: none
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.5" }
    status: candidate
    notes: "Cross-cutting read/evidence layer — no new authoritative fact, no recomputation performed BY review-evidence-service itself (PR-030 no-recompute preserved; deterministic recomputation for VIEW-003 parity is delegated to decision-evaluation-engine, a separate non-authoritative module). ADR-016 v0.8 correction: `decision-engine` → `decision-authority-service` (Decision→Position lineage trace uses authoritative fact only). ADR-020 v0.1 (Approved, 2026-08-06) alignment: VIEW-002/UC-003 existence-check responsibility added — the four source modules (decision-authority-service, risk-gateway, execution-engine, execution-result-processor) were already present in depends_on; consumes/emits unchanged ([event]/[query]); review-evidence-service gains no new authority, remains owns_authoritative_state: false. ADR-021 v0.1 (Approved, 2026-08-07) alignment: VIEW-003/UC-005 replay-parity comparison responsibility added — ONE new dependency edge (decision-evaluation-engine, for pinned recomputation delegation only); decision-authority-service edge (recorded-side) already present; consumes/emits unchanged ([event]/[query]); review-evidence-service gains no new authority, remains owns_authoritative_state: false; final comparison and query exposure stay at review-evidence-service, recomputation stays non-authoritative at decision-evaluation-engine. The exact request/response interaction mechanism between review-evidence-service and decision-evaluation-engine (synchronous vs. asynchronous, correlation, timeout, failure codes, transport) is NOT resolved by this depends_on edge — carried forward as an unresolved implementation/interaction-mechanism gap, same pattern as the Package 1.5 contract-category interaction gap (database-architecture.md §11)."

  - module_id: ux-application-shell
    name: UX Application Shell
    module_type: runtime_service
    hybrid: null
    responsibilities:
      - "Frontend application shell hosting workspace/navigation/research/replay/backtest/paper/review surfaces — command orchestration for user-initiated action."
    implements_capabilities: []
    serves_contexts: []
    owns_authoritative_state: false
    consumes: [query]
    emits: [command]
    depends_on: [command-query-api-surface]
    forbidden_dependencies: [structure-engine, raw-regime-engine, feature-engine, context-aggregator, strategy-engine, decision-evaluation-engine, decision-authority-service, risk-gateway, execution-engine, execution-result-processor, fill-processor, position-projection, replay-integration-service, review-evidence-service]
    plugin_relation: none
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.6" }
    status: candidate
    notes: "Forbidden direct-engine dependencies enforce UX must go through command-query-api-surface, not bypass it (same non-bypass principle applied to UI as to plugins)."

  - module_id: contract-compatibility-authority
    name: Contract Compatibility Authority
    module_type: compute_engine
    hybrid: null
    responsibilities:
      - "Deterministically evaluate Package 1.4 published semantic-contract compatibility against the exact pinned compatibility-policy root and, only when a valid evaluator Grant is in force for the declared scope, originate the immutable authoritative Compatibility Result for that scope."
    implements_capabilities: []
    serves_contexts: []
    owns_authoritative_state: true
    consumes: []
    emits: [event]
    depends_on: []
    forbidden_dependencies: []
    plugin_relation: none
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: "1.4" }
    status: candidate
    notes: "ADR-023 v0.5 (Approved, 2026-08-10) Declaration-tier registration only -- evaluator Grant NOT yet issued (module identity != evaluator grant, Declaration -> Grant -> Enforcement -> Verification, granted subset declared per Chapter 9 §9.6), NO operational permission exists yet to create a Compatibility Result. implements_capabilities/serves_contexts intentionally empty -- no existing context-map.yaml capability/Domain Context registered for this responsibility, and this module is not a candidate to invent one (same empty-field convention as command-query-api-surface). consumes: [] / depends_on: [] intentionally: the Package 1.4 published-contract artifact and the pinned compatibility-policy root are resolved via MANIFEST identity/applicability authority (ADR-022 §5.2, same reading pattern as ADR-022 §8/ADR-023 §8) -- NOT a runtime module dependency or consumes contract-category edge. emits: [event] reflects the eventual, Grant-conditional authoritative Compatibility Result issuance (Chapter 10 §10.4) -- same emits convention as other compute_engine/owns_authoritative_state: true modules (structure-engine/raw-regime-engine/feature-engine). Taxonomy: Type 1 Compute Engine (Chapter 7 §7.1) -- deterministic transform, no external side effect, no runtime interaction/orchestration/coordination/control responsibility (ADR-023 v0.4 root-cause correction, OPTION_A_COMPUTE_ENGINE). This module does NOT own runtime activation/enforcement coordination (ADR-023 §3.0a Part C, out of scope), Package 1.4 routing/exposure, compatibility-policy identity/version authority (ADR-022 §5.2, unchanged), evaluator Grant authority or Grant applicability authority (still unresolved, deferred to a future governed Grant transaction, ADR-023 §9), Decision/Risk/Execution semantics, or Plugin release/runtime compatibility."

  - module_id: stream-registry-authority
    name: Stream Registry Authority
    module_type: runtime_service
    hybrid: null
    responsibilities:
      - "Exclusive writer authority for the two Genesis-protected control-plane streams (canonical Lifecycle Stream `platform-lifecycle`, canonical Audit Stream `platform-audit`); sole authority permitted to author stream lifecycle boundaries (Genesis activation, stream activation/retirement, writer-authority handoff, registry-version transition) on the Lifecycle Stream, and audit-preservation facts on the Audit Stream, per Chapter 8 §8.3.1/§8.3.5/§8.4.1."
    implements_capabilities: []
    serves_contexts: []
    owns_authoritative_state: true
    consumes: []
    emits: [event]
    depends_on: []
    forbidden_dependencies: []
    plugin_relation: none
    security_classification: none
    phase: { identified_in: "1.1", elaborated_by: null }
    status: candidate
    notes: "ADR-036 v0.3 (Approved, 2026-08-26T10:44+07:00) Declaration-tier registration -- governed semantic module-registry alignment, EXACT mechanical transcription of the Approved ADR, containing no new architecture choice (same precedent already recorded above for contract-compatibility-authority/ADR-023). implements_capabilities/serves_contexts intentionally empty -- no existing context-map.yaml capability/Domain Context registered for this responsibility, and this module is not a candidate to invent one (same empty-field convention as contract-compatibility-authority/command-query-api-surface). consumes: [] intentionally -- no existing Approved authority requires a declared inbound contract category for this module; not invented here. depends_on: [] -- ADR-036 assigns this module no dependency on any existing module (it is a bootstrap/control-plane root, writing the two protected streams every other module's lifecycle/audit facts may ultimately rely on, per Chapter 8 §8.3.5's Genesis Registry exception); no dependency edge added to any existing module by this transaction. emits: [event] reflects the stream lifecycle-boundary and audit-preservation facts ADR-036 assigns to this module -- same emits convention as other owns_authoritative_state: true modules. Taxonomy: Type 3 Runtime Service (Chapter 7 §7.1) -- owns runtime coordination/control responsibility over Stream Registry lifecycle boundaries (ADR-036 §\"Protected streams\", OPTION selected there after confirming no existing registered module carries this responsibility). This module does NOT own Stream Registry topology/writer-authority decisions for the five analytical streams (ADR-036, already decided, unchanged), does NOT own the concrete `stream-registry.yaml` artifact's content (Chapter 8 §8.3.1, a separate future authoring transaction), does NOT own Event Contract `allowed_streams` eligibility (Chapter 8 §8.3.1, stays with each event type's own Event Contract), and does NOT own Feature/Structure/Raw-Regime/Decision/Execution domain semantics. `elaborated_by: null` -- no later Phase 1 sub-package (1.2-1.6) currently assigned to elaborate this module's detailed architecture; phase field left honestly unresolved rather than invented."
