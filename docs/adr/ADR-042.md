---
id: ADR-042
title: "Review Gate Simplification — Single Mandatory Review A with Optional Risk-Classified Cross-Check (Retires Mandatory Independent Review B)"
version: "0.5"
status: Approved
owner: Product Owner
reviewers: [ChatGPT, Claude]
approved_by: Product Owner
approved_at: "2026-09-10T15:51+07:00"
created_at: "2026-09-10"
last_review: "2026-09-10"
next_review: null
depends_on: [ADR-031]
addresses: []
resolves: []
supersedes: [ADR-031]
---

# ADR-042: Review Gate Simplification — Single Mandatory Review A with Optional Risk-Classified Cross-Check

> **Bounded Review A correction**, addressed/remediated pending re-review (not self-closed by the correction executor — closure is a Review A re-review determination, never asserted here): `P3-ADR042-A-MAJ-01` — the v0.1 migration set omitted `docs/constitution/12-approval-gates.md`, which hard-codes the retired mandatory-two-review rule at two locations (the intro prose immediately before §12.1, and §12.2 item 8), and split the activation set into a mandatory tier (A-C) versus an "optional, may remain stale" tier (D-H) — leaving a genuine possibility of activating the new model while known-contradictory Review-B language remained textually live elsewhere in Constitution-tier/Phase-tier documents. Corrected: `docs/constitution/12-approval-gates.md` added to the set; the two-tier mandatory/optional split removed in favor of one unambiguous set, sized to exactly what would actively contradict the new model if left stale — §11.4's `reviewers` field description was independently re-checked and found to hard-code no count, so it is explicitly excluded as a verified determination, not a deferred allowance. `P3-ADR042-A-MAJ-02` — the v0.1 Migration treated this ADR's own Product Owner approval and the Constitution/template/rules activation as two separate governed boundaries, with `ADR-042` reaching `Approved` (declaring `supersedes: [ADR-031]`) *before* the amendment bundle existed — violating Chapter 11 §11.6/§11.8's atomic-approval-transition requirement (a `supersedes` declaration must land with the superseded ADR's MANIFEST state update in the SAME change) and creating a window where `ADR-031` would be MANIFEST-recorded `Superseded` while the old review rules remained controlling text elsewhere in the Constitution. Corrected: this ADR's own approval is now itself the first item of the single atomic governance action described in Migration below — it is never approved before or separately from the rest of the amendment bundle. **No change to:** the R0/R1/R2 definitions, optional cross-check semantics, the target workflow `Executor → Review A → Risk Classification → Product Owner Decision → Execution`, the Alternatives considered, or any other Decision content — this correction is scoped entirely to the Migration/activation-boundary design.
>
> `P3-ADR042-A-MAJ-01: CLOSED`
> `P3-ADR042-A-MAJ-02: CLOSED`
>
> **v0.3 bounded correction**, addressed/remediated pending re-review: `P3-ADR042-A-MAJ-03` — Migration item 4 (the future §11.9 Validator contract amendment) contradicted this ADR's own Decision — optional cross-check semantics, which explicitly state a cross-check "requires no session/execution-ID persistence," "requires no Mode A/Mode B bookkeeping," and "need not appear in the ADR/MANIFEST/CHANGELOG record of the decision" — by requiring the future validator to record the Product Owner's `CROSS-CHECK`/`PROCEED WITHOUT CROSS-CHECK` choice as an approval-eligibility input. Corrected: item 4 no longer requires any cross-check-related record (existence, absence, invocation choice, reviewer identity, transcript, evidence, execution/session ID, or result) as a validator input; the R2 Review-A-to-Product-Owner communication (why R2, what uncertainty a cross-check could reduce) remains an operational/advisory interaction, not repository evidence the validator checks for. **No change to:** R0/R1/R2 definitions, Review A's mandatory status, Product Owner's sole authority, the rest of the optional cross-check model, the corrected 10-item atomic activation set, Chapter 12's inclusion in that set, or the atomic ADR-042-approval/ADR-031-supersession timing (`P3-ADR042-A-MAJ-01`/`-MAJ-02` remediations, unchanged). The requirement that an actually-discovered substantive defect (from Review A, tests, QG, an optional cross-check, or other valid evidence) be addressed through ordinary governance before approval where applicable is unaffected — this correction removes a *mandatory persistence* requirement for the cross-check's own existence/choice, not the obligation to act on a real defect once found.
>
> `P3-ADR042-A-MAJ-03: CLOSED`
>
> **v0.4 bounded correction**, addressed/remediated pending re-review — remediates Independent Review B findings on the v0.3 Migration/activation candidate. `P3-ADR042-B-MAJ-01` — the activation set omitted a live prospective prerequisite: `docs/governance/phases/phase-3-rules.md` §11 Gate-path controls lists `≥2 independent review` as a Phase-3 Approval-Gate prerequisite, which would re-impose the retired mandatory-two-review gate after activation if left unamended. Corrected: item 8 now also covers §11 Gate-path, deferring to the amended Chapter 12 §12.2 item 8 model instead of a hard-coded count. `P3-ADR042-B-MAJ-02` — two Constitution banners outside the previously scoped §3/§11.5/§11.9 sections still explicitly declare ADR-031 Mode A/Mode B governance `ACTIVE` in present tense: the top governance-migration banner in `docs/constitution/00-governance.md` and in `docs/constitution/11-adr-process.md`. Left unamended, these would keep asserting the retired model as currently controlling even after activation. Corrected: items 2 and 3 now explicitly include reframing these banners to historical (past-boundary) language — they remain accurate history of the ADR-031 migration, but no longer read as describing the *currently* controlling model. `P3-ADR042-B-MIN-01` (folded, non-blocking) — `docs/governance/execution-rules.md` contains further live review-control wording that would go stale: `G-REV-002` references "full Review A/B," `G-REV-003` presupposes mandatory Independent Review B, and Semantic Sufficiency interpretation point 7 references "Independent Review B" by name. Corrected: item 7 now covers all three locations, not `G-REV-003` alone. Historical/version-history records are not rewritten. `P3-ADR042-B-MIN-02` (folded, non-blocking) — the "Independent reviews" section below contained a stale statement asserting no review has occurred, which would freeze false the moment any review happens. Corrected: replaced with evergreen wording that does not itself assert a review-completion state. No Review B transcript/report is persisted into the repository and no standalone Review-B evidence file is created. `P3-ADR042-B-MIN-03` (folded, non-blocking) — confirmed no standalone MANIFEST transaction is created merely to record this Draft; MANIFEST treatment remains folded entirely into item 10's single atomic-activation final-state candidate. **No change to:** R0/R1/R2 definitions, Review A's mandatory status, Product Owner's sole authority, the optional cross-check model (including its `P3-ADR042-A-MAJ-03` correction), the atomic ADR-042-approval/ADR-031-supersession timing, or Chapter 12's inclusion in the set.
>
> `P3-ADR042-B-MAJ-01: CLOSED`
> `P3-ADR042-B-MAJ-02: CLOSED`
> `P3-ADR042-B-MIN-01/-MIN-02/-MIN-03: CLOSED`
>
> **v0.5 bounded correction**, addressed/remediated pending re-review — remediates a further bounded-correction pass on the v0.4 activation candidate itself (`ACT-A-MAJ-01..04`, `ACT-A-MIN-01/02`). `ACT-A-MAJ-01` — three remaining locations still contradicted the new single-Review-A model on the candidate branch: Chapter 0 §3's Decision Workflow diagram (still `Independent AI Technical Architect Reviews → Architecture Review → Product Owner Decision`, implying plural mandatory reviews and a separate mandatory Architecture Review stage); Chapter 12's persistent delegation blockquote (still cited "minimum cardinality"/"Mode A/Mode B" as what Constitution locks); Global `G-ID-001` (still defined "reviewed semantic candidate identity" as a blob reviewed "qua Review A/B"). All three corrected on the candidate branch to the Review A + Risk Classification + optional-cross-check model, without requiring cross-check evidence for `G-ID-001`'s own definition. `ACT-A-MAJ-02` — `docs/governance/phases/phase-3-rules.md` and `docs/governance/execution-rules.md` had their live review-gate semantics changed on the candidate branch while frontmatter still claimed their old accepted version/state (`phase-3-rules.md` `"0.2"`/`EFFECTIVE`/`Product Owner`/`"2026-08-19T11:10:00+07:00"`; `execution-rules.md` `"0.5"`/`EFFECTIVE`/`Product Owner`/`"2026-08-26"`) — invalid provenance. Corrected: both bumped to their next semantic version (`phase-3-rules.md` `"0.3"`, `execution-rules.md` `"0.6"`) in `operational_state: CANDIDATE` with `accepted_by`/`accepted_at` reset to `null`, following each file's own established version/lifecycle convention (`phase-3-rules.md`'s own v0.1 candidate-state precedent), with an accurate Change History entry appended to each — no fabricated Product Owner acceptance. `ACT-A-MAJ-03` — Chapter 11 §11.9's new validator criteria lacked explicit non-retroactivity/bootstrap boundary semantics. Added: decisions completed before activation remain valid under the review rules effective at their own original boundary; historical ADRs/two-review evidence are never retroactively required to carry Risk Classification or be re-reviewed; `ADR-042`'s own approval/activation transaction is explicitly governed by the pre-activation mandatory-two-review model, never by the validator criteria it itself introduces (no self-bootstrap contradiction); the new criteria apply only to decisions governed by the new model after the activation boundary. `ACT-A-MAJ-04` — the ADR template's mandatory Risk Classification block included a `cross_check: N/A | PROCEED WITHOUT CROSS-CHECK | CROSS-CHECK` field, contradicting the Decision's own optional cross-check semantics (no mandatory persistence, not approval-eligibility input) by placing the choice inside the mandatory persisted block. Removed — the mandatory block now contains only `class`/`reason`; the R2 Review-A-to-Product-Owner communication and the Product Owner's use/skip choice remain operational/advisory, not an ADR field. `ACT-A-MIN-01` (folded) — `P3-MODULE-BATCH-001` still referenced an "A/B governance cycle"; updated to the new review-gate terminology (historical "A/B" phrasing preserved as explicit bracketed reference), the underlying module-batching rule itself unchanged. `ACT-A-MIN-02` (folded) — the MANIFEST candidate-preview section inconsistently stated both that the current mandatory-two-review gate controls this candidate AND that this candidate is reviewed via Review A plus optional R2 cross-check; corrected to state unambiguously that this activation candidate itself requires Review A followed by a mandatory bounded Independent Review B re-review before Product Owner approval — the new R0/R1/R2 + optional-cross-check model applies only to decisions made after ADR-042's own successful activation. No Review B transcript/report persisted into any repository file. **No change to:** R0/R1/R2 definitions, the target workflow, the optional cross-check model's own substantive semantics, or the corrected 10-item atomic activation set's item count/ordering.
>
> `ACT-A-MAJ-01: CLOSED`
> `ACT-A-MAJ-02: CLOSED`
> `ACT-A-MAJ-03: CLOSED`
> `ACT-A-MAJ-04: CLOSED`
> `ACT-A-MIN-01/-MIN-02: CLOSED`
>
> **APPROVED.** Final bounded Review A (scope: the v0.4→v0.5 correction delta plus cross-file coherence of the complete activation candidate, boundary `64fc05becc6ca4245229db79b3fb6c5d9187e622`) confirmed `ACT-A-MAJ-01..04`/`ACT-A-MIN-01/02` CLOSED, 0 new Blocker/Major/Minor — verdict CLEAN, routed to Independent Review B. Independent Review B (Mode A `DISTINCT_PRINCIPAL` — principal `Claude` distinct from Review A principal `ChatGPT`, both holding `AI Technical Architect` per `/team/team.yaml`, same boundary) independently confirmed `P3-ADR042-B-MAJ-01`/`-MAJ-02` CLOSED and raised one new non-blocking Minor, `P3-ADR042-B2-MIN-01` (MANIFEST candidate-preview section's own `version "0.4"` reference stale against the actual reviewed `v0.5` candidate) — Blocker 0 / Major 0 / Minor 1, verdict CLEAN — READY_FOR_PRODUCT_OWNER_DECISION. Product Owner decision (verbatim): "APPROVE ADR-042 v0.5 AND ITS ATOMIC GOVERNANCE ACTIVATION at reviewed semantic boundary 64fc05becc6ca4245229db79b3fb6c5d9187e622, based on current main e975d44f813b1ee91d2dbf3793376ca1827dc0f1. Accept Review A CLEAN and Independent Review B CLEAN. Accept P3-ADR042-B2-MIN-01 as non-blocking and fold its deterministic MANIFEST preview correction (v0.4 → v0.5) into the same atomic activation transaction. Authorize the single atomic activation defined by ADR-042..." — recorded `2026-09-10T15:51+07:00`. `P3-ADR042-B2-MIN-01`'s own correction lands in this same activation commit (MANIFEST). This satisfied the **pre-activation** mandatory-two-independent-review gate (Chapter 0 §3 v1.2 / Chapter 11 §11.5/§11.9 v2.2 / `ADR-031` Mode A, as this ADR's own Migration section requires — this ADR was never used to bootstrap its own rules). Reviewed Draft boundary/blob (frozen at approval, G-ID-001): commit `64fc05becc6ca4245229db79b3fb6c5d9187e622`, content identity `18e90f134a7a8aa0dc5c9ed3642ef2716b6397da`, `version: "0.5"`/`status: Draft` at review time — distinct from this transaction's own resulting lifecycle-record blob (mechanical status/reviewer-field edit only; the full Constitution/template/rules amendment bundle below is the semantic content of the reviewed candidate itself, carried unchanged into activation).

**Context:** [Chapter 0](../constitution/00-governance.md) §3's "Review gate bắt buộc" and [Chapter 11](../constitution/11-adr-process.md) §11.5/§11.9 currently require a minimum of **two** independent review executions before every Product Owner ADR/approval-gate decision — satisfied either by Mode A (`DISTINCT_PRINCIPAL`) or Mode B (`SAME_PRINCIPAL_DISTINCT_EXECUTION`, with a mandatory execution-isolation evidence contract), per [ADR-031](./ADR-031.md) (Approved). This mandatory-two-review gate applies uniformly to every decision regardless of actual semantic risk — a typo fix and a Platform Invariant change both require the same two-independent-review machinery today. Product Owner has decided to retire this uniform requirement: keep a single mandatory formal technical review (Review A), and make a second independent review a strictly **optional, advisory** cross-check selected by risk classification rather than a blanket gate.

**Ground truth, independently confirmed at this boundary:** `docs/constitution/00-governance.md` §3 (re-read fresh) states the mandatory-two-review rule verbatim ("Trước khi Product Owner quyết một ADR hoặc tài liệu thuộc approval gate, phải có tối thiểu hai independent reviews"), Mode A/Mode B eligibility, and fail-closed behavior when independence cannot be resolved. `docs/constitution/11-adr-process.md` §11.4 (Metadata contract — `reviewers` field semantic), §11.5 (Review and acceptance gate — mirrors Chapter 0 §3), §11.6 (Approval transition must pin reviewer evidence atomically with approval), and §11.9 (Validator contract — mechanically checks "minimum-two eligible independent review EXECUTIONS," Mode A/Mode B resolution, fail-closed on unresolved independence) all currently encode this same mandatory-two-review premise. `docs/adr/ADR-031.md` (Approved v0.2) defines the Principal Identity vs Execution Identity distinction and the Mode A/Mode B execution-isolation evidence contract that exists *specifically* to let a second review be satisfied without a second distinct principal — its own §11 "Migration / Atomic Activation Boundary" establishes the exact atomic-activation-set pattern this ADR reuses by direct citation. `docs/constitution/12-approval-gates.md` (re-read fresh, `P3-ADR042-A-MAJ-01` remediation) independently hard-codes the same mandatory-two-review premise at two separate locations: the intro prose immediately before §12.1 ("tối thiểu hai independent review... eligibility precondition bắt buộc") and §12.2 item 8 ("minimum-two eligible independent reviews complete và evidence được pin"); its own §12.3 authority-boundary table states it only references, never redefines, Chapter 0 §3/Chapter 11 §11.5's review-eligibility authority. `docs/templates/adr-template.md` embeds a two-row `Independent reviews` evidence table with Mode A/Mode B guidance as boilerplate on every new ADR. `docs/governance/execution-rules.md` `G-REV-003` ("Independent Review B PHẢI giữ độc lập THẬT SỰ...") presupposes Independent Review B is itself a mandatory fixture. `docs/governance/phases/phase-3-rules.md` `P3-REVIEW-001`'s review-depth table currently routes "new architecture / authority / contract semantics" to `full governed review (Review A/B)`, and `P3-IDENTITY-001` is an operational pre-check anchored explicitly on "Chapter 0 §3/Chapter 11 §11.5's reviewer-independence authority (Mode A/Mode B)" without redefining it. This session's own transaction history independently corroborates the operational cost this ADR responds to: multiple transactions in this repository were correctly halted specifically because genuine, traceable Independent Review B evidence could not be produced for a given boundary — not because the underlying change was judged high-risk, but because the mandatory-two-review gate applies uniformly regardless of risk.

**Scope classification — `ADR Required`.** [Chapter 0 §4b](../constitution/00-governance.md)'s own table lists "Governance/Approval process" changes as explicitly `ADR Required` — this is exactly that category.

**ADR inflation/scope check (`G-ADR-004`):**

```text
1. Existing authority/alignment resolves this without a new decision? NO --
   Chapter 0 §3 / Chapter 11 §11.5/§11.9 / ADR-031 currently make a second
   independent review MANDATORY for every ADR/approval-gate decision
   platform-wide, with no existing risk-tiered exception; no lower-tier
   document (execution-rules.md, phase rules) may override Constitution-
   tier review-gate authority on its own.
2. Genuine architecture/governance decision, not cheaply reversible? YES --
   review-gate policy gates every future ADR/approval-gate decision
   platform-wide; changing it later requires another full governed
   amendment, and under-reviewing a genuinely high-risk change if the
   replacement policy is wrong is a real, hard-to-detect failure mode.
3. Chapter 0 §4b trigger fires independently? YES -- "Governance/Approval
   process" change is explicitly listed as ADR Required, disjunctively
   from any other trigger.
4. Authored merely to "complete" a prior transaction (G-ADR-003)? NO --
   directly requested by Product Owner decision direction as a standalone
   governance simplification, not derived from a gap left by the ADR-041
   bootstrap work or any other prior transaction.
Result: ADR REQUIRED confirmed independently.
```

**Decision question:** what review-gate model keeps Review A's independent technical scrutiny mandatory for every decision while making a second independent review proportional to actual semantic risk instead of uniform ceremony?

**Decision — target workflow:**

```text
Executor -> Review A -> Risk Classification -> Product Owner Decision -> Execution
```

- **Review A remains the single mandatory formal technical review.** Role `AI Technical Architect` eligibility (Chapter 0 §2) is unchanged. Review A must independently inspect the exact candidate/repository authority — it does not inherit or rubber-stamp the Executor's own conclusion (this preserves `G-REV-003`'s actual independence discipline, reframed onto Review A now that it is the sole mandatory review rather than a property specifically of "Independent Review B").
- **Product Owner remains the sole approve/reject authority** (Chapter 0 §2/§3, unchanged) and remains free to request a cross-check for any reason, including reasons outside the R0/R1/R2 taxonomy below.
- **Risk Classification is a new, mandatory step between Review A and Product Owner Decision** — Review A (or, failing that, the Product Owner) classifies the change into exactly one of three risk classes before the Product Owner decides.

**Decision — the three risk classes:**

- **R0 — Mechanical / no semantic risk.** Typo/formatting; deterministic bookkeeping; mechanical factual correction; test-expectation alignment; refactor proven not to alter behavior/contract; generated/index/reference synchronization. Default: `NO CROSS-CHECK`. Use deterministic verification/tests as appropriate instead.
- **R1 — Bounded semantic / normal implementation risk.** Implementation change within one module; bounded bug fix; bounded semantic correction; internal algorithm change that does not alter external contract/authority; reversible change with limited blast radius. Default: `NO CROSS-CHECK` — Review A is sufficient unless Review A or the Product Owner escalates the specific case to R2 because actual uncertainty/risk is materially higher than the nominal category suggests.
- **R2 — High semantic / architectural risk.** Classified when one or more materially applies: Platform Invariant or Event Schema change; authority / source-of-truth semantics; identity, lifecycle, replay, versioning, retention, or activation semantics; cross-module contract/dependency change with significant blast radius; migration difficult or expensive to reverse; destructive/irreversible state transition; Review A finds conflicting authority/evidence or unresolved ambiguity; Review A's own confidence is materially low; repeated semantic correction on the same artifact indicates the root cause may still be misunderstood; or the Product Owner explicitly requests a second opinion. R2 behavior: `RECOMMEND OPTIONAL INDEPENDENT CROSS-CHECK` — **R2 MUST NOT automatically require the cross-check.** For an R2 case, Review A must briefly tell the Product Owner (a) why the case is R2, and (b) what specific uncertainty/risk a cross-check could reduce. The Product Owner then chooses `CROSS-CHECK` or `PROCEED WITHOUT CROSS-CHECK`. **Absence of a cross-check must never make the approval mechanically ineligible** once this governance is active — this is the central difference from the retired mandatory gate.

**Decision — optional cross-check semantics.** When a cross-check is chosen (R2 only, Product Owner's choice, never automatic), it:

- independently inspects the exact candidate/repository authority — it must not inherit Review A's conclusion as ground truth;
- is advisory only, with no veto over the Product Owner's decision;
- is not an approval prerequisite under any circumstance;
- requires no persisted transcript/report;
- requires no standalone Review-B evidence file;
- requires no session/execution-ID persistence;
- requires no Mode A/Mode B bookkeeping for approval eligibility (ADR-031's execution-isolation evidence contract is not a precondition for this optional step);
- need not appear in the ADR/MANIFEST/CHANGELOG record of the decision.

If an optional cross-check finds a substantive defect, ordinary engineering judgment addresses the defect before the Product Owner decides — exactly as any other reviewer input would be handled. This practical response **must not** be written, interpreted, or operationalized as a hidden mandatory second-review gate; it is the same category of response as Review A itself finding a defect.

**Decision — historical evidence unaffected.** This ADR is prospective only, mirroring [ADR-031](./ADR-031.md) §10's own non-retroactivity principle. Every ADR/approval-gate decision already recorded under the mandatory-two-review gate keeps its existing reviewer evidence, Mode A/Mode B disposition, and approval validity exactly as recorded — nothing here reinterprets, invalidates, or requires re-review of prior decisions.

**Authority compatibility — `ADR-031` supersession scope.** `ADR-031`'s entire operative machinery (Principal vs Execution Identity distinction, Mode A/Mode B independence modes, the mandatory execution-isolation evidence contract) exists specifically to let a second review satisfy the *mandatory* two-review gate this ADR retires. Once this ADR's target model is activated (see Migration below), that machinery is no longer load-bearing for approval eligibility — an optional cross-check, when chosen, requires **no** Mode A/Mode B bookkeeping (see above). `ADR-031`'s file is never modified — not now, not later (Chapter 11 §11.3 forbids editing an Approved ADR; `ADR-031.md` stays byte-identical/immutable). Instead, `ADR-042` itself — via `depends_on: [ADR-031]`/`supersedes: [ADR-031]` (frontmatter) — carries the effect at its own approval boundary, which the corrected Migration section below establishes as the SAME single governed action that also amends Chapter 0/Chapter 11/Chapter 12, the ADR template, `G-REV-003`, and Phase-3 `P3-REVIEW-001`/`P3-IDENTITY-001`, and updates MANIFEST — never a separate, later boundary (fixes `P3-ADR042-A-MAJ-02`). This mirrors the same governance-relationship pattern already precedented by `ADR-016`→`ADR-015`, `ADR-014`→`ADR-003`, and `ADR-041`→`ADR-040` in this repository: supersession is a frontmatter/MANIFEST declaration, never a text edit into the superseded ADR's own file. `ADR-031`'s own definitions (§1-2, Principal Identity vs Execution Identity) are not declared incorrect and may still be referenced informationally if a Product Owner or reviewer wants to describe an optional cross-check's provenance — but no future governance artifact may treat that description as *required* bookkeeping for approval eligibility.

**Alternatives considered:**

1. **(1) Keep the mandatory two-review gate unconditionally (status quo).** Rejected — this is precisely the uniform ceremony the Product Owner decision direction identifies as disproportionate to actual risk for the majority of R0/R1 changes.
2. **(2) Keep the mandatory two-review gate only for R2 (auto-required, not optional).** Rejected — the Product Owner decision direction is explicit: "R2 MUST NOT automatically require the cross-check" and "Absence of a cross-check must never make the approval mechanically ineligible." An auto-required R2 gate would silently reintroduce the same ineligibility failure mode this ADR retires, just with a narrower trigger.
3. **(3) Retire Review A's mandatory status as well, leaving only Product Owner judgment.** Rejected — out of scope of the Product Owner decision direction, which explicitly keeps "Review A remains the single mandatory formal technical review." Removing all mandatory independent technical review would be a materially larger governance change than requested.
4. **(4) A formal risk-scoring system, review-evidence registry, or additional cross-check ceremony (forms, scoring, mandatory logging).** Rejected — the Product Owner decision direction explicitly instructs to "Avoid designing scoring systems, forms, evidence registries, or additional ceremony." R0/R1/R2 is a classification, not a scored/weighted system, and the optional cross-check is explicitly exempted from persistence/evidence requirements.

**Migration — ADR-042's own approval and the governance activation/supersession boundary are ONE coherent atomic governance action (corrected, closes `P3-ADR042-A-MAJ-02`).** `ADR-042` does not itself perform this action — it is `Draft`, deciding the target model only — but the model it decides is that its own eventual Product Owner approval, its `supersedes: [ADR-031]` lifecycle effect, and the full Constitution/template/rules amendment bundle below all land in the SAME governed transaction. There is no intermediate state where `ADR-042` is `Approved` and MANIFEST records `ADR-031 Superseded` while Chapter 0/Chapter 11/Chapter 12 still assert the retired mandatory-two-review rule: that window is exactly what Chapter 11 §11.6/§11.8 already forbid (the approval transition must be atomic; a `supersedes` declaration must land with the superseded ADR's MANIFEST state update in the same change) and exactly the defect `P3-ADR042-A-MAJ-02` identified.

Until that single bundled transaction occurs, `ADR-042` remains `Draft`/unapproved, and the **current** mandatory two-independent-review gate (Chapter 0 §3, Chapter 11 §11.5/§11.9, Chapter 12 intro prose/§12.2 item 8, `ADR-031` Mode A/Mode B) governs every decision — **including `ADR-042`'s own eventual approval**. This ADR is not used to bootstrap its own rules, exactly as `ADR-031` itself was reviewed and approved under the then-current distinct-principal-only gate before its own Mode B mechanism could be used for anything else. The difference from `ADR-031`'s own precedent is timing: `ADR-031` was approved first and activated second (two governed boundaries — Mode B's later activation did not retroactively change what governed `ADR-031`'s own approval, and `ADR-031` itself asserted no `supersedes` relation that MANIFEST needed to record atomically). `ADR-042`'s approval and its activation must instead be the SAME boundary, because approving `ADR-042` alone already asserts `ADR-031 Superseded` — an assertion Chapter 11 §11.6/§11.8 requires to land atomically with the MANIFEST state change, which in turn requires the amendment bundle that makes that state change coherent to already exist as part of the same reviewed, approved change.

```text
SINGLE COHERENT ATOMIC GOVERNANCE ACTION (one future transaction, NOT this one -- ALL items
  below land together, are reviewed together, and are approved together; there is NO
  mandatory-vs-optional split -- everything whose stale semantics would contradict the new
  review model is REQUIRED in this one set; missing ANY item invalidates the whole action,
  and none of it takes effect until all of it does; fixes the two-tier defect
  `P3-ADR042-A-MAJ-01` identified):

  1. ADR-042 itself -- Draft -> Approved, IN this same action, not before it.

  2. docs/constitution/00-governance.md -- SUBSTANTIVE AMENDMENT, two locations
     (`P3-ADR042-B-MAJ-02` remediation -- top banner added, was omitted from the prior
     draft's set). (a) §3: replace the mandatory-two-independent-review rule with: Review
     A mandatory; Risk Classification (R0/R1/R2) mandatory step; R2 recommends, never
     requires, an optional advisory cross-check; Product Owner sole approve/reject
     authority unchanged. (b) top-of-file "Governance migration (v1.2, ACTIVE)" banner
     (currently declares ADR-031 Mode A/Mode B "mở rộng independent-review eligibility...
     ACTIVE" in present tense): reframe to past-tense history of the ADR-031 migration --
     it remains accurate as a historical record of what activated on 2026-08-18, but must
     no longer read as describing the *currently* controlling review-eligibility model
     once this action lands; add a new banner line recording THIS activation as the
     current one, same pattern already used for the ADR-031 banner itself. (c) §3's
     "Decision Workflow" diagram (`ACT-A-MAJ-01` remediation): replace `Independent AI
     Technical Architect Reviews -> Architecture Review -> Product Owner Decision` (plural
     reviews, an implied separate mandatory stage) with `Review A -> Risk Classification
     (R0/R1/R2) -> Product Owner Decision`.

  3. docs/constitution/11-adr-process.md -- SUBSTANTIVE AMENDMENT, three locations
     (`P3-ADR042-B-MAJ-02` remediation -- top banner added, was omitted from the prior
     draft's set). (a) §11.5 Review and acceptance gate: mirror the Chapter 0 §3
     amendment; drop the minimum-two-review premise; keep role eligibility (`AI Technical
     Architect`), no-veto, and Product Owner sole-authority language unchanged. (b)
     top-of-file "Governance migration (v2.2, ACTIVE)" banner (currently: "§11.5/§11.9
     dưới đây kích hoạt mô hình đã được Product Owner approve tại ADR-031... cùng nội dung
     Mode A/Mode B"): same past-tense reframing as Chapter 0 item (b) above, same
     boundary. (c) §11.9 Validator contract, covered separately at item 4 below.

  4. docs/constitution/11-adr-process.md, §11.9 Validator contract -- SUBSTANTIVE
     AMENDMENT (corrected, closes `P3-ADR042-A-MAJ-03`). Validator no longer mechanically
     requires "minimum-two eligible independent review EXECUTIONS" or Mode A/Mode B
     resolution as an approval-eligibility precondition; replace with exactly two
     mandatory checks: Review A execution resolved (principal identity, role, review
     boundary); Risk Classification present and resolves to exactly one of R0/R1/R2.
     Cross-check existence, absence, invocation choice, reviewer identity, transcript,
     evidence, execution/session ID, and result are NOT approval-eligibility inputs and
     MUST NOT be checked by the validator -- consistent with the Decision's own optional
     cross-check semantics above (no persisted transcript, no Mode A/Mode B bookkeeping,
     need not appear in ADR/MANIFEST/CHANGELOG). Fail-closed only on a MISSING Review A
     execution or a missing/invalid Risk Classification -- never on cross-check absence,
     and never on an absent record of the Product Owner's R2 cross-check-or-proceed
     choice. This does not weaken the separate, ordinary requirement that a substantive
     defect actually discovered (by Review A, tests, Quality Gates, an optional
     cross-check, or other valid evidence) be addressed through normal governance before
     approval where applicable -- that obligation is about acting on a real defect, not
     about persisting the cross-check's own existence or choice as validator evidence.
     Also adds (`ACT-A-MAJ-03` remediation) explicit non-retroactivity/bootstrap boundary
     semantics immediately below the validator checklist: decisions completed before this
     activation remain valid under the review rules effective at their own original
     boundary; historical ADRs/two-review evidence are never retroactively required to
     carry Risk Classification or be re-reviewed; `ADR-042`'s own approval/activation
     transaction is governed by the pre-activation mandatory-two-review model, never by
     the validator criteria it itself introduces (no self-bootstrap contradiction); the
     new criteria apply only to decisions governed by the new model after the activation
     boundary.

  5. docs/constitution/12-approval-gates.md -- SUBSTANTIVE AMENDMENT, two locations
     (`P3-ADR042-A-MAJ-01` remediation -- omitted from the prior draft's set entirely;
     corrected here). (a) Two locations hard-code the retired rule and both must change in
     this same action: the intro prose immediately before §12.1 ("tối thiểu hai independent
     review... eligibility precondition bắt buộc") and §12.2 item 8 ("minimum-two eligible
     independent reviews complete và evidence được pin"). Both replaced with a reference to
     the amended Chapter 0 §3/Chapter 11 §11.5 Review-A + Risk-Classification model. (b) the
     persistent delegation blockquote immediately after the review-gate banners (`ACT-A-
     MAJ-01` remediation, `main`-boundary correction): currently states Constitution locks
     "role, minimum cardinality, và independence mode hợp lệ (Mode A/Mode B)" -- stale after
     this activation; replace with role (`AI Technical Architect`), Review A mandatory, Risk
     Classification mandatory, and optional cross-check semantics. §12.3's own
     authority-boundary table already states Chapter 12 does not define review eligibility,
     only references it, so §12.3 needs no substantive amendment -- only re-verification,
     at the same boundary, that its reference targets (2/3 above) still say what §12.3
     claims they say.

  6. docs/templates/adr-template.md -- SUBSTANTIVE AMENDMENT. Replace the two-row
     mandatory Independent-reviews evidence table + Mode A/Mode B guidance block with: one
     mandatory Review A row and an explicit, mandatory Risk Classification block containing
     ONLY `class` (R0/R1/R2) and `reason` (`ACT-A-MAJ-04` remediation -- a prior draft of
     this candidate placed a `cross_check` choice field inside this mandatory block,
     contradicting the Decision's own optional-cross-check semantics; removed -- the R2
     Review-A-to-Product-Owner communication and the Product Owner's use/skip choice remain
     operational/advisory, never an ADR field or validator evidence). Left stale, every ADR
     authored after this action would keep instructing authors that two reviews are
     required -- a direct, repeating contradiction of the just-activated rule, not a
     passive reference.

  7. docs/governance/execution-rules.md -- SUBSTANTIVE AMENDMENT, four locations
     (`P3-ADR042-B-MIN-01` remediation -- `G-REV-002` and Semantic Sufficiency point 7
     added, were omitted from the prior draft's set; `ACT-A-MAJ-01` remediation --
     `G-ID-001` added). (a) `G-REV-003`: reframe from
     "Independent Review B PHẢI giữ độc lập THẬT SỰ" (asserts a mandatory Independent
     Review B) to governing Review A's own independence from the Executor, and, when an
     optional cross-check DOES occur, that cross-check's independence from Review A's
     conclusion (already stated in this ADR's Decision above; wording must not contradict
     it). (b) `G-REV-002`: its reference to "full Review A/B" (in "KHÔNG tự động kích hoạt
     full Review A/B lại toàn bộ artifact") must be updated to the amended terminology --
     the underlying rule (bounded correction receives bounded re-review, not automatic
     full re-review) is unaffected, only the stale pairing name. (c) Semantic Sufficiency
     interpretation point 7 ("Diễn giải này KHÔNG làm yếu G-REV-002, Independent Review B,
     hay bất kỳ yêu cầu remediate Blocker/Major nào"): update the "Independent Review B"
     reference to the amended model; the substantive guarantee (this interpretation does
     not weaken remediation requirements) is unaffected. (d) `G-ID-001`: its definition of
     "reviewed semantic candidate identity" as a blob reviewed "qua Review A/B" updated to
     "qua review gate đang áp dụng tại boundary đó" (Review A, or Review A plus an
     optional cross-check if the Product Owner chose one at R2) -- explicitly NOT requiring
     cross-check evidence for this definition to apply. No new `G-*` rule ID required for
     any of the four -- direct amendment of existing rule text, same class as
     `G-REV-001..004` itself. Historical/version-history records elsewhere in the file are
     not rewritten. Also (`ACT-A-MAJ-02` remediation): this file's own `version`/
     `operational_state`/`accepted_by`/`accepted_at` frontmatter bump from `"0.5"`/
     `EFFECTIVE`/`Product Owner`/`"2026-08-26"` to `"0.6"`/`CANDIDATE`/`null`/`null` lands
     in this same action -- the changed semantics above were never reviewed/accepted under
     the old v0.5 acceptance; `operational_state` mechanically transitions to `EFFECTIVE`
     on this exact version only once Product Owner actually accepts it, atomically with
     this whole bundle.

  8. docs/governance/phases/phase-3-rules.md -- SUBSTANTIVE AMENDMENT, three locations
     (`P3-ADR042-B-MAJ-01` remediation -- §11 Gate-path added, was omitted from the prior
     draft's set entirely). (a) `P3-REVIEW-001`: replace the review-depth table's `new
     architecture / authority / contract semantics -> full governed review (Review A/B)`
     row with routing through this ADR's R0/R1/R2 policy. Left stale, this is an ACTIVE
     routing rule Phase-3 transactions actually follow today -- leaving it would keep
     directing real work back to a mandatory "Review A/B" pairing the same action just
     retired. (b) §11 Gate-path controls: its Approval-Gate prerequisite list ends with a
     hard-coded `≥2 independent review` item -- a live, prospective prerequisite that
     would re-impose the retired gate the first time a Phase 3 Approval Gate is evaluated
     after this action. Replace with a reference to the amended Chapter 12 §12.2 item 8
     model (Review A + Risk Classification resolved) instead of a hard-coded count. (c)
     `P3-MODULE-BATCH-001` (`ACT-A-MIN-01` remediation, folded, non-blocking): its
     "KHÔNG cần A/B governance cycle riêng cho mỗi file" reference updated to the new
     review-gate terminology; the underlying module-batching rule itself unchanged;
     historical "A/B" phrasing preserved as an explicit bracketed reference, not deleted.
     (d) `P3-IDENTITY-001`, covered separately at item 9 below. Also (`ACT-A-MAJ-02`
     remediation): this file's own `version`/`operational_state`/`accepted_by`/
     `accepted_at` frontmatter bump from `"0.2"`/`EFFECTIVE`/`Product Owner`/
     `"2026-08-19T11:10:00+07:00"` to `"0.3"`/`CANDIDATE`/`null`/`null` lands in this same
     action, following this file's own v0.1 candidate-state precedent -- Phase 3
     substantive work's already-accepted `PERMITTED_TO_BEGIN` status (under v0.2)
     is explicitly preserved, not reopened; `operational_state` mechanically transitions
     to `EFFECTIVE` on this exact version only once Product Owner actually accepts it,
     atomically with this whole bundle.

  9. docs/governance/phases/phase-3-rules.md, `P3-IDENTITY-001` -- WORDING-SYNC, same
     action. It anchors explicitly on "Chapter 0 §3/Chapter 11 §11.5's
     reviewer-independence authority (Mode A/Mode B)" without redefining it; once 2/3
     amend that authority's text, this reference must be updated to what it now defers to
     -- same class of defect `ADR031-A-MAJ-01` warned against (a Phase-tier document
     silently stale relative to an amended Constitution-tier authority it only
     references).

  10. MANIFEST -- IN this same action: record `ADR-042`'s new current state (`Approved`);
      record `ADR-031`'s current lifecycle state as `Superseded`, reverse relation
      `ADR-031 superseded_by ADR-042`; reflect the new version/status of items 2-9.

  VERIFIED NON-CONTRADICTORY, NOT PART OF THIS SET (a determination, not a deferral):
  docs/constitution/11-adr-process.md §11.4 Metadata contract -- the `reviewers` field is
    described only as "Actor identities đã review boundary; historical evidence," with no
    hard-coded count anywhere in its text. It remains accurate under any number of
    reviewers (one mandatory Review A, plus zero-or-one optional cross-check) without
    amendment. If a future re-read at the activation boundary itself finds §11.4 wording
    that DOES presuppose a count, it must be added to the numbered set above BEFORE that
    action, not left stale after it.

There is no second, lower-priority tier: every item 1-10 above is required in the SAME
  action. If any one is missing, NONE of the others take effect either -- [Chapter 11
  §11.10](../constitution/11-adr-process.md)'s own directly applicable rule ("Không
  partial activation") is the primary authority for this, not merely an indirect analogy;
  Chapter 9 §9.5/Chapter 10 §10.5.1's no-partial-activation principle applies to the same
  effect for the Constitution-tier documents specifically. Applied here to forbid
  `ADR-042` `Approved` + `ADR-031` `Superseded` while old review-gate text remains live
  anywhere in 2-9 above.

Until this single action occurs: `ADR-042` remains `Draft`; `ADR-031` remains `Approved`
  and fully controlling; Chapter 0 (top banner and §3)/Chapter 11 (top banner, §11.5,
  §11.9)/Chapter 12 (intro prose and §12.2 item 8)/`docs/governance/phases/
  phase-3-rules.md` §11 Gate-path's `≥2 independent review` prerequisite, as they all
  stand today, continue to govern every decision without exception, including whatever
  review `ADR-042` itself receives before this action.
```

**Independent reviews / Concerns / Risks noted:**

| Reviewer principal | Role at review boundary | Execution ID | Review boundary | Independence mode | Isolation attestation | Concern | Risk | Recommendation |
|---|---|---|---|---|---|---|---|---|
| ChatGPT | AI Technical Architect | N/A (Mode A — not required) | `64fc05becc6ca4245229db79b3fb6c5d9187e622` | Mode A (`DISTINCT_PRINCIPAL`) | Distinct principal from Independent Review B | — | — | CLEAN, READY_FOR_INDEPENDENT_REVIEW_B |
| Claude | AI Technical Architect (Independent Review B) | N/A (Mode A — not required) | `64fc05becc6ca4245229db79b3fb6c5d9187e622` | Mode A (`DISTINCT_PRINCIPAL`) | Distinct principal from Review A | `P3-ADR042-B2-MIN-01` — MANIFEST candidate-preview `version "0.4"` reference stale vs. reviewed `v0.5` | Minor, non-blocking | Accept; fold deterministic `v0.4 → v0.5` correction into the atomic activation transaction |

> Trước approval phải có tối thiểu hai independent review execution đủ điều kiện, đúng Chapter 0 §3 / Chapter 11 §11.5 / [ADR-031](./ADR-031.md) §5 — **the pre-activation gate this ADR was itself subject to, per its own Migration section (not used to bootstrap its own rules).** Satisfied via Mode A (`DISTINCT_PRINCIPAL`): ChatGPT (Review A) and Claude (Independent Review B), both `AI Technical Architect` per `/team/team.yaml`, at boundary `64fc05becc6ca4245229db79b3fb6c5d9187e622`. Both CLEAN; `P3-ADR042-B2-MIN-01` accepted non-blocking by Product Owner, corrected in this same activation transaction (see MANIFEST).

**Scale check:**

```yaml
scale_check:
  current_scale: "Phase 3 — Core Backend; governance/review-process change, not a runtime/strategy/exchange/plugin artifact"
  expected_scale:
    strategy: 0
    exchange: 0
    plugin: 0
  decision_still_valid: true
  reason: >
    Review-gate policy cost scales with the number of governed decisions made over time,
    never with strategy/exchange/plugin count or event volume -- this ADR reduces uniform
    per-decision review ceremony for R0/R1 cases while keeping Review A mandatory and R2's
    cross-check available on demand, so it remains valid regardless of platform scale.
  reason_if_no: ""
```

**Consequences:** This ADR decides the target review-gate model only — it does not itself amend Constitution Chapter 0/11/12, `docs/templates/adr-template.md`, `docs/governance/execution-rules.md`, `docs/governance/phases/phase-3-rules.md`, or MANIFEST, and does not retire the mandatory two-review gate (see Migration above — a single, later, bundled governance action is required, of which `ADR-042`'s own approval is itself the first item, never a separate prior step). It does not modify or mutate `ADR-031.md`, which stays byte-identical/immutable. It does not touch the ADR-041 bootstrap work, Feature implementation, Quality Gates, module approval, the Phase 3 Approval Gate, or `LIVE` authorization. Until the single coherent atomic governance action (items 1–10 above) lands together, the mandatory two-independent-review gate remains fully controlling, including for this ADR's own approval — `ADR-042` cannot reach `Approved` outside that same bundled action. After that action: Review A remains mandatory for every decision; a second independent review becomes optional and advisory, recommended only for Product-Owner-visible R2 cases, never a hidden or automatic gate; historical review evidence recorded under the retired mandatory gate remains valid and is not reinterpreted.

**Accepted risks:** `P3-ADR042-B2-MIN-01` — MANIFEST candidate-preview's stale `version "0.4"` reference against the actual reviewed `v0.5` candidate — accepted by Product Owner as non-blocking, documentation-only; corrected deterministically in this same activation transaction (MANIFEST), not requiring a standalone correction/re-review.
