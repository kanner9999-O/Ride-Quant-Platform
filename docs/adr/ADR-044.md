---
id: ADR-044
title: "Bounded-Uncertainty Interpretation for Mutation Timeout Triage"
version: "0.4"
status: Draft
owner: Product Owner
reviewers: []
approved_by: null
approved_at: null
created_at: "2026-09-22"
last_review: null
next_review: null
depends_on: []
addresses: []
resolves: []
supersedes: []
---

# ADR-044: Bounded-Uncertainty Interpretation for Mutation Timeout Triage

> **Bounded Review A correction, remediated pending re-review** (not
> self-closed by the correction executor — closure is Review A's own
> re-review determination, never asserted here). Review A (ChatGPT, `AI
> Technical Architect`) reviewed v0.1 (reviewed blob at boundary
> `ca7034143a6e0c7a4d78c88baa3daa0e83364e3d`) and returned verdict
> `REVISION_REQUIRED — 0 Blocker / 2 Major / 1 Minor`, Risk `R2`.
>
> **`MAJOR-01`** — v0.1 did not reconcile the proposed truth table with
> Chapter 13 §13.8's existing fail-closed semantics ("measurement không
> reproducible" → FAIL). Corrected: §§"Formal bound definitions"/"Gate
> interpretation" below now explicitly distinguish **per-mutant
> reproducibility** (an `UNSTABLE_TIMEOUT_TRIAGE` mutant's own
> classification remains unresolved in every case, never asserted
> otherwise) from **gate-level bounded-measurement reproducibility** (the
> derived interval `[lower_score, upper_score]` is itself a deterministic,
> reproducible function of pinned inputs) — and states plainly that the
> latter is a **new** Quality-Gate measurement semantic, not already
> authorized by Chapter 13 v1.7 as written.
>
> **`MAJOR-02`** — v0.1 left a Chapter-13 update as a later, optional/
> likely documentation step; Chapter 13 §13.13 (Locked) reserves
> "measurement rules"/"pass/fail semantics" to Chapter 13 itself — an ADR
> alone cannot make a new gate measurement rule operational. Corrected:
> this ADR is now explicitly the *architecture decision/rationale* layer
> only; the *authoritative Quality-Gate measurement rule* is separately
> carried by `docs/constitution/13-quality-gates.md` v1.8 (candidate,
> `Draft`, new §13.8.1) — see "Relationship to Chapter 13" and
> "Activation model" below. Neither this ADR's own future approval, nor
> the Chapter 13 v1.8 candidate's mere existence, makes the rule
> operational alone — see the activation model's explicit conjunctive
> requirement.
>
> **`MINOR-01`** — Alternative 3 ("treat all unstable as survived")
> incorrectly stated it would force a FAIL even when `lower_score ≥ T`.
> Corrected: treating all unstable as survived is equivalent to using
> `lower_score` as a single point estimate, so it agrees with the
> bounded rule whenever `lower_score ≥ T` (PASS) or `upper_score < T`
> (FAIL) — its actual defect is confined to the straddle region
> (`lower_score < T ≤ upper_score`), where it wrongly forces FAIL instead
> of the correct `STOPPED / UNRESOLVED` (see corrected Alternative 3
> below).
>
> `MAJOR-01: CLOSED — REVIEW A VALIDATED` (v0.2 re-review, boundary
> `09bf6c7dd760deff7307fe8fbccb6f57217c4fc5` — see below)
> `MAJOR-02: CLOSED — REVIEW A VALIDATED` (v0.2 re-review, same boundary)
> `MINOR-01: CLOSED — REVIEW A VALIDATED` (v0.2 re-review, same boundary)

> **Bounded Review A correction (round 2), remediated pending re-review**
> (not self-closed — closure is Review A's own re-review determination,
> never asserted here). Review A (ChatGPT, `AI Technical Architect`)
> reviewed v0.2 (reviewed blob at boundary
> `09bf6c7dd760deff7307fe8fbccb6f57217c4fc5`), confirmed round-1
> `MAJOR-01`/`MAJOR-02`/`MINOR-01` genuinely remediated (`CLOSED — REVIEW
> A VALIDATED`, recorded above — NOT reopened by this correction), and
> returned a fresh verdict `REVISION_REQUIRED — 0 Blocker / 1 Major / 1
> Minor`, Risk `R2`, against two new findings.
>
> **`MAJOR-01` (round 2)** — Chapter 13 v1.8 candidate §13.8.1's Case C
> (`lower_score < T ≤ upper_score`) read as though `STOPPED / UNRESOLVED`
> were a fourth final Quality-Gate result, alongside §13.9's existing
> `FAIL — criteria` / `FAIL — evidence` / `PASS` taxonomy — no such fourth
> result is authorized by existing Chapter 13 structure. Corrected, in
> both this ADR (§3 below) and Chapter 13 §13.8.1: Case A/B remain **final
> Quality-Gate results** (`FAIL — criteria` / `PASS`, unchanged, no new
> vocabulary); **Case C is reclassified as an evaluation state, not a
> final result** — no `PASS`/`FAIL` is emitted, Quality-Gate eligibility
> remains incomplete/fail-closed (§13.1/§13.8's existing "Missing gate ≠
> passed gate" principle), and a downstream Approval Gate `PASS`
> prerequisite (Chapter 12 §12.2(5)) is not satisfied. Case C's evidence
> is explicitly NOT relabeled `FAIL — evidence` either, since the bounded
> interval itself may be complete/pinned/reproducible — it simply does
> not yet determine a final criteria outcome. No fourth final-result type
> is introduced; §13.9's existing three-entry taxonomy is preserved
> internally consistent.
>
> **`MINOR-01` (round 2)** — the Chapter 13 v1.8 candidate's own banner
> still cited `docs/adr/ADR-044.md v0.1`, stale since this ADR had already
> reached v0.2. Corrected: banner now cites the exact resulting v0.3
> candidate version (this document).
>
> `MAJOR-01 (round 2): CLOSED — REVIEW A VALIDATED` (v0.3 Review A,
> boundary `926feab8b4c1b6b7168f153713c37fe18a04d28c` — see below)
> `MINOR-01 (round 2): CLOSED — REVIEW A VALIDATED` (same boundary)

> **Review A (round 3), CLEAN, then optional R2 independent cross-check —
> substantive defects found, accepted, remediated.** Review A (ChatGPT,
> `AI Technical Architect`) reviewed v0.3 (boundary
> `926feab8b4c1b6b7168f153713c37fe18a04d28c`), confirmed round-2
> `MAJOR-01`/`MINOR-01` genuinely remediated (`CLOSED — REVIEW A
> VALIDATED`, recorded above — NOT reopened by this correction), and
> returned `CLEAN — 0 Blocker / 0 Major / 0 Minor`, Risk `R2`. At R2,
> Review A recommended the optional advisory cross-check; the Product
> Owner elected to use it (never a mandatory ADR field, never an
> approval prerequisite — ADR-042). The independent cross-check returned
> `DEFECT FOUND — 0 Blocker / 1 Major / 4 Minor`. ChatGPT independently
> re-verified all five findings against live repository authority and
> accepted all five, upgrading one:
>
> `X-MAJ-01: ACCEPTED — Major`
> `X-MIN-01: ACCEPTED, UPGRADED TO MAJOR` (strict authority reading could
>   otherwise derive both a gate-level PASS and a FAIL — evidence from
>   the same scoped inputs)
> `X-MIN-02: ACCEPTED — Minor`
> `X-MIN-03: ACCEPTED — Minor`
> `X-MIN-04: ACCEPTED — Minor`
>
> Effective correction input: `0 Blocker / 2 Major / 3 Minor`. Per
> `P3-CORRECTION-CHAIN-001` (this being the third semantic correction
> round), all five findings are consolidated into ONE internally
> complete correction rather than five narrow patches:
>
> - **`X-MAJ-01`** — the ADR's applicability scope (`UNSTABLE_TIMEOUT_TRIAGE`
>   only) was narrower than Chapter 13 §13.8.1's own broader language
>   ("governed unresolved-mutant populations", "whatever governed
>   protocol") — an ADR cannot narrow an overly-broad Chapter-13 rule,
>   since §13.13 makes Chapter 13, not the ADR, the measurement-semantics
>   authority. Corrected: §13.8.1 rewritten with a **closed applicability
>   predicate** (6 explicit conditions), an explicit exclusion list
>   (`AMBIGUOUS`/`TOOL_IDENTITY_DRIFT`/`not_checked`/other unresolved
>   categories), and a **mixed-population rule** (a scoped-`U` `Case B`
>   `PASS` never masks a separate non-scoped `FAIL — evidence`). This
>   ADR's own §4 now summarizes the same closed predicate as rationale,
>   deferring to §13.8.1 as authoritative, never narrowing it
>   independently. Protocol-equivalence language tightened throughout
>   (this ADR's Consequences; §13.8.1's own "Protocol identity"): no
>   validator/evaluator/executor may self-declare a future protocol
>   equivalent — only a separately governed, Product-Owner-approved
>   determination may establish that.
> - **`X-MIN-01`, upgraded to Major** — Testing Convention item 8's
>   per-mutant `FAIL — evidence` rule and this rule's gate-level `PASS`
>   were not explicitly layered, permitting a strict reading where the
>   same inputs simultaneously produce both. Corrected: new §6
>   ("Reconciliation with Testing Convention item 8") in this ADR, and a
>   matching subsection in §13.8.1, establish explicit precedence —
>   per-mutant status unchanged; the interval is the gate-level
>   measurement; Cases A/B/C govern the gate-level outcome; a non-scoped
>   defect still invokes ordinary §13.8/§13.9 independently — preventing
>   the same evidence set from ever yielding both a gate-level `PASS` and
>   a final gate-level `FAIL — evidence`.
> - **`X-MIN-02`** — §13.8.1's arithmetic was not fully self-sufficient
>   (relied implicitly on ADR wording for precision). Corrected: §13.8.1's
>   formal bound definitions now state percentage units explicitly for
>   `lower_score`/`upper_score`/`T`, and state `U ∩ confirmed_favorable =
>   ∅` and "no silent denominator exclusion" explicitly, self-sufficient
>   without needing this ADR's own text.
> - **`X-MIN-03`** — §13.8.1's body embedded candidate/lifecycle wording
>   ("`v1.8 CANDIDATE — NOT YET EFFECTIVE`" in its own heading, "proposed
>   text only", a stale reference to this ADR's "Consequences/Migration
>   sections") that would require semantic deletion at activation and
>   implied per-gate opt-in activation. Corrected: all such wording
>   removed from §13.8.1's body — lifecycle state now lives only in the
>   file banner, frontmatter, and MANIFEST; §13.8.1 is written as the
>   exact text intended to remain after activation, applying
>   deterministically wherever its closed predicate is satisfied, never a
>   per-gate evaluator "activation" choice.
> - **`X-MIN-04`** — this ADR's frontmatter `addresses: ["P3-FEATURE-QG-
>   EVID-03"]` misused a field Chapter 11 §11.4 defines strictly for OQ
>   identities ("OQ ADR đang xử lý"); `P3-FEATURE-QG-EVID-03` is not an OQ
>   identity, and no real OQ was found this ADR addresses. Corrected:
>   `addresses: []`.
>
> `X-MAJ-01: REMEDIATED — PENDING BOUNDED REVIEW A RE-REVIEW`
> `X-MIN-01 (upgraded MAJOR): REMEDIATED — PENDING BOUNDED REVIEW A RE-REVIEW`
> `X-MIN-02: REMEDIATED — PENDING BOUNDED REVIEW A RE-REVIEW`
> `X-MIN-03: REMEDIATED — PENDING BOUNDED REVIEW A RE-REVIEW`
> `X-MIN-04: REMEDIATED — PENDING BOUNDED REVIEW A RE-REVIEW`

**Status: Draft — NOT reviewed (this corrected v0.4 candidate), NOT
approved, NOT effective.** This ADR decides a *governed interpretation
rule* only. It does not itself apply that rule to any current evidence,
does not change any current Condition-1/2/3 state, and does not become
binding authority — nor does it, by itself, make the rule operational for
any gate evaluation — until Review A, Risk Classification, an explicit
Product Owner approval decision, **and** the separate Chapter 13 successor
(§13.8.1) becoming authoritative all complete. See "Activation model"
below.

## Context

`docs/governance/mutation-baseline-evidence/feature-engine-mutation-step9-formal-evidence-005.json`'s
locked timeout-triage protocol runs each raw-timeout mutant candidate
through two independent, freshly-regenerated, isolated single-worker
reproductions and classifies the outcome deterministically: timeout+timeout
→ `confirmed_timeout`; killed+killed → resolved `killed`; survived+survived
→ resolved `survived`; any disagreement → `UNSTABLE_TIMEOUT_TRIAGE`, with an
explicit "no majority vote, no guess" rule. `evidence-005-correction-001.json`
(`P3-FEATURE-EVID03-STEP9-005-A-MAJ-01`, Review A validated) established
that this protocol's disagreement clause is a genuine STOP on Condition-1
gate interpretation — the protocol defines what happens *up to and
including* a disagreement, but not what a Quality Gate is authorized to
conclude *given* one or more permanently-unresolved mutants in the
population.

The completed bounded investigation `FE-EVID03-COND1-STOP-001`
(`docs/governance/mutation-baseline-evidence/feature-engine-condition1-unstable-timeout-investigation-001.json`,
Review A `CLEAN — 0/0/0`, Risk `R1`, `ADR_NOT_REQUIRED` for the
investigation itself) confirmed the exact 9 mutants currently in this
state, found them structurally confined to real-filesystem-I/O-touching
`authority_resolver.py` functions, and classified all 9
`REQUIRES_GOVERNED_PROTOCOL_DECISION` — evidence is complete and
consistent for all 9; the existing protocol simply does not define a next
step.

## Problem

A Quality Gate whose raw score depends on a mutant population containing
one or more permanently-unresolved classifications has no existing,
repository-authoritative rule for producing a PASS/FAIL/STOP verdict. Two
naive resolutions are both wrong: (a) silently excluding unresolved mutants
from the population would change the denominator without governed
authority and would quietly discard exactly the disagreement the protocol
was designed to surface; (b) waiting indefinitely for the disagreement to
resolve blocks the gate even in cases where the disagreement is
*mathematically irrelevant* to the verdict (i.e., the gate would FAIL or
PASS identically regardless of how the unresolved mutants eventually
classify). Chapter 13 §13.10 (Locked) governs ordinary test-suite flakiness
— a *test* producing an unstable pass/fail on unchanged code — and is
relevant context but does not define how a mutation-evaluation
classification disagreement (`timeout ↔ killed`, an infrastructure-level
per-mutant outcome) should be formally consumed by a gate's score. No other
existing repository authority (Testing Convention v0.17 item 8's
deterministic-reproduction-before-credit rule) supplies this either — it
requires reproduction before credit, but does not define gate behavior when
reproduction itself disagrees. This is a genuine authority gap:
`EXISTING_AUTHORITY_INSUFFICIENT`.

## Decision candidate

**Bounded-Uncertainty Interpretation for Mutation Timeout Triage.**

### 1. Preserve unstable classification

A mutant with conflicting qualifying timeout-triage reruns remains exactly
`UNSTABLE_TIMEOUT_TRIAGE`. This decision never resolves, guesses, or infers
whether such a mutant is killed, survived, or confirmed-timeout.
Specifically prohibited, permanently, for this class of disagreement:

- majority vote;
- 2-of-3 (or any N-of-M) voting;
- an arbitrary third-run tie-break;
- retry-until-green.

### 2. Formal bound definitions

For a mutation gate whose population contains `U` unresolved
`UNSTABLE_TIMEOUT_TRIAGE` mutants, reusing — never replacing — the
already-authoritative mutation metric for that gate (Testing Convention
v0.17 item 7's Ride-owned formula, `(killed + confirmed_timeout) / (total −
skipped) × 100`, and that gate's own already-established denominator):

```text
lower_numerator (conservative) = confirmed favorable numerator
  = killed + confirmed_timeout
  (the U unstable mutants are granted ZERO favorable credit)

upper_numerator (best-case)    = lower_numerator + U
  (the U unstable mutants are hypothetically ALL resolved favorably)

denominator                    = the gate's own already-authoritative
                                  denominator (total − skipped), unchanged

lower_score = lower_numerator / denominator × 100
upper_score = upper_numerator / denominator × 100
```

No new metric, formula, or denominator is introduced. `U` unresolved
mutants never silently leave the denominator; they remain counted, exactly
as today, simply without favorable credit in the lower bound and with full
favorable credit in the upper bound.

### 3. Gate interpretation — final results vs. evaluation state

Given the gate's already-approved required threshold `T`. **Cases A and B
are final Quality-Gate results** — the same `FAIL — criteria`/`PASS`
vocabulary Chapter 13 §13.9 already defines, unchanged, not extended.
**Case C is not a fourth final result — it is an evaluation state.** No
new entry is added to §13.9's existing `FAIL — criteria` / `FAIL —
evidence` / `PASS` taxonomy:

| Case | Condition | Outcome type | Result/state | Reason |
|---|---|---|---|---|
| A | `upper_score < T` | **Final result** | `FAIL — criteria` | No possible resolution of the unstable cases reaches `T`. Per-mutant uncertainty persists but is mathematically irrelevant to the verdict. |
| B | `lower_score ≥ T` | **Final result** | `PASS` | `T` is met granting the unstable cases zero credit — PASS never relies on unstable evidence. |
| C | `lower_score < T ≤ upper_score` | **Evaluation state (NOT a final result)** | `STOPPED / UNRESOLVED` | The unresolved classifications can still change the outcome. No `PASS`/`FAIL` is emitted. Gate eligibility remains incomplete and fail-closed (§13.1/§13.8's "Missing gate ≠ passed gate") — a downstream Approval Gate `PASS` prerequisite (Chapter 12 §12.2(5)) is not satisfied. Not relabeled `FAIL — evidence` either: the bounded interval itself may be complete, pinned, and reproducible (§13.9) — it simply does not yet determine a final criteria outcome. |

Cases A/B/C are exhaustive and mutually exclusive for any `U ≥ 0`
(`U = 0` collapses `lower_score = upper_score`, reducing to the ordinary
two-way PASS/FAIL the gate already used before this decision existed —
Case C becomes structurally unreachable, not merely rare).

**Resume/follow-up for Case C.** A later evaluation may reach a final
result (Case A or B) only after a separately governed action changes the
authoritative inputs relevant to the interval — e.g. legitimate
resolution/remediation of the underlying unresolved cases under an
authorized protocol, a new authoritative measurement/evidence boundary, or
other separately governed remediation. This decision does not prescribe
which specific mechanism applies to any given Case C — that is future,
separately governed work. It explicitly does not authorize, as a way out
of Case C: majority vote, N-of-M voting, an arbitrary third-run tie-break,
retry-until-green, or denominator manipulation — all remain prohibited
exactly as in Cases A/B (§1/§4).

### 4. Anti-gaming constraints

- This rule NEVER increases a gate's numerator by excluding, reclassifying,
  or silently resolving an unresolved mutant — `lower_numerator` and
  `upper_numerator` are the only two computed values, and both are strict
  functions of the *count* of unresolved mutants, never their guessed
  status.
- A `PASS` under Case B is valid only when it holds with **zero** credit
  granted to any unresolved mutant — a future implementer MUST NOT tune
  `U`'s treatment per-mutant to manufacture a `PASS`.
- This rule supplies no mechanism to reduce `U` other than the
  already-existing, unchanged two-run timeout-triage protocol itself
  (§5 below) — it is a **read-only interpretation layer** over that
  protocol's output, never a second, competing way to resolve a mutant's
  status.
- Case C (`STOPPED`) is the *conservative default* whenever both bounds
  straddle `T` — there is no "close enough" or discretionary override to
  Case A/B from Case C.
- **Closed applicability scope (authoritative predicate: Chapter 13
  §13.8.1).** This rule applies only to mutants carrying the exact
  governed `UNSTABLE_TIMEOUT_TRIAGE` classification, produced by the
  presently-governed two-run timeout-triage protocol, remaining counted
  in the authoritative denominator and disjoint from the confirmed-
  favorable numerator (`U ∩ confirmed_favorable = ∅`). It is explicitly
  **not** a general "unresolved/ambiguous mutant" rule — `AMBIGUOUS —
  EVIDENCE INSUFFICIENT`, `TOOL_IDENTITY_DRIFT`, `not_checked`, and any
  other unresolved/evidence-integrity category are categorically
  excluded from `U` and remain governed solely by ordinary §13.8/§13.9.
  This ADR does not restate the full closed predicate here — Chapter 13
  §13.8.1 is the authoritative text (§13.13); this rationale summarizes
  it only to explain why the rule is deliberately narrow, never to
  narrow an otherwise-broader Chapter 13 rule (an ADR cannot narrow or
  widen Chapter 13's own measurement-rule scope; see "Relationship to
  Chapter 13 §13.13" below).
- **Mixed-population rule.** If an evaluation contains both eligible
  `UNSTABLE_TIMEOUT_TRIAGE` entries and any other unresolved/evidence-
  integrity defect outside this rule's scope, that other defect must be
  dispositioned under its own authority first — a `Case B` `PASS`
  computed from the scoped `U` never masks, substitutes for, or
  otherwise makes disappear a separate `FAIL — evidence` condition
  arising from the non-scoped defect. The same exact evidence set can
  never simultaneously yield both a `PASS` and a final `FAIL —
  evidence` result.

### 5. Reconciliation with Chapter 13 §13.8 (fail-closed semantics)

Chapter 13 §13.8 (Locked, v1.7) requires a gate to `FAIL` when
"measurement không reproducible." Read naively, an individually
unresolved `UNSTABLE_TIMEOUT_TRIAGE` mutant could seem to trigger this —
its own two-run classification genuinely did not reproduce. This decision
resolves that tension by distinguishing two separate reproducibility
questions, never conflating them:

- **Per-mutant reproducibility** — unchanged, governed exactly as §13.8
  already requires. An `UNSTABLE_TIMEOUT_TRIAGE` mutant's own
  killed/survived/confirmed-timeout classification remains unresolved.
  This decision NEVER asserts, implies, or treats that classification as
  having become reproducible, in any of Cases A/B/C.
- **Gate-level bounded-measurement reproducibility** — the *new* semantic
  this decision adds. When every input to §2's bound computation is
  immutable and pinned per §13.9 (confirmed-favorable numerator, `U`, the
  gate's own authoritative denominator, `T`, the exact criteria/policy
  version, and the evaluation boundary), the derived interval
  `[lower_score, upper_score]` is itself a deterministic, reproducible
  function of those pinned inputs — reproducible in exactly the sense
  §13.8/§13.9 require, even though the individual mutants inside `U`
  remain unresolved.

This is explicitly **not already authorized by Chapter 13 v1.7 as
written** — v1.7 has no concept of a gate-level bounded-measurement
interval distinct from per-mutant resolution. It is a genuinely new
Quality-Gate measurement semantic, which is exactly why this decision
requires both an ADR (this document, the architecture rationale) and a
separate Chapter 13 successor carrying the authoritative rule text (see
"Relationship to Chapter 13" and "Activation model" below) — never this
ADR alone.

Under this reconciliation: Case A (`FAIL — criteria`, a **final result**)
is authorized only when bounded measurement *proves*, with certainty, that
no resolution of the unstable cases reaches `T` — a genuine §13.8-style
criteria-failure, not a guess. Case B (`PASS`, a **final result**) never
relies on unresolved evidence. **Case C (`STOPPED / UNRESOLVED`) is not a
final result at all — it is an evaluation state**, the fail-closed outcome
whenever the interval straddles `T`: the bounded measurement itself can be
entirely valid, pinned, and reproducible, while the *threshold question*
remains genuinely undetermined by current evidence. This is consistent
with, not a weakening of, §13.8's own fail-closed philosophy and its
"Missing gate ≠ passed gate" principle — Case C leaves gate eligibility
incomplete, so a downstream Approval Gate `PASS` prerequisite (Chapter 12
§12.2(5)) is not satisfied, without asserting a criteria-failure that has
not been proven and without inventing a fourth entry in §13.9's existing
`FAIL — criteria` / `FAIL — evidence` / `PASS` result taxonomy.

### 6. Reconciliation with Testing Convention item 8 (per-mutant vs. gate-level)

Testing Convention v0.17 item 8 states, for timeout handling: "an
unresolved/unreproduced timeout is `FAIL — evidence` for that mutant."
This decision preserves that rule **exactly, unchanged, as a per-mutant
evidence disposition** — it never claims a `UNSTABLE_TIMEOUT_TRIAGE`
mutant's own classification became reproduced, and that mutant continues
to be disclosed/pinned as unresolved evidence with zero favorable credit.
What item 8 does not itself define is **gate-level aggregation** across a
population containing such mutants — and it does not, by itself,
automatically emit a *final gate-level* `FAIL — evidence` result for a
population satisfying Chapter 13 §13.8.1's closed applicability
predicate. For such a population, Chapter 13 §13.8.1 — not Testing
Convention item 8 directly — defines the gate-level aggregation semantics;
this is a deliberate, new Quality-Gate measurement rule owned by Chapter
13 (§13.13), and Testing Convention does not independently hold gate-level
PASS/FAIL authority. Strict precedence for an eligible population: the
per-mutant unresolved status remains exactly as item 8 describes; the
`[lower_score, upper_score]` interval is the gate-level measurement;
Cases A/B/C (§3) govern the gate-level outcome; and a separate, non-scoped
evidence defect (§4's mixed-population rule) still invokes ordinary
§13.8/§13.9 independently, regardless of this rule's own Case A/B/C
result. This reconciliation exists specifically so the same exact
evidence set can never simultaneously produce both a gate-level `PASS`
and a final gate-level `FAIL — evidence`.

## Relationship to Chapter 13 §13.10

Chapter 13 §13.10 (Locked) is explicitly preserved, not redefined, by this
decision:

- retry-until-green remains prohibited — this decision computes bounds
  from the *existing* two canonical protocol runs; it never triggers an
  additional run to change a mutant's classification;
- unstable evidence is never credited as passing evidence — the lower
  bound (which alone can produce a `PASS`) grants unresolved mutants zero
  credit, satisfying §13.10's rule directly;
- a specific *test* is quarantined only when evidence establishes that
  **that test itself** is flaky/timing-dependent on unchanged code —
  §13.10's own trigger, unchanged;
- mutation-evaluation classification instability (a mutmut-level, per
  mutant timeout-detection disagreement on *mutated* code) does NOT, by
  itself, automatically quarantine any test covering the unstable mutant.
  `FE-EVID03-COND1-STOP-001` found the 9 current unstable mutants' covering
  tests pass reliably and quickly (0.26s, uncontended) against unmutated
  code — there is no evidence any of those tests are themselves flaky under
  §13.10's own definition.
- Test-level quarantine (§13.10) and mutation-classification uncertainty
  (this ADR) are governed as **distinct concerns** with distinct triggers;
  this decision creates no automatic bridge from one to the other in
  either direction.

This ADR is a **gate-interpretation mechanism**, not a test-flakiness
labeling mechanism, and does not touch, extend, or narrow §13.10's own
quarantine trigger or evidence rule.

## Relationship to the existing locked timeout-triage protocol

The existing two-run protocol (`evidence-005.json` `timeout_triage.protocol`)
remains the sole, unchanged, authoritative mechanism for producing each
mutant's own classification: `confirmed_timeout`, `killed`, `survived`, or
`UNSTABLE_TIMEOUT_TRIAGE`. This decision does not alter, re-run, or
reinterpret any individual mutant's classification — it adds *only* the
previously-missing gate-level interpretation for a population that
*contains* one or more `UNSTABLE_TIMEOUT_TRIAGE` entries. No historical
evidence artifact is rewritten by this ADR: `evidence-005.json` and
`evidence-005-correction-001.json` remain byte-unchanged, and are not
modified by this ADR's approval, if it occurs.

## Relationship to Chapter 13 §13.13 — authority boundary and activation model

Chapter 13 §13.13 (Locked) is explicit: Chapter 13 owns "quality dimensions,
quality gate categories, minimum evidence, **pass/fail semantics**,
**measurement rules**, evidence contract" — other authorities, including
ADRs, "chỉ tham chiếu, không định nghĩa lại" (reference only, never
redefine). This decision respects that boundary strictly:

```text
ADR-044               = architecture decision / rationale for the
                         bounded-uncertainty interpretation (this
                         document) — WHY the rule is correct and what
                         it must guarantee.

Chapter 13 successor   = the authoritative Quality-Gate measurement rule
(v1.8, new §13.8.1)      itself — the ONLY artifact that can make this
                         rule an operational part of "pass/fail
                         semantics" for any gate, per §13.13.
```

`ADR-044 Approved` **alone** does not, and under §13.13 cannot, make the
bounded-uncertainty rule operational for any gate evaluation. Until the
Chapter 13 successor carrying §13.8.1 is itself reviewed, approved, and
activated as the chapter's controlling version, `v1.7` (without §13.8.1)
remains the sole authoritative Quality-Gate contract, and no EVID-03 (or
any other gate) evaluation may invoke the bounded-uncertainty rule.

### Activation model

Per the precedent already established in this repository for bundling an
ADR with a coordinated Constitution-chapter activation — [`ADR-042`](./ADR-042.md)'s
own "Atomic Activation Boundary," which activated the ADR itself together
with coordinated Chapter 0/11/12 lifecycle transitions and MANIFEST state,
in one Product Owner decision at one boundary — this decision's preferred
activation design is a **single atomic activation transaction**, requiring
ALL of the following to be decided together, at the same reviewed boundary,
by the same Product Owner decision:

```text
1. Product Owner approval of the reviewed semantic bundle (this ADR's
   final candidate content + the Chapter 13 v1.8 successor's final
   candidate content, reviewed together as one coherent decision)
2. ADR-044 -> Approved
3. Chapter 13 successor (v1.8, carrying §13.8.1) -> the chapter's new
   controlling authoritative version (v1.7 -> Superseded/retired as
   controlling, per Chapter 0 §5.1/§7.1's living-document lifecycle)
4. MANIFEST state updated to reflect both transitions atomically
= the bounded-uncertainty rule becomes effective
```

This design exists specifically to prevent a partial-activation state
where the ADR asserts new gate semantics are effective while the
controlling Chapter 13 version does not yet contain them (or vice
versa) — no such intermediate state is authorized. If, at the time of
Review A/Product Owner decision on this ADR, repository authority is
found not to support bundling an ADR approval and a Constitution-chapter
approval into one atomic transaction (contrary to the `ADR-042` precedent
cited above), the fallback is a **fail-closed staged sequence**: ADR-044
approval alone, followed by the Chapter 13 successor's own separate
Review A + Risk Classification + Product Owner approval, with the
bounded-uncertainty rule remaining explicitly `NOT EFFECTIVE` for any gate
evaluation until the LATER of the two approvals' own activation boundary.
Neither this fallback nor the preferred atomic design is executed, chosen
definitively, or activated by this Draft candidate — that determination is
Review A's/the Product Owner's own, at the appropriate future boundary.

## Current EVID-03 worked example (illustrative only — NON-CONTROLLING / FUTURE-IF-ACTIVATED)

Using `evidence-005.json`/`evidence-005-correction-001.json`'s own current,
already-recorded figures (verified fresh this transaction, not
recomputed): `killed = 2209`, `confirmed_timeout = 5`, `U = 9`,
`total = 2629`, required threshold `T = 87.001959503592%`.

```text
lower_numerator = 2209 + 5           = 2214
upper_numerator = 2214 + 9           = 2223
lower_score = 2214 / 2629 × 100      = 84.21453023963484%
upper_score = 2223 / 2629 × 100      = 84.55686572841384%

upper_score (84.56%) < T (87.00%)    -> Case A
```

**IF AND ONLY IF both (a) this ADR is reviewed and approved AND (b) the
Chapter 13 successor carrying §13.8.1 becomes the chapter's controlling
authoritative version** (see "Activation model" above — neither alone is
sufficient), the projected Condition-1 result under this rule would be
`FAIL — criteria` — because no possible resolution of the 9 unstable
mutants can reach the threshold. This projection is explicitly
**NON-CONTROLLING / FUTURE-IF-ACTIVATED** (not merely "future-if-ADR-
approved" — Chapter-13 activation is an equally required, separate
condition, per §13.13). This ADR candidate, by itself, changes nothing:
**Condition 1 remains `STOPPED / UNRESOLVED` now**, and remains so until
the full activation model above completes. `EVID-03` is not closed by
this document.

## Alternatives considered

1. **Permanent STOP on any disagreement, indefinitely.** Rejected as the
   sole rule — it is exactly today's un-augmented state, and blocks the
   gate even in cases (like the current one) where the disagreement is
   provably irrelevant to the outcome. Case C of the chosen candidate
   preserves this behavior exactly when — and only when — the disagreement
   genuinely could change the result.
2. **Majority vote / third-run tie-break.** Rejected — directly
   contradicted by the already-Review-A-validated locked protocol's own
   "no majority vote, no guess" clause and by Chapter 13 §13.10's
   retry-until-green prohibition; would silently manufacture a status for
   a mutant the protocol deliberately left unresolved.
3. **Treat all unstable as survived (maximally conservative).** Equivalent
   to using `lower_score` alone as a single point estimate for the final
   verdict — so it agrees with the chosen candidate whenever `lower_score
   ≥ T` (both would PASS) or `upper_score < T` (both would FAIL, as in the
   current EVID-03 case). Its actual defect is confined to the straddle
   region: when `lower_score < T ≤ upper_score`, this alternative would
   force a `FAIL` (since its own single point estimate, `lower_score`, is
   below `T`), while the correct bounded interpretation must `STOP —
   UNRESOLVED`, because the unresolved mutants can still change the
   verdict in that region and a forced FAIL would misstate genuine
   uncertainty as a certain criteria-failure. Rejected for this
   straddle-region defect — not for the reason v0.1 of this document
   incorrectly stated (corrected per `MINOR-01`).
4. **Treat all unstable as killed (maximally optimistic).** Rejected —
   this is precisely the ungoverned credit-without-evidence Testing
   Convention item 8 and Chapter 13 §13.10 both exist to prevent; it would
   allow an unresolved mutant to silently pass a gate.
5. **Test quarantine alone (apply §13.10 mechanically to covering tests).**
   Rejected as the sole mechanism — §13.10 quarantines a *test* found to be
   individually flaky; the 9 current unstable mutants' covering tests show
   no such evidence (fast, reliable, uncontended). Quarantining tests that
   are not shown to be flaky, merely because they cover an
   unstable-classified mutant, would misapply §13.10's own trigger and
   could suppress genuine future mutation-detection coverage.
6. **Bounded uncertainty / interval interpretation (CHOSEN).** Computes
   both bounds from the *existing*, unchanged protocol output; never
   credits an unresolved mutant; permits PASS only when uncredited
   evidence already suffices; permits FAIL only when no possible resolution
   changes the outcome; STOPs only when the outcome is genuinely
   undetermined. Strictly the narrowest rule that resolves the authority
   gap without inventing new per-mutant resolution machinery.

No alternative other than the bounded-uncertainty candidate is endorsed;
no fresh repository authority was found that contradicts or forecloses it.

## Consequences

This ADR decides the interpretation rule/rationale only; the authoritative
measurement-rule text lives in the Chapter 13 successor (§13.8.1), per
"Relationship to Chapter 13 §13.13" above. It does not implement, apply, or
trigger a recomputation of any current Quality Gate result. **Only after
the full activation model completes** — this ADR Approved AND the Chapter
13 successor activated as the chapter's controlling version AND MANIFEST
updated accordingly (see "Activation model") — would a **separate,
subsequent, bounded transaction** be required to formally apply the rule
to current EVID-03 evidence and record the resulting Condition-1 verdict;
this ADR does not perform that application itself, and ADR approval alone
does not suffice to authorize it. This decision is reusable in principle:
once activated, it applies to any Tier-1/Tier-0 mutation/test-effectiveness
gate evaluation satisfying Chapter 13 §13.8.1's own closed applicability
predicate — scoped strictly to the canonical `UNSTABLE_TIMEOUT_TRIAGE`
classification contract produced by the presently-governed two-run
timeout-triage protocol. A future, different protocol may qualify only
via its own separately governed, Product-Owner-approved equivalence
determination recorded before the relevant evaluation boundary — never by
an evaluator's, validator's, or executor's own self-declared equivalence.
This is precisely why it is a governed, reusable interpretation rule
rather than a one-off evidence correction, while remaining narrowly
bounded rather than open-ended.

## Failure modes

- **Silent scope creep to non-timeout uncertainty.** This rule is
  narrowly scoped to `UNSTABLE_TIMEOUT_TRIAGE`-class disagreements produced
  by the existing locked protocol. It must not be read as authorizing
  bounded-uncertainty treatment for any other kind of unresolved or
  ambiguous mutant status (e.g. Condition-2's own, separately-governed
  `AMBIGUOUS — EVIDENCE INSUFFICIENT`/`TOOL_IDENTITY_DRIFT` categories,
  which this ADR does not touch).
- **Threshold gaming via denominator manipulation.** Guarded against by
  §2's explicit requirement to reuse the gate's own already-authoritative
  denominator unchanged — this rule supplies no mechanism to alter it.
- **Case C used as a permanent escape hatch.** If a future population
  persistently straddles `T` across repeated measurements, that is a
  signal for separate, future governed work (e.g., root-cause remediation
  of the instability itself, per `FE-EVID03-COND1-STOP-001`'s own
  recommendation) — this ADR does not define a time limit or automatic
  escalation for a persistent Case C; that is intentionally left to
  ordinary Product Owner/architecture judgment, not hard-coded here.

## Scope / non-goals

**In scope:** the gate-level interpretation rule (§§1–6 of "Decision
candidate") for the exact, closed `UNSTABLE_TIMEOUT_TRIAGE`-class
mutation-evaluation applicability predicate (§4) only — never a general
"unresolved/ambiguous mutant" rule.

**Explicitly out of scope / non-goals:**

- does not change the locked two-run timeout-triage protocol itself;
- does not change Chapter 13 §13.10 flaky-test policy;
- does not change Testing Convention v0.17's Ride-owned metric formula or
  ten-status taxonomy;
- does not change the 87.001959503592% Feature Engine threshold or any
  other gate's threshold;
- does not apply itself to any current evidence or change any current
  Condition-1/2/3 state;
- does not resolve, or provide a mechanism for, Condition-2's ambiguous or
  tool-identity-drift rows;
- does not approve Feature Engine or authorize LIVE;
- does not, by itself, activate the Chapter 13 v1.8 successor (§13.8.1)
  as the chapter's controlling version — that is a separate, coordinated
  step of the "Activation model" above, not implied by this ADR's own
  approval alone.

## Migration / adoption implications (FUTURE / NOT YET EFFECTIVE)

The authoritative measurement-rule text is already drafted — as part of
this same corrective transaction — in `docs/constitution/13-quality-gates.md`
v1.8 (candidate, `Draft`, new §13.8.1); it is **not** deferred to a later
optional documentation step (correcting `MAJOR-02`). That candidate is
itself `NOT EFFECTIVE`: `v1.7` remains the chapter's sole controlling
version until the activation model above completes. Once activated, a
**future, separately-governed** update would likely also be warranted to
cross-reference this rule from Testing Convention v0.17 (alongside item
7's metric formula and item 8's timeout-credit rule) and/or the relevant
Quality-Gate evidence template, so future formal evidence transactions
apply it consistently rather than re-deriving it ad hoc. This is recorded
here strictly as a **prospective, not-yet-effective** note — no such
Testing Convention update is made or authorized by this Draft candidate.

## Rollback / reversal consideration

This decision is reversible: it is a pure interpretation-layer rule with
no schema, contract, or irreversible state change. Reversal would require
only a future ADR superseding this one (Chapter 0 §5.2 — immutable file,
new ADR identity, `supersedes: [ADR-044]`); no data migration, evidence
rewrite, or implementation rollback is implied, since this rule never
itself mutates any stored evidence artifact — it only computes bounds
from already-recorded, unchanged classifications at the moment a gate
verdict is formed.

## Scale check

```yaml
scale_check:
  current_scale: "Phase 3 — Core Backend, Feature Engine EVID-03 gate; 2629-mutant population, 9 UNSTABLE_TIMEOUT_TRIAGE cases currently outstanding; no other module currently exercises this exact protocol"
  expected_scale:
    strategy: 0
    exchange: 0
    plugin: 0
  decision_still_valid: true
  reason: >
    The decision's relevant scale dimension is mutant-population size and
    unresolved-case count (U), not strategy/exchange/plugin count (retained
    at 0 -- no production Strategy/Decision exists, not fabricated). The
    bound computation (lower/upper numerator, §2) is O(1) given already-
    computed ten_status_counts -- it does not degrade or change shape as
    the mutant population, module count, or U grows; a future module with
    a much larger population or a different U applies the identical three-
    case truth table (§3) unchanged. Growth in strategy/exchange/plugin
    count does not touch this decision's scope (test-effectiveness gate
    interpretation), since strategy/exchange/plugin are Decision-Pipeline-
    tier concerns, not Tier-1 Core Logic module mutation-gate concerns.
  reason_if_no: ""
```

## ADR Scope rationale (Chapter 0 §4b — `ADR_REQUIRED`)

```text
Governance/Approval-process change: YES. This decision changes how a
  Quality Gate PASS/FAIL/STOP verdict is formally derived when its
  underlying mutant population contains unresolved classifications --
  that is Quality-Gate/test-effectiveness interpretation semantics
  (Chapter 13's own domain), not a one-off evidence correction confined
  to a single artifact.
Decision affecting >1 module / reusable: YES. The rule is defined in
  terms of the already-authoritative mutation metric/denominator/
  threshold for "a mutation gate" generally, and is triggered by any
  future occurrence of UNSTABLE_TIMEOUT_TRIAGE-class disagreement under
  the same or an equivalently-governed protocol -- not scoped to
  Feature Engine's EVID-03 population specifically, even though EVID-03
  is its first and currently only applicable instance.
Not an internal refactor: this is a new interpretation RULE, not an
  implementation detail reversible at no cost to correctness -- a wrong
  choice here could silently manufacture a PASS or an unnecessary
  permanent STOP for any gate that encounters this pattern.
Conclusion: ADR_REQUIRED. Not downgraded to ADR_OPTIONAL or
  ADR_NOT_REQUIRED by this executor, per explicit instruction and per
  this rationale's own merits.
```

**Review A / Risk Classification / Concerns / Risks noted:**

| Reviewer principal | Role at review boundary | Review boundary | Concern | Risk | Recommendation |
|---|---|---|---|---|---|
| ChatGPT | AI Technical Architect | `ca7034143a6e0c7a4d78c88baa3daa0e83364e3d` (v0.1) | `MAJOR-01` (§13.8 reconciliation), `MAJOR-02` (§13.13 authority boundary/activation), `MINOR-01` (Alternative 3 logic) | R2 | `REVISION_REQUIRED — 0 Blocker / 2 Major / 1 Minor` — remediated in v0.2, CLOSED — REVIEW A VALIDATED at round 2 |
| ChatGPT | AI Technical Architect | `09bf6c7dd760deff7307fe8fbccb6f57217c4fc5` (v0.2) | `MAJOR-01` round 2 (§13.8.1 Case-C final-result-vs-evaluation-state conflict), `MINOR-01` round 2 (stale Chapter-13 banner ADR-version reference) | R2 | `REVISION_REQUIRED — 0 Blocker / 1 Major / 1 Minor` — remediated in v0.3, CLOSED — REVIEW A VALIDATED at round 3 |
| ChatGPT | AI Technical Architect | `926feab8b4c1b6b7168f153713c37fe18a04d28c` (v0.3) | Review A: none (`CLEAN`). Optional R2 cross-check (Product-Owner-selected, advisory, not an approval prerequisite): `X-MAJ-01` (applicability scope too broad in Chapter 13), `X-MIN-01`→upgraded Major (Testing Convention item 8 / gate-level precedence), `X-MIN-02` (arithmetic self-sufficiency), `X-MIN-03` (activation/candidate wording embedded in rule body), `X-MIN-04` (`addresses` metadata misuse) — all 5 independently re-verified and accepted by ChatGPT | R2 | Review A `CLEAN — 0 Blocker / 0 Major / 0 Minor`; cross-check `DEFECT FOUND — 0 Blocker / 1 Major / 4 Minor`; ChatGPT-accepted effective input `0 Blocker / 2 Major / 3 Minor` — see banner above for remediation |

> All three rows above are **historical provenance**, not an eligibility
> determination for this corrected v0.4 candidate. This exact v0.4
> candidate has **not yet been reviewed**. Before approval there must be a
> fresh Review A on v0.4 itself, eligible per Chapter 0 §3 / Chapter 11
> §11.5 — reviewer holding role `AI Technical Architect` at the review
> boundary, independently checking this candidate (including the closed
> applicability predicate, the mixed-population rule, the protocol-
> identity rule, the Testing Convention item 8 reconciliation, the
> self-sufficient arithmetic, the activation-wording cleanup, and the
> `addresses: []` correction) directly against repository authority —
> not inheriting this executor's own conclusions, or any prior round's
> review/cross-check, as ground truth for v0.4. Approval remains deferred
> because **known, accepted, substantive defects existed and required
> remediation** — not because the optional cross-check itself must be
> "satisfied"; the cross-check was advisory input that surfaced real
> defects, never an approval gate of its own.

**Risk Classification (expected for v0.4, not self-finalized by this
executor):**

```text
class: R2 (expected — not self-finalized; matches all three prior
  rounds' own historical R2 classifications, carried forward as an
  expectation only)
reason: >
  Introduces reusable Quality-Gate interpretation semantics for
  unresolved mutation outcomes, applicable beyond a single evidence
  correction, now spanning two coordinated artifacts (this ADR + the
  Chapter 13 v1.8 successor), with a closed applicability predicate,
  explicit Testing Convention item 8 precedence, and self-sufficient
  Chapter-13-side arithmetic. Final classification is ChatGPT's own
  determination at v0.4's fresh Review A, not asserted here as settled.
```

> R0/R1 default `NO CROSS-CHECK`. At R2, Review A recommends an *optional*
> advisory cross-check; Product Owner chooses whether to use it — this is
> never a mandatory ADR field, never an approval prerequisite, and its
> absence never disqualifies approval eligibility (ADR-042). The Product
> Owner exercised that choice for the v0.3 candidate (row above) and the
> cross-check surfaced genuine, since-remediated defects — this does not
> retroactively make cross-check mandatory for v0.4 or any future round;
> it remains the Product Owner's own discretionary choice each time.

**Consequences:** See "Consequences," "Failure modes," and "Migration /
adoption implications" above — this ADR decides an interpretation rule
only; no implementation, no evidence application, no Condition-1 state
change, no EVID-03 closure occurs by virtue of this Draft or any future
approval of it alone.

**Accepted risks:** None recorded — no Concern currently raised requires
Product Owner risk acceptance against a Platform Invariant.
