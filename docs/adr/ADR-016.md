---
id: ADR-016
title: Decision Engine Taxonomy — Hybrid Retention vs Decomposition
version: "0.8"
status: Approved
owner: Product Owner
reviewers: [ChatGPT, Claude]
approved_by: Product Owner
approved_at: "2026-08-04"
created_at: "2026-08-03"
last_review: "2026-08-04"
next_review: null
depends_on: [ADR-015]
addresses: []
resolves: []
supersedes: [ADR-015]
---

# ADR-016: Decision Engine Taxonomy — Hybrid Retention vs Decomposition

**APPROVED — status: Approved (2026-08-04). Disposition: APPROVED — Candidate B (split) selected, Mechanism A exercised (ADR-016 tự nó amend hiệu lực kiểm soát của ADR-015 cho phần bị ảnh hưởng).** ADR này quyết định taxonomy/responsibility placement — KHÔNG author Decision algorithm, field-level Decision schema, API schema, database schema, runtime deployment topology, programming framework, hay source code nào (ranh giới KHÔNG đổi bởi approval).

> **Atomic approval transition (2026-08-04):** sau reviewed Package 1.3-C exploratory evidence v0.2 (Review A CLEAN, Independent Review B CLEAN, Backward Consistency Check NO CONFLICT, `P13C-A-MAJ-01` RESOLVED), Decision Workflow resumed (v0.5), và hai bounded correction (v0.6 đóng `ADR016-B-MAJ-01`, v0.7 đóng `ADR016-A-MAJ-03`) làm governance path mechanism-neutral và trigger-accounting chính xác, Product Owner đã quyết định: **"I approve ADR-016 v0.7 with Candidate B — Split under Mechanism A."** (2026-08-04) — `status: Draft → Approved`, `version: 0.7 → 0.8`, `approved_by: Product Owner`, `approved_at: "2026-08-04"`, `last_review: "2026-08-04"`, `reviewers: [ChatGPT, Claude]`, `supersedes: [ADR-015]` (SCOPED — xem §"Approved ADR-015 amendment scope" dưới, ADR-015 KHÔNG bị sửa/invalidate toàn bộ). Đúng Chapter 11 §11.6 (Decision Workflow) và Chapter 11 §11.3: từ thời điểm này, `ADR-016.md` bất biến byte-for-byte trừ lifecycle-state cập nhật qua MANIFEST.md tương lai. **Trigger 1–5 đều SATISFIED** ngay tại approval này (Mechanism A, Path tương ứng §"Governance mechanism paths" — ADR-016 tự nó thiết lập authoritative responsibility definition VÀ ghi Product Owner decision). **Approval này KHÔNG tự động modify `module-registry.yaml`/`system-decomposition.md`, KHÔNG author field-level evaluation-proposal schema, KHÔNG thêm/đổi `attempt_outcome` value nào, KHÔNG authorize implementation, KHÔNG đánh dấu Package 1.1 Consolidated Stable, KHÔNG approve Package 1.1, KHÔNG pass Gate 2, KHÔNG tuyên bố Phase 1 hoàn thành, KHÔNG mở Phase 2, KHÔNG authorize Live.** Package 1.1 artifact correction + Review A/B + consolidation decision là các bước RIÊNG BIỆT, CHƯA thực hiện tại transaction này (xem §"Required sequence after any future approval").

**v0.5 — Decision Workflow resumed (2026-08-04), đóng deferral condition qua reviewed Package 1.3-C evidence:** [Package 1.3-C exploratory evidence](../architecture/package-1.3-c-decision-taxonomy-exploration.md) v0.2 (blob `6f0f65e91187184778f8984a814b5f3f1a47be2a`, authored dưới Gate 1 — `phase-1-plan.md` §6.1) hoàn tất Review A CLEAN + Independent Review B CLEAN + Backward Consistency Check NO CONFLICT (đóng `P13C-A-MAJ-01`, Blocker 0/Major 0/Minor 0) — thỏa `§"Required deferral trigger"` mục 2 (Package 1.3-C architecture candidate), mục 3 (Chapter 7 §7.1 validation), và mục 4 (Review A + Independent Review B) của bản v0.4 trước đó. **Mục 1 (authoritative responsibility definition) và mục 5 (Product Owner decision qua Approved ADR) VẪN pending** — xem §"Resume-trigger accounting (v0.5)". ADR này nay bổ sung §"Author recommendation (v0.5)" — một **recommendation** (ưu tiên Candidate B/split, CÓ ĐIỀU KIỆN, dựa trên evidence đã review), **KHÔNG PHẢI approval**. `status: Draft`, `approved_by: null`, `approved_at: null` không đổi. KHÔNG reopen architecture comparison của evidence artifact, KHÔNG select/approve Candidate A hay B, KHÔNG sửa ADR-015/Package 1.1 artifact/`phase-1-plan.md` (byte-identical, không đổi bởi transaction này).

**v0.6 — bounded mechanism-neutral governance correction (2026-08-04), đóng `ADR016-B-MAJ-01`** (finding review round trên v0.5: ADR-016 khai báo cả Mechanism A lẫn Mechanism B available, nhưng trigger wording/consequences/mandatory future sequence lại hardcode approval của CHÍNH ADR-016 làm vehicle mang hiệu lực amendment/supersession ADR-015 — khiến Mechanism B chỉ mang tính danh nghĩa, không executable; Blocker 0, Major 1, Minor 0; CHƯA verified): sửa Trigger 1/Trigger 5 wording (§"Resume-trigger accounting (v0.5)") thành mechanism-neutral — Trigger 1 thỏa khi MỘT Approved ADR-level taxonomy decision tồn tại (KHÔNG nhất thiết là ADR-016), Trigger 5 thỏa khi Product Owner decision ghi trong CHÍNH Approved ADR-level taxonomy decision đó. Thay thế `### Required governance effect if Candidate B is recommended AND Approved` bằng `### Governance mechanism paths (mechanism-neutral)` — định nghĩa đầy đủ **Mechanism A** (ADR-016 làm final governance vehicle, sequence bốn bước) VÀ **Mechanism B** (ADR kế nhiệm làm governance vehicle, hai path hợp lệ: **Path B1** — ADR kế nhiệm mang toàn bộ taxonomy decision + amendment effect; **Path B2** — ADR-016 chọn taxonomy trước, ADR kế nhiệm riêng biệt mang amendment/supersession effect sau, Trigger 1 CHƯA thỏa cho tới khi CẢ HAI Approved). Sửa `### Required sequence after any future approval` thành năm bước mechanism-neutral, tường minh bước 1-2 CÓ THỂ là một HOẶC hai Approved ADR transaction. Sửa "Consequences" split-branch — KHÔNG còn hardcode "Approval ADR-016 với Candidate B PHẢI..."; thay bằng "NẾU Mechanism A... NẾU Mechanism B...". KHÔNG reopen Candidate A/B architecture analysis, KHÔNG đổi author recommendation (vẫn ưu tiên Candidate B, có điều kiện), KHÔNG chọn Mechanism A hay B, KHÔNG approve ADR-016, KHÔNG sửa ADR-015/Package 1.1 artifact/`phase-1-plan.md`/Package 1.3-C evidence artifact (byte-identical, không đổi bởi transaction này). `status: Draft`, `disposition: RESUMED`, `approved_by: null`, `approved_at: null` không đổi.

**v0.7 — bounded Path B2 trigger-accounting correction (2026-08-04), đóng `ADR016-A-MAJ-03`** (finding review round trên v0.6 — ID mới, KHÔNG trùng với `ADR016-B-MAJ-01` lịch sử đã đóng tại v0.4; Blocker 0, Major 1, Minor 0; CHƯA verified): Mechanism B Path B2 (v0.6) nói Trigger 5 CÓ THỂ thỏa bởi ADR-016 Approved với Candidate B nhưng Trigger 1 CHƯA thỏa cho tới khi ADR-015 amendment/supersession effect cũng Approved — đây là lỗi, gộp lẫn **authoritative taxonomy responsibility definition** (Trigger 1, thứ ADR-016 TỰ NÓ thiết lập đầy đủ khi Approved với taxonomy decision + bốn ràng buộc authority guarantee) với **permission to change ADR-015's pinned Package 1.1 baseline** (một governance requirement RIÊNG BIỆT, không phải một phần của Trigger 1). Sửa: Path B2 nay năm bước — ADR-016 Approved với Candidate B VÀ authoritative responsibility definition (bước 1) thỏa CẢ Trigger 1 lẫn Trigger 5 NGAY (bước 2); Package 1.1 vẫn frozen (bước 3) VÌ ADR-015 vẫn LÀ controlling exact-artifact authority — KHÔNG PHẢI vì Trigger 1 pending; một ADR kế nhiệm riêng biệt mang ADR-015 amendment/supersession effect (bước 4); bounded Package 1.1 correction CHỈ bắt đầu SAU KHI cả hai Approved governance effect tồn tại (bước 5). KHÔNG đổi Path B1 (vẫn nguyên trạng), KHÔNG đổi Mechanism A, KHÔNG đổi mandatory five-step future ordering (đã mechanism-neutral từ v0.6), KHÔNG reopen Candidate A/B architecture analysis, KHÔNG đổi Candidate B recommendation, KHÔNG chọn Mechanism A hay B, KHÔNG approve ADR-016, KHÔNG sửa ADR-015/Package 1.1 artifact/`phase-1-plan.md`/Package 1.3-C evidence artifact (byte-identical, không đổi bởi transaction này). `status: Draft`, `disposition: RESUMED`, `approved_by: null`, `approved_at: null` không đổi.

**v0.8 — Product Owner approval (2026-08-04), Candidate B (split) selected under Mechanism A:** Product Owner quyết định "I approve ADR-016 v0.7 with Candidate B — Split under Mechanism A." — `status: Draft → Approved`, `version: 0.7 → 0.8`. Taxonomy decision APPROVED: Decision Evaluation Engine (non-authoritative deterministic evaluation only, KHÔNG Decision/Trade Intent authority) + Decision Authority Service (sole invariant-validation/Decision-append/Trade-Intent-identity authority); Strategy Plugin Host/Context Aggregator/Event Bus/Projection đều KHÔNG có Decision authority — đúng bốn ràng buộc đã nêu tại §"Author recommendation (v0.5)"/nay §"Approved taxonomy decision (v0.8)". Mechanism A exercised: ADR-016 tự nó amend hiệu lực kiểm soát của ADR-015 CHỈ cho phần decision-engine module classification + dependency edge bị ảnh hưởng (xem §"Approved ADR-015 amendment scope") — ADR-015 KHÔNG bị sửa, byte-identical, vẫn Approved/immutable/controlling cho toàn bộ 22-module inventory NGOÀI phạm vi hẹp này. Trigger 1–5 đều SATISFIED. **KHÔNG tự động sửa `module-registry.yaml`/`system-decomposition.md`, KHÔNG author field-level schema, KHÔNG thêm `attempt_outcome`, KHÔNG authorize implementation, KHÔNG đánh dấu/approve Package 1.1 Consolidated Stable, KHÔNG pass Gate 2, KHÔNG hoàn thành Phase 1, KHÔNG mở Phase 2, KHÔNG authorize Live.** Required post-approval sequence bước 3–5 (Package 1.1 correction transaction, Review A/B, consolidation decision) CHƯA thực hiện.

**v0.4 — narrow residual wording correction (2026-08-03), đóng `ADR016-A-MAJ-02` (hoàn tất) VÀ `ADR016-B-MAJ-01`** (Review A + Independent Review B trên v0.3 — `ADR016-A-MAJ-02` đánh giá lại là UNRESOLVED tại vòng review này vì đúng residual contradiction dưới đây khiến fallacy correction của v0.3 chưa triệt để; `ADR016-B-MAJ-01` CONFIRMED MAJOR ghi nhận cụ thể câu văn còn sót; Blocker 0, Major 1, Minor 0; CHƯA verified): v0.3's Alternative A block giữ lại câu "Ưu điểm: đúng bốn điều kiện Chapter 7 §7.1" ở current tense — mâu thuẫn TRỰC TIẾP với chính disposition DEFERRED/điều kiện 1 NOT ESTABLISHED/điều kiện 4 not satisfied mà v0.3 vừa thiết lập. Sửa: câu đó thay bằng "Lợi thế TIỀM NĂNG NẾU trong tương lai một authoritative responsibility boundary xác lập đủ cả bốn điều kiện... VÀ một ADR chứa quyết định có đủ bằng chứng đó được Approved" — điều kiện hóa hoàn toàn, KHÔNG còn khẳng định A hiện đang thỏa Chapter 7 §7.1. KHÔNG mở lại architectural analysis, KHÔNG đổi DEFERRED disposition, KHÔNG đổi Chapter 7 condition table, KHÔNG đổi resume trigger, KHÔNG đổi Package 1.1 consequence, KHÔNG đổi parallel-work boundary. ADR-015 KHÔNG bị sửa (byte-identical, blob `8940e14258ee331e7f2a38ffb0de49f5d01723e6`). `status: Draft`, `approved_by: null`, `approved_at: null` không đổi.

**v0.3 — final bounded correction (2026-08-03), đóng `ADR016-A-MAJ-02`** (Review A + Independent Review B trên v0.2 — ADR015-A-MAJ-01/ADR015-B-MAJ-02/ADR015-A-MIN-01/ADR016-A-MAJ-01 RESOLVED, `ADR016-A-MAJ-02` CONFIRMED MAJOR mới; Blocker 0, Major 1, Minor 0; CHƯA verified): v0.2 lập luận ĐÚNG rằng (a) split kỹ thuật khả thi, (b) Chapter 8 không cấm cross-module evaluation, (c) non-authoritative proposal khác authoritative Decision, (d) Alternative B cải thiện idempotency/testability/least-privilege, (e) ranh giới chính xác với `strategy-plugin-host` CHƯA được authoritative xác định — nhưng sau đó suy luận SAI: "vì ranh giới chưa được xác định, nên Chapter 7 §7.1 điều kiện 1 THỎA (giữ hybrid)." Đây là ngụy biện — thiếu authority/detail hiện tại chứng minh SỰ KHÔNG CHẮC CHẮN, KHÔNG chứng minh "không thể tách hợp lý" (điều kiện 1 đòi hỏi bằng chứng TÍCH CỰC cho việc KHÔNG THỂ tách, không phải bằng chứng thiếu vắng cho việc CÓ THỂ tách). Sửa: đổi disposition từ "Alternative A" sang **DEFERRED** — ADR-016 KHÔNG hiện approve hybrid (Alternative A) LẪN split (Alternative B); ghi nhận rõ evidence hiện tại KHÔNG đủ để chấp nhận CẢ HAI, KHÔNG đủ để định nghĩa stable split boundary mà không tự phát minh Decision semantics (ngoài phạm vi ADR). `decision-engine` hybrid tại Package 1.1 v0.2 vẫn: proposed, unresolved, KHÔNG governance-valid theo Chapter 7 §7.1 (điều kiện 1 KHÔNG ESTABLISHED, điều kiện 4 KHÔNG thỏa vì chưa ADR nào chứa quyết định thực sự được Approved), KHÔNG usable làm approved downstream architecture boundary. ADR-015 KHÔNG bị sửa bởi transaction này (byte-identical, blob `8940e14258ee331e7f2a38ffb0de49f5d01723e6`). Package 1.1 pin (`module-registry.yaml` v0.2 blob `2dd1e1fae8f886b605896864b432f3f79a3726d1`, `system-decomposition.md` v0.2 blob `45d745315ba36ea4ca53b5bb4bcd2aa6ca076293`) KHÔNG đổi. `status: Draft`, `approved_by: null`, `approved_at: null` không đổi.

**v0.2 — bounded correction (2026-08-03), đóng `ADR016-A-MAJ-01`** (Review A REVISE + Independent Review B REVISE trên v0.1, finding CONFIRMED — Blocker 0, Major 3 tổng cộng với ADR-015, Minor 1; CHƯA verified): v0.1's điều kiện 1 sai khi khẳng định Chapter 8 §8.4 bắt buộc evaluation+record atomic trong MỘT module — Chapter 8 KHÔNG có yêu cầu module-level đó, và chính pattern "Append-and-Revalidate" (ADR-010 §2.6) là bằng chứng NGƯỢC LẠI (Chapter 8 dự liệu và chấp nhận một khoảng hở evaluation→append). Sửa toàn diện: (a) Alternative B viết lại đầy đủ thành split kiến trúc khả tín (Decision Evaluation Engine compute_engine + Decision Authority Service runtime_service, phân biệt non-authoritative proposal/validation/atomic append); (b) điều kiện 1 re-evaluate qua mười bốn tiêu chí trung thực, KHÔNG chỉ atomicity; (c) ADR-010 analogy relabel thành operational-complexity concern, KHÔNG phải governance/semantic prohibition; (d) Alternative C giữ nguyên kết luận (vi phạm) nhưng phân biệt tường minh "plugin candidate/advisory signal" khỏi "platform deterministic Decision evaluation"; (e) Alternative D sửa wording "Governance cấm tuyệt đối defer" thành lý do schedule/dependency cụ thể. **Kết luận cuối vẫn Alternative A (hybrid retained)** — nhưng vì lý do MỚI: separation kỹ thuật khả thi (không còn atomicity block) nhưng KHÔNG đủ hợp lý/rõ ràng do ranh giới chưa giải quyết được giữa Decision Evaluation Engine đề xuất và `strategy-plugin-host` đã tồn tại (đòi hỏi author Decision algorithm để phân biệt — ngoài phạm vi ADR này) — KHÔNG force giữ nguyên kết luận cũ vì lý do cũ đã sai. Đây là kết luận GẦN, ghi nhận cho Review A/B xem xét kỹ, và có thể đảo ngược qua ADR hẹp tương lai nếu Package 1.3-C làm rõ ranh giới đó. `decision-engine` entry trong `module-registry.yaml` KHÔNG đổi (Package 1.1 pin nguyên trạng). `status: Draft`, `approved_by: null`, `approved_at: null` không đổi.

**Depends on / Amends (v0.8):** [ADR-015](./ADR-015.md) (**Approved**, v0.3, 2026-08-03, blob `37f2712aa0b204dcc6c58687226a4adcbeaa2f4f`, byte-identical/immutable — KHÔNG bị sửa bởi ADR-016 v0.8) — `decision-engine` module identity/existence tự nó được thiết lập bởi ADR-015 (official module decomposition, controlling authority). ADR-016 v0.8 (Approved) nay **amend hiệu lực kiểm soát của ADR-015 SCOPED** — CHỈ cho taxonomy classification của `decision-engine` module + dependency edge liên quan (xem §"Approved ADR-015 amendment scope"); ADR-015 vẫn LÀ controlling authority KHÔNG đổi cho 21 module còn lại + toàn bộ dependency graph ngoài phạm vi hẹp này. `frontmatter.supersedes: [ADR-015]` phản ánh quan hệ formal ADR-to-ADR (cùng pattern ADR-014/ADR-003) — KHÔNG có nghĩa ADR-015 vô hiệu toàn bộ; đây là supersession CỤC BỘ, không phải full invalidation.

**Context:** Package 1.1 v0.2 candidate ([`module-registry.yaml`](../architecture/module-registry.yaml) blob `2dd1e1fae8f886b605896864b432f3f79a3726d1`) khai báo `decision-engine` là `runtime_service` primary với `hybrid` **potential** — secondary responsibility `decision_evaluation` (deterministic evaluation of Strategy Instance tại `decision_context_cursor`, ADR-010/[Chapter 8 §8.4](../constitution/08-event-model.md)/§8.5). [Chapter 7 §7.1](../constitution/07-module-taxonomy.md) điều kiện 4 khóa: hybrid CHỈ hợp lệ khi được ghi bằng **ADR** thuộc diện ADR Required (Governance §4b). `system-decomposition.md` §12 Decision 2 tự xác định quyết định này ADR Required — ADR-016 là cơ chế đó.

**Existing candidate (Package 1.1 §8 — ARTIFACT itself KHÔNG đổi bởi approval này, chờ correction transaction riêng biệt):**

```text
module_id:              decision-engine
primary type:            runtime_service
authoritative ownership: Decision identity, Trade Intent identity
secondary responsibility: deterministic evaluation of Strategy Instance at
                          decision_context_cursor (ADR-010, Chapter 8 §8.4/§8.5)
current ADR state:        ADR-016 v0.8 APPROVED — Candidate B (split) selected. Entry trên
                          KHÔNG còn phản ánh authoritative taxonomy decision (ADR-016 v0.8
                          nay controlling) — NHƯNG `module-registry.yaml`/
                          `system-decomposition.md` ARTIFACT vẫn giữ nguyên nội dung này
                          (byte-identical, blob 2dd1e1fae8f886b605896864b432f3f79a3726d1/
                          45d745315ba36ea4ca53b5bb4bcd2aa6ca076293) cho tới khi một bounded
                          Package 1.1 correction transaction riêng biệt cập nhật nó (xem
                          §"Required sequence after any future approval").
```

**Decision question:** Liệu `decision-engine` giữ nguyên MỘT module hybrid (`runtime_service` primary + `decision_evaluation` secondary), hay tách thành module riêng.

**Decision (v0.8 — APPROVED, có hiệu lực từ 2026-08-04; disposition: APPROVED, Candidate B/split selected under Mechanism A):**

**ADR-016 v0.8 APPROVE Candidate B (split) làm official taxonomy decision.** Product Owner đã quyết định: **"I approve ADR-016 v0.7 with Candidate B — Split under Mechanism A."** (2026-08-04) — dựa trên Package 1.3-C exploratory evidence v0.2 (blob `6f0f65e91187184778f8984a814b5f3f1a47be2a`, Review A CLEAN + Independent Review B CLEAN + Backward Consistency Check NO CONFLICT, đóng `P13C-A-MAJ-01`) và hai bounded correction (v0.6/v0.7) làm governance mechanism/trigger-accounting chính xác. `decision-engine` tách thành **Decision Evaluation Engine** (compute_engine, non-authoritative deterministic evaluation only) + **Decision Authority Service** (runtime_service, sole invariant-validation/Decision-append/Trade-Intent-identity authority) — xem §"Approved taxonomy decision (v0.8)" cho đầy đủ authoritative responsibility definition. Approval này có hiệu lực NGAY — KHÔNG còn là recommendation.

**Trạng thái `decision-engine` hybrid SAU approval (v0.8) — Package 1.1 ARTIFACT không đổi bởi ADR này, chờ correction transaction riêng biệt:**

```text
Package 1.1 decision-engine hybrid (module-registry.yaml entry hiện tại, byte-identical):
                                      REJECTED bởi ADR-016 v0.8 — Candidate B (split) được
                                        Approved thay vì Candidate A (hybrid). Entry hybrid
                                        hiện tại KHÔNG còn phản ánh authoritative taxonomy,
                                        NHƯNG artifact vẫn giữ nguyên nội dung (blob
                                        2dd1e1fae8f886b605896864b432f3f79a3726d1) cho tới khi
                                        correction transaction cập nhật.
                                      Chapter 7 §7.1 điều kiện 4 (ADR-required decision) NAY
                                        SATISFIED cho quyết định TÁCH (không phải cho giữ
                                        hybrid) — ADR-016 v0.8 Approved chính là quyết định
                                        đó, nhưng disposition của quyết định là "tách", KHÔNG
                                        PHẢI "giữ hybrid hợp lệ".
                                      NOT usable làm approved downstream architecture
                                        boundary CHO TỚI KHI Package 1.1 correction transaction
                                        (§"Required sequence after any future approval") hoàn
                                        tất — artifact hiện tại phản ánh trạng thái CŨ, không
                                        phải quyết định mới.
```

**Chapter 7 §7.1 — bốn điều kiện (v0.3, sửa toàn diện):**

```text
Điều kiện 1 — responsibility không thể tách hợp lý về semantic/transaction boundary:
  NOT ESTABLISHED. v0.2 suy luận SAI rằng "ranh giới chưa được authoritative xác định" ⟹
  "không thể tách hợp lý". Đây là ngụy biện lập luận từ sự thiếu vắng bằng chứng (absence of
  evidence for separability) sang bằng chứng của sự không thể tách (evidence of
  inseparability) — hai mệnh đề KHÔNG tương đương. Điều kiện 1 đòi hỏi bằng chứng TÍCH CỰC
  rằng KHÔNG THỂ tách hợp lý; ADR này KHÔNG có bằng chứng đó, cũng KHÔNG có bằng chứng CÓ THỂ
  tách hợp lý — trạng thái đúng là CHƯA XÁC ĐỊNH (not established), không phải "thỏa" theo
  hướng nào.

Điều kiện 2 — ownership không vi phạm Chapter 3 §3.1 hay Context Map:
  Potentially satisfiable, pending chosen boundary. Chapter 3 §3.1 xác nhận Decision identity
  ownership thuộc Decision Engine — NHƯNG liệu "decision_evaluation" secondary responsibility
  cụ thể có vi phạm hay không phụ thuộc CHÍNH XÁC ranh giới được chọn (hybrid giữ nguyên, hay
  split theo boundary nào) — chưa chọn được boundary thì chưa đánh giá dứt khoát được.

Điều kiện 3 — primary type và secondary role khai báo tường minh trong module-registry:
  Satisfied ONLY cho representation candidate HIỆN TẠI (module-registry.yaml `decision-engine`
  entry đã khai `module_type: runtime_service` + `hybrid.secondary_responsibility:
  decision_evaluation` tường minh) — nhưng representation candidate đó tự nó CHƯA được approve
  (Package 1.1 vẫn candidate, KHÔNG Consolidated Stable). Điều kiện 3 hỏi "đã khai báo tường
  minh chưa", KHÔNG hỏi "đã đúng/đã approve chưa" — hai câu hỏi khác nhau.

Điều kiện 4 — quyết định ghi bằng ADR (Governance §4b):
  NOT satisfied. Sự TỒN TẠI của ADR-016 (dù ở trạng thái Draft) KHÔNG tự nó thỏa điều kiện 4 —
  điều kiện 4 chỉ thỏa khi một ADR CHỨA một quyết định THỰC SỰ được hỗ trợ bằng chứng và đã
  `Approved`. ADR-016 v0.3 KHÔNG chứa quyết định như vậy (disposition là DEFERRED) — điều kiện
  4 vẫn KHÔNG thỏa cho tới khi một ADR tương lai (v0.3 này hoặc bản kế nhiệm) đạt tới một quyết
  định có bằng chứng đầy đủ VÀ được Approved.
```

**Lý do deferral (bắt buộc theo đúng khuôn task) — KHÔNG khẳng định responsibilities không thể tách, KHÔNG khẳng định split invalid:**

```text
Product/Domain/Constitution authority hiện tại CHƯA định nghĩa ranh giới trách nhiệm CHÍNH XÁC
giữa:

  strategy-specific advisory output do Strategy Plugin Host sinh ra;
  bất kỳ platform-owned deterministic Decision evaluation nào (nếu có, và nếu khác Plugin
    output);
  authoritative Decision validation và append.
```

Sự thiếu vắng authority này KHÔNG phải bằng chứng cho "không thể tách" (Alternative A) VÀ KHÔNG phải bằng chứng cho "nên tách" (Alternative B) — nó chỉ là SỰ KHÔNG CHẮC CHẮN cần resolve TRƯỚC KHI một trong hai được approve.

**Atomicity wording correction (đóng một phần `ADR016-A-MAJ-01`):** v0.1 dùng cách diễn đạt ngụ ý "evaluation completion TỰ NÓ là authoritative Decision creation" — KHÔNG chính xác. Phân biệt tường minh ba khái niệm riêng biệt dùng xuyên suốt phần còn lại của ADR này:

```text
Non-authoritative evaluation proposal:  kết quả tính toán deterministic (candidate LONG/SHORT/
                                         NO_ACTION + evidence) — CHƯA phải Decision fact, có thể
                                         bị bỏ (abandon)/thử lại (retry) tự do, KHÔNG tạo partial
                                         authoritative state nào.
Authoritative Decision validation:      kiểm tra proposal identity/version/cursor/eligibility
                                         trước khi ghi nhận — vẫn CHƯA phải authoritative fact.
Atomic authoritative append:            hành động DUY NHẤT tạo ra Decision fact chính thức — CHỈ
                                         hành động này atomic/authoritative, KHÔNG phải toàn bộ
                                         chuỗi evaluation→validation→append phải atomic.
```

Chapter 8 KHÔNG cấm cross-module evaluation — Chapter 8 chỉ khóa NỘI DUNG field (decision_time/decision_context_cursor) VÀ cách xử lý registry-transition (Append-and-Revalidate, ADR-010 §2.6). Việc evaluation compute chạy ở module nào là câu hỏi module-taxonomy (Chapter 7), KHÔNG phải câu hỏi event-content (Chapter 8).

**ADR-010 Append-and-Revalidate analogy — relabeled (đóng một phần `ADR016-A-MAJ-01`):** analogy này trong v0.1 bị dùng SAI như bằng chứng governance/semantic cấm split. Sửa: đây CHỈ là một **operational-complexity concern** (nếu split, cần thiết kế recoverable-append-gap tương tự Append-and-Revalidate cho chính Decision creation) — **KHÔNG phải governance hay semantic prohibition**. Chapter 8/ADR-010 KHÔNG tuyên bố văn bản nào cấm việc này; ngược lại, chính sự tồn tại của Append-and-Revalidate như một pattern đã được đặt tên là bằng chứng Chapter 8 CHẤP NHẬN khoảng hở evaluation→append là hợp lệ khi được xử lý đúng cách — không phải bằng chứng cấm nó.

**Alternatives considered:**

```text
A. Retain one hybrid Decision Engine — runtime_service primary, deterministic evaluation
   secondary. [KHÔNG selected tại v0.8 — Product Owner approved Candidate B thay vì Candidate
   A, xem §"Approved taxonomy decision (v0.8)". Phân tích ưu/nhược điểm dưới đây GIỮ LẠI làm
   historical record/context, KHÔNG PHẢI decision hiện tại.]
   Lợi thế TIỀM NĂNG NẾU trong tương lai một authoritative responsibility boundary xác lập đủ cả
   bốn điều kiện Chapter 7 §7.1 (đóng `ADR016-B-MAJ-01` — sửa contradiction: v0.3 hiện đã ghi
   nhận điều kiện 1 NOT ESTABLISHED/điều kiện 4 not satisfied cho MỌI alternative, kể cả A; câu
   dưới đây KHÔNG còn khẳng định A "đúng bốn điều kiện" ở current tense) VÀ một ADR chứa quyết
   định có đủ bằng chứng đó được Approved: deterministic replay đơn giản hơn (một module, không
   cần đồng bộ hai); explainability cao hơn (một điểm trace); KHÔNG governance overhead cho một
   contract "evaluation proposal" mới (retention policy, identity scheme riêng).
   Nhược điểm: module lớn hơn (hai trách nhiệm liên kết); latency/testability/security-isolation
   theo lý thuyết kém hơn Alternative B (xem bảng tiêu chí) — NHƯNG internal code-level separation
   (evaluation logic tách biệt append logic BÊN TRONG cùng module) vẫn đạt được phần lớn lợi ích
   testability mà KHÔNG cần tách module.

B. Split into Decision Evaluation Engine (compute_engine) + Decision Authority Service
   (runtime_service). [**APPROVED tại v0.8, 2026-08-04, Product Owner, Mechanism A** — xem
   §"Approved taxonomy decision (v0.8)" cho authoritative responsibility definition đầy đủ.
   Ranh giới với `strategy-plugin-host` (redundancy risk nêu ở Nhược điểm dưới) vẫn là một
   governance gap CHƯA resolve, ghi nhận tường minh, KHÔNG tự động resolve bởi approval này.]

   Decision Evaluation Engine (compute_engine):
     - deterministic; đọc cursor-bounded/version-pinned input;
     - sinh ra evaluation proposal KHÔNG authoritative (immutable, có identity + version riêng
       để pin cho deterministic replay — cùng nguyên tắc "Referenced Authoritative Artifact"
       Chapter 8 §8.1.1 áp dụng tương tự cho artifact loại proposal);
     - KHÔNG sở hữu Decision hay Trade Intent identity;
     - KHÔNG được bypass Decision Authority Service hay Risk Gateway.

   Decision Authority Service (runtime_service):
     - validate proposal identity/version/cursor/eligibility;
     - reject proposal cũ/trùng/invalid;
     - sở hữu Decision và Trade Intent identity;
     - thực hiện atomic authoritative Decision append DUY NHẤT (single point of authoritative
       write, không đổi so với Alternative A).

   Ưu điểm THẬT (không phải chỉ "taxonomy sạch hình thức"):
     - Idempotent, dễ retry — Decision Evaluation Engine là pure-function-like compute, khớp
       chính xác định nghĩa Compute Engine (Chapter 7 §7.1: "KHÔNG sở hữu external side effect").
     - Precedent tồn tại trong chính domain model: risk.md đã dùng pattern Attempt-rồi-Record
       (RiskEvaluationAttemptRecorded → RiskEvaluation, C5-MAJ-01) — proposal→validate→append là
       cùng HỌ pattern, đã được chứng minh khả thi ở capability liền kề.
     - Testability tốt hơn — Decision Evaluation Engine test được độc lập không cần toàn bộ
       Decision Authority machinery.
     - Security/isolation tốt hơn — Decision Evaluation Engine (đọc-only, không ghi authoritative
       log) có thể chạy với ít quyền hơn Decision Authority Service (cần write access tới
       authoritative event log) — least-privilege.
   Nhược điểm THẬT:
     - REDUNDANCY RISK CHƯA GIẢI QUYẾT — "Decision Evaluation Engine" (compute_engine,
       deterministic, sinh candidate signal) có trách nhiệm rất giống `strategy-plugin-host`
       (đã tồn tại, cũng compute_engine, cũng sinh non-authoritative candidate signal theo
       Chapter 9 §9.1). Alternative B, như đề xuất, KHÔNG làm rõ ranh giới: Decision Evaluation
       Engine "chuẩn hóa" tín hiệu từ Plugin thành typed LONG/SHORT/NO_ACTION theo platform rule
       (ví dụ crossing_policy trong decision.md), hay nó TRÙNG LẶP việc Plugin đã làm? Câu hỏi này
       KHÔNG thể trả lời dứt khoát mà không author Decision algorithm — ngoài phạm vi ADR này.
     - Governance overhead mới: cần contract riêng cho "evaluation proposal" (identity/version/
       retention policy) — Package 1.3-C phải định nghĩa thêm một artifact loại mới.
     - Latency: thêm ít nhất một cross-module hop so với in-process call (Alternative A).
     - Dependency/operational complexity: thêm một node/edge triển khai/vận hành/giám sát.
   Đánh giá: KHÔNG PHẢI "trade-off tồi" như v0.1 khẳng định sai — đây là một alternative NGHIÊM
   TÚC với ưu/nhược điểm thật. Xem kết luận cuối cùng tại §"Chapter 7 condition 1 re-evaluation".

C. Place deterministic evaluation inside Strategy Plugin Host; Decision Engine chỉ record. [Kết
   luận GIỮ NGUYÊN — vi phạm — nhưng phân biệt rõ với Alternative B]

   **Phân biệt bắt buộc (Alternative C ≠ Alternative B):** Alternative B đề xuất một module MỚI,
   platform-owned, KHÔNG swappable (`Decision Evaluation Engine`) — khác về bản chất với
   Alternative C, vốn đặt evaluation logic BÊN TRONG chính `strategy-plugin-host` — module HOST
   CHO plugin có thể thay thế/promote/rollback theo Chapter 9 §9.1. Phân biệt:

   ```text
   Plugin candidate/advisory signal:      output của Strategy Plugin — có thể swappable,
                                           promotable, rollback-able, do bên thứ ba/nhiều phiên
                                           bản cùng tồn tại (Chapter 3 §3.1: authoritative ·
                                           shadow · experimental · migration song song).
   Platform deterministic Decision
     evaluation:                          logic platform-owned, KHÔNG swappable theo cùng cơ chế
                                           plugin lifecycle, áp dụng ĐỒNG NHẤT cho mọi Strategy
                                           Instance bất kể plugin nào đang chạy.
   ```

   KHÔNG phải MỌI computation một plugin thực hiện đều là "Decision logic thuộc thẩm quyền
   platform" — Plugin's chức năng chính LÀ sinh candidate signal (đó CHÍNH LÀ lý do Strategy
   Plugin tồn tại). Vấn đề của Alternative C KHÔNG phải "plugin tính toán gì đó", mà là đặt
   AUTHORITATIVE EVALUATION AUTHORITY (quyền quyết định LONG/SHORT/NO_ACTION CHÍNH THỨC, không
   chỉ đề xuất) vào một component swappable/replaceable.

   Đánh giá theo I-4/I-7/Decision authority/no hidden Decision ownership in plugin (GIỮ NGUYÊN
   kết luận, KHÔNG đổi):
     I-4 Strategy Isolation:      VI PHẠM RỦI RO CAO — đặt AUTHORITATIVE evaluation vào Plugin
                                   Host làm mờ ranh giới evaluation-là-authoritative-fact vs
                                   evaluation-là-candidate-signal.
     I-7 Plugin Non-Bypass:       VI PHẠM — Decision Engine trở thành pass-through thụ động,
                                   decision authority rò rỉ vào Plugin — anti-pattern Chapter 9
                                   §9.4/§9.5 cảnh báo (Decision Advisor tham gia NHƯNG KHÔNG sở
                                   hữu Decision fact).
     Decision authority:          VI PHẠM — decision-management capability sở hữu Decision
                                   identity BAO GỒM evaluation result — tách AUTHORITATIVE
                                   evaluation ra khỏi authority ghi nhận nó phá vỡ ownership.
     No hidden Decision ownership
       in plugin:                 VI PHẠM TRỰC TIẾP — swappable/promotable/rollback-able
                                   component (Chapter 9 §9.1) KHÔNG được là nơi AUTHORITATIVE
                                   evaluation authority cư trú.
   KHÔNG chọn — vi phạm cả bốn tiêu chí đánh giá bắt buộc, KHÔNG đổi bởi correction này.

D. Defer taxonomy decision. [ĐÃ SELECTED làm disposition tại v0.3 (historical) — KHÔNG phải vì
   deferral là lý tưởng, mà vì CẢ Alternative A LẪN Alternative B lúc đó KHÔNG đạt evidence
   threshold cần thiết mà không tự phát minh Decision semantics ngoài phạm vi ADR này. **SUPERSEDED
   tại v0.5 (Decision Workflow Resumed) sau khi Package 1.3-C evidence cung cấp evidence còn
   thiếu, và tại v0.8 (Approved, Candidate B selected)** — deferral KHÔNG còn là disposition hiện
   tại; giữ nguyên đoạn dưới làm historical record cho lý do deferral ban đầu hợp lý tại thời điểm
   đó.]
   Đánh giá (sửa — v0.1/v0.2 tuyên bố sai rằng Governance "cấm tuyệt đối" mọi defer tạm thời):
   Governance §4b KHÔNG có văn bản cấm tuyệt đối một defer tạm thời. Deferral tại đây KHÔNG
   giải quyết được sự chặn (Package 1.1 vẫn KHÔNG thể Consolidated Stable — candidate hiện tại
   ĐÃ khai báo một hybrid chưa resolve cần ADR, system-decomposition.md §12 Decision 2 đã tự xác
   định applicability này) — nhưng đây là disposition TRUNG THỰC duy nhất khi bằng chứng hiện có
   không đủ cho A lẫn B. §"Required deferral trigger" dưới đây định nghĩa CHÍNH XÁC evidence cần
   để resume quyết định — deferral KHÔNG phải "đóng băng vô thời hạn", mà có điều kiện resume rõ
   ràng.
```

## Required deferral trigger — evidence cần để resume quyết định

Deferral KHÔNG vô thời hạn. Quyết định resume KHI VÀ CHỈ KHI đủ NĂM điều kiện sau:

```text
1. Một authoritative responsibility definition phân biệt tường minh:
     plugin advisory output (Strategy Plugin Host);
     platform Decision evaluation (nếu tồn tại như một trách nhiệm riêng biệt);
     Decision authority/append (Decision Engine hoặc Decision Authority Service).
   Nguồn hợp lệ: Domain Contract correction (decision.md/strategy.md), ADR mới, hoặc Product
   Owner decision qua Decision Workflow (Chapter 0 §3) — KHÔNG tự phát minh bởi chính ADR-016.

2. Một Package 1.3-C architecture candidate cho thấy MỘT trong hai:
     một bounded hybrid module (decision-engine giữ nguyên, ranh giới rõ ràng không chồng lấn
       strategy-plugin-host); HOẶC
     một Evaluation Engine + Authority Service split KHÔNG chồng lấn (ranh giới rõ ràng, không
       trùng lặp trách nhiệm với strategy-plugin-host).

3. Explicit validation candidate đó đối chiếu đầy đủ bốn điều kiện Chapter 7 §7.1 — KHÔNG suy
   luận từ sự thiếu vắng bằng chứng như v0.2 đã sai.

4. Review A + Independent Review B trên candidate cụ thể đó.

5. Product Owner decision qua Decision Workflow (Chapter 0 §3), ghi nhận tại một ADR (v0.3 này
   cập nhật, hoặc ADR kế nhiệm) đạt `Approved`.
```

**KHÔNG author Decision algorithm hay Domain semantics còn thiếu tại ADR này** — mục trên chỉ định danh evidence CẦN, KHÔNG tự cung cấp evidence đó.

## Resume-trigger accounting (v0.5)

```text
Trigger 1 — authoritative responsibility definition:
  PENDING — thỏa KHI một Approved ADR-level taxonomy decision thiết lập authoritative
  responsibility definition. Package 1.3-C exploratory evidence (v0.2) phân tích domain-level
  evidence và đề xuất vocabulary/authority-question answers, nhưng KHÔNG PHẢI Domain Contract
  correction, KHÔNG PHẢI ADR Approved, KHÔNG PHẢI Product Owner decision — do đó Trigger 1
  CHƯA satisfied. ADR mang taxonomy decision đó CÓ THỂ là chính ADR-016 (Mechanism A) HOẶC
  một ADR kế nhiệm (Mechanism B) — KHÔNG giả định phải là chính ADR-016 (đóng
  `ADR016-B-MAJ-01`, xem §"Governance mechanism paths (mechanism-neutral)" dưới).

Trigger 2 — Package 1.3-C architecture candidate:
  SATISFIED — reviewed Package 1.3-C exploratory evidence v0.2 (blob
  `6f0f65e91187184778f8984a814b5f3f1a47be2a`, Review A CLEAN, Independent Review B CLEAN,
  Backward Consistency Check NO CONFLICT, `P13C-A-MAJ-01` RESOLVED, Blocker 0/Major 0/Minor 0)
  author đầy đủ CẢ HAI candidate yêu cầu: Candidate A (bounded hybrid) và Candidate B
  (non-overlapping split).

Trigger 3 — Chapter 7 §7.1 validation:
  SATISFIED — evidence §7 evaluate đầy đủ bốn điều kiện cho Candidate A với bằng chứng tích
  cực (KHÔNG suy luận từ thiếu chi tiết); evidence §8 validate Candidate B đối chiếu mười tiêu
  chí split-specific — cả hai đã qua cùng review evidence trên.

Trigger 4 — Review A + Independent Review B:
  SATISFIED — Review A CLEAN + Independent Review B CLEAN trên Package 1.3-C exploratory
  evidence v0.2 (đóng `P13C-A-MAJ-01`), Blocker 0/Major 0/Minor 0.

Trigger 5 — Product Owner decision qua Decision Workflow, ghi nhận tại Approved ADR:
  PENDING — CHƯA có Product Owner decision. `approved_by: null`, `approved_at: null`,
  `status: Draft` tại tài liệu này.
```

**Trigger 1 và Trigger 5 — mechanism-neutral (sửa `ADR016-B-MAJ-01`):** Trigger 1 thỏa khi một Approved ADR-level taxonomy decision thiết lập authoritative responsibility definition; Trigger 5 thỏa khi Product Owner decision được ghi nhận TRONG chính Approved ADR-level taxonomy decision đó. Trigger 1 và Trigger 5 CÓ THỂ thỏa bởi CÙNG một Approved ADR transaction (Mechanism A — chính ADR-016), NHƯNG KHÔNG BẮT BUỘC phải là chính ADR-016 nếu một ADR kế nhiệm trở thành final governance vehicle (Mechanism B — xem §"Governance mechanism paths (mechanism-neutral)" dưới). **KHÔNG đánh dấu sớm** Trigger 1 hay Trigger 5 satisfied trước khi approval thực sự xảy ra, dưới BẤT KỲ mechanism nào.

## Approved taxonomy decision (v0.8) — Approved by Product Owner, 2026-08-04

**Approved decision:** Product Owner đã quyết định: **"I approve ADR-016 v0.7 with Candidate B — Split under Mechanism A."** (2026-08-04) — dựa trên Package 1.3-C exploratory evidence v0.2 (reviewed, Review A CLEAN + Independent Review B CLEAN + Backward Consistency Check NO CONFLICT). Author recommendation trước đây (v0.5, ưu tiên Candidate B có điều kiện) nay đã được Product Owner **APPROVE nguyên trạng** — Candidate B là official taxonomy decision, có hiệu lực NGAY từ 2026-08-04. Đây KHÔNG còn là recommendation.

### Candidate A evidence (recap, KHÔNG reopen — cite Package 1.3-C evidence v0.2 §4/§7)

```text
Condition 1:  NOT ESTABLISHED
Condition 2:  NOT ESTABLISHED
Condition 3:  SATISFIED cho current candidate representation ONLY (đã khai báo, KHÔNG đã
              approve)
Condition 4:  NOT SATISFIED cho tới khi ADR-016 Approved
```

**KHÔNG tuyên bố Candidate A hiện thỏa Chapter 7 §7.1** — hai trong bốn điều kiện NOT ESTABLISHED, một NOT SATISFIED, một chỉ SATISFIED theo nghĩa hẹp "đã khai báo".

```text
Benefits (trung lập):  một authority boundary; một điểm transaction/idempotency/replay;
                        operational/dependency complexity thấp hơn Candidate B.
Costs (trung lập):     hybrid necessity (điều kiện 1) và ownership compatibility (điều kiện 2)
                        vẫn KHÔNG được thiết lập tích cực — đây là gap chính khiến Candidate A
                        KHÔNG đủ evidence-based để recommend tại thời điểm này.
```

### Candidate B evidence (recap, KHÔNG reopen — cite Package 1.3-C evidence v0.2 §5/§6/§8)

```text
Technically credible:                       §5/§8 evidence — cấu trúc split rõ ràng, mọi split-
                                             validation criterion Addressed hoặc Addressed-với-
                                             chi-phí.
One authoritative append owner:             Decision Authority Service — không đổi từ authority
                                             hiện tại của decision-engine.
Least-privilege/evaluation isolation:       tốt hơn Candidate A — Decision Evaluation Engine
                                             đọc-only, không cần write access authoritative log.
Operational/dependency/replay complexity:   CAO HƠN Candidate A — hai điểm idempotency, hai
                                             trục replay-pin, thêm một module/node/edge.
Evaluation-proposal artifact:               governance/Domain gap CHƯA giải quyết — loại
                                             artifact MỚI chưa có Domain Contract.
Authority-level proposal rejection:         CHƯA có mapping xác nhận trong `attempt_outcome`
                                             enum hiện có của `decision.md` — residual gap ghi
                                             nhận, KHÔNG tự resolve tại ADR này (cấm invent
                                             attempt_outcome mới).
```

**Approved authoritative responsibility definition (bắt buộc, nay CÓ HIỆU LỰC — Trigger 1 thỏa bởi chính bốn ràng buộc này):**

```text
Decision Evaluation Engine:  non-authoritative; pure deterministic evaluation; KHÔNG có quyền
                              Decision hay Trade Intent append.
Decision Authority Service:  sole invariant validation authority; sole Decision append
                              authority; sole Trade Intent identity authority.
Strategy Plugin Host:        advisory output only; KHÔNG có Decision authority.
Context Aggregator/
  Event Bus/Projection:      KHÔNG có Decision authority.
```

(Xác nhận đầy đủ cả bốn — đúng Package 1.3-C evidence v0.2 §5.1/§5.2/§5.4, không mâu thuẫn — nay là authoritative, có hiệu lực.)

### Approved ADR-015 amendment scope (v0.8 — Mechanism A exercised)

**Mechanism A exercised.** ADR-016 v0.8 (chính tài liệu này, Approved) tường minh amend hiệu lực kiểm soát của [ADR-015](./ADR-015.md) — **SCOPED, KHÔNG toàn bộ**:

```text
Amended (nay controlling qua ADR-016 v0.8):
  - Taxonomy classification của module `decision-engine` (module-registry.yaml v0.2 entry) —
    KHÔNG còn primary runtime_service + hybrid secondary decision_evaluation. Thay bằng hai
    module riêng: Decision Evaluation Engine (compute_engine) + Decision Authority Service
    (runtime_service) — xem §"Approved taxonomy decision (v0.8)" cho authoritative
    responsibility definition đầy đủ.
  - Dependency edge liên quan: risk-gateway.depends_on hiện trỏ `decision-engine` — cần
    resolve lại tới decision-authority-service khi Package 1.1 correction transaction thực
    hiện; decision-authority-service.depends_on cần thêm edge tới decision-evaluation-engine.

KHÔNG amended (ADR-015 vẫn LÀ controlling authority, KHÔNG đổi):
  - 21 module còn lại trong 22-module inventory (module identity/type/responsibility/
    dependency KHÔNG đổi).
  - Toàn bộ dependency graph NGOÀI hai edge liên quan decision-engine nêu trên.
  - ADR-015.md TỰ NÓ — byte-identical, blob 37f2712aa0b204dcc6c58687226a4adcbeaa2f4f,
    KHÔNG bị sửa, vẫn Approved/immutable (Chapter 11 §11.3) — supersession này là quan hệ
    GOVERNANCE (frontmatter `supersedes: [ADR-015]`, cùng pattern ADR-014/ADR-003), KHÔNG
    PHẢI một edit vào chính văn bản ADR-015.
```

**Approval này KHÔNG tự động modify `module-registry.yaml`/`system-decomposition.md`** — hai artifact này vẫn byte-identical (blob `2dd1e1fae8f886b605896864b432f3f79a3726d1`/`45d745315ba36ea4ca53b5bb4bcd2aa6ca076293`) cho tới khi một bounded Package 1.1 correction transaction riêng biệt (xem §"Required sequence after any future approval") phản ánh quyết định này vào artifact.

### Rationale

```text
Candidate A lacks positive evidence for mandatory hybrid inseparability condition (Chapter 7
§7.1 điều kiện 1) — v0.1 mắc sai lầm ngụy biện "thiếu boundary ⟹ inseparable" (đã sửa
`ADR016-A-MAJ-02`); reviewed v0.5 evidence KHÔNG tìm thêm được bằng chứng tích cực nào cho
inseparability, chỉ tái xác nhận NOT ESTABLISHED.

Candidate B demonstrates một non-overlapping split khả tín, với authority isolation/least-
privilege rõ ràng hơn — mọi split-validation criterion Addressed, KHÔNG rò rỉ Decision
authority vào bất kỳ component nào khác (Plugin Host/Evaluation Engine/Context Aggregator/
Event Bus/Projection, đúng evidence v0.2 §5.4).
```

**KHÔNG che giấu chi phí Candidate B**: operational/dependency/replay complexity cao hơn, VÀ một governance/Domain gap thật (evaluation-proposal artifact chưa có Domain Contract; authority-level rejection chưa có `attempt_outcome` mapping) — cả hai PHẢI resolve TRƯỚC khi Candidate B trở thành implementation-ready, ngay cả SAU KHI Approved tại tầng taxonomy.

### Governance mechanism paths (mechanism-neutral, historical) — đóng `ADR016-B-MAJ-01`

**Cập nhật (v0.8):** Product Owner đã chọn VÀ exercise **Mechanism A** tại approval này — xem §"Approved ADR-015 amendment scope" cho hiệu lực đã có. Mục dưới đây GIỮ NGUYÊN làm historical/reference record mô tả đầy đủ các path từng available TRƯỚC approval — Mechanism B (Path B1/Path B2) nay **moot** cho quyết định NÀY (đã exercise Mechanism A rồi, không cần một ADR kế nhiệm nào để mang taxonomy decision hay amendment effect), nhưng vẫn giữ nguyên làm tài liệu tham khảo cho các quyết định taxonomy tương lai khác (nếu có).

**Nguyên tắc bắt buộc (không đổi):** mọi wording governance PHẢI phân biệt tường minh **taxonomy decision** (Trigger 1 + Trigger 5 — chọn Candidate A hay B, ghi Product Owner decision) khỏi **ADR-015 amendment/supersession vehicle** (ADR nào tường minh đổi hiệu lực kiểm soát của ADR-015 cho phần module inventory/dependency graph bị ảnh hưởng, CHỈ CẦN THIẾT nếu Candidate B được chọn). Hai việc này CÓ THỂ xảy ra trong CÙNG một Approved ADR transaction (Mechanism A — ĐÃ exercise tại v0.8), HOẶC qua HAI Approved ADR transaction phối hợp (Mechanism B — KHÔNG dùng tại quyết định này).

#### Mechanism A — ADR-016 làm final governance vehicle

```text
1. Product Owner approve ADR-016 với taxonomy decision (Candidate A hoặc B).
2. ADR-016 tường minh amend/supersede hiệu lực kiểm soát của ADR-015 cho đúng phần Decision
   module inventory/dependency graph bị ảnh hưởng — CHỈ áp dụng NẾU Candidate B được chọn
   (nếu Candidate A được chọn, KHÔNG cần amendment/supersession — module inventory/dependency
   graph không đổi).
3. Trigger 1 và Trigger 5 cùng thỏa.
4. Một bounded Package 1.1 correction transaction riêng biệt SAU ĐÓ CÓ THỂ bắt đầu.
```

**Path này ĐÃ được exercise tại v0.8 (2026-08-04, Product Owner approval) — bước 1-3 hoàn tất; bước 4 (Package 1.1 correction transaction) CHƯA thực hiện, xem §"Required sequence after any future approval".**

#### Mechanism B — ADR kế nhiệm riêng biệt làm governance vehicle

Hỗ trợ hai hình thức hợp lệ:

##### Path B1 — ADR kế nhiệm mang toàn bộ

```text
1. ADR-016 giữ vai trò reviewed recommendation/evidence carrier — KHÔNG tự nó Approved với
   taxonomy decision.
2. Một ADR kế nhiệm chứa: final taxonomy decision; authoritative responsibility definition;
   Product Owner decision; explicit ADR-015 amendment/supersession effect (nếu Candidate B).
3. Trigger 1 và Trigger 5 cùng thỏa qua Approved ADR kế nhiệm đó.
4. Một bounded Package 1.1 correction transaction riêng biệt SAU ĐÓ CÓ THỂ bắt đầu.
```

##### Path B2 — ADR-016 mang taxonomy decision; ADR kế nhiệm đổi baseline (sửa `ADR016-A-MAJ-03`)

```text
1. Product Owner approve ADR-016 với Candidate B VÀ authoritative responsibility definition
   đầy đủ:
     Decision Evaluation Engine: non-authoritative deterministic evaluation only;
     Decision Authority Service: sole invariant-validation authority; sole Decision append
       authority; sole Trade Intent identity authority.
2. Trigger 1 VÀ Trigger 5 cùng thỏa bởi CHÍNH Approved ADR-016 taxonomy decision đó — ADR-016
   TỰ NÓ đã thiết lập authoritative responsibility definition đầy đủ tại bước 1, KHÔNG cần
   chờ bất kỳ ADR nào khác.
3. Package 1.1 vẫn frozen — NHƯNG lý do là ADR-015 vẫn LÀ controlling exact-artifact authority
   cho 22-module inventory/dependency graph hiện tại (Approved, immutable), KHÔNG PHẢI vì
   Trigger 1 pending (Trigger 1 đã thỏa ở bước 2).
4. Một ADR kế nhiệm riêng biệt tường minh amend/supersede hiệu lực kiểm soát của ADR-015 cho
   đúng phần module inventory/dependency graph bị ảnh hưởng.
5. CHỈ SAU KHI CẢ HAI Approved governance effect (bước 1 và bước 4) tồn tại, bounded Package
   1.1 correction transaction mới được bắt đầu.
```

**Phân biệt bắt buộc (đóng `ADR016-A-MAJ-03`):** *authoritative taxonomy responsibility definition* (Trigger 1 — CÁI GÌ là platform-owned deterministic evaluation, CÁI GÌ là sole authority validate/append, ai không có Decision authority) KHÁC khỏi *permission to change ADR-015's pinned Package 1.1 baseline* (một governance requirement RIÊNG BIỆT — được phép sửa `module-registry.yaml`/`system-decomposition.md`). ADR-015 amendment/supersession KHÔNG phải một phần của Trigger 1 TRỪ KHI chính taxonomy decision đó thất bại trong việc định nghĩa trách nhiệm một cách authoritative (ví dụ: chọn Candidate B nhưng KHÔNG nêu đủ bốn ràng buộc authority guarantee — trường hợp đó KHÔNG áp dụng ở đây, vì bước 1 yêu cầu tường minh cả bốn). Dưới Path B2 đã sửa, Trigger 1 thỏa NGAY tại approval ADR-016 (bước 2) — Package 1.1 frozen là hệ quả của ADR-015 pin, KHÔNG phải hệ quả của Trigger 1 pending.

**Cập nhật (v0.8):** Product Owner ĐÃ chọn **Mechanism A** tại approval 2026-08-04 (xem §"Approved ADR-015 amendment scope"). Path B1/Path B2 KHÔNG được exercise — giữ nguyên làm reference record cho các quyết định taxonomy tương lai khác, KHÔNG áp dụng cho quyết định decision-engine này nữa.

**KHÔNG tự động mechanically modify Package 1.1 artifact dưới BẤT KỲ mechanism nào.** Mọi mechanism đều yêu cầu một transaction riêng biệt để phản ánh quyết định vào Package 1.1 artifact (xem "Required sequence after any future approval" dưới).

### Required sequence after any future approval (mechanism-neutral, sửa `ADR016-B-MAJ-01`) — trạng thái sau v0.8

```text
1. [HOÀN TẤT, 2026-08-04] Approved ADR-level taxonomy decision thiết lập authoritative
   responsibility definition VÀ ghi nhận Product Owner decision (Trigger 1 + Trigger 5) —
   ADR-016 v0.8 Approved, Candidate B selected.
2. [HOÀN TẤT, 2026-08-04] Approved ADR-level amendment/supersession của ADR-015 (Mechanism A,
   chính ADR-016 v0.8) — xem §"Approved ADR-015 amendment scope".
3. [CHƯA thực hiện] Một bounded Package 1.1 correction transaction riêng biệt
   (module-registry.yaml/system-decomposition.md), CHỈ được bắt đầu SAU KHI bước 1 VÀ bước 2
   (cả hai đã thỏa tại v0.8).
4. [CHƯA thực hiện] Review A + Independent Review B trên Package 1.1 artifact ĐÃ correct.
5. [CHƯA thực hiện] Product Owner quyết định Package 1.1 consolidation.
```

Bước 1 và bước 2 đã hoàn tất qua:

```text
một Approved ADR transaction duy nhất (Mechanism A, ADR-016 v0.8) — KHÔNG cần Path B1/Path B2
(Mechanism B không được exercise cho quyết định này).
```

**Bước 3-5 CHƯA xảy ra tại transaction v0.8 này** — đây là một mechanical approval transaction, KHÔNG PHẢI Package 1.1 correction transaction.

### Required non-effects (v0.8 approval KHÔNG tự nó)

```text
- modify module-registry.yaml;
- modify system-decomposition.md;
- author field-level evaluation-proposal schema;
- add/change attempt_outcome value nào;
- authorize implementation;
- mark Package 1.1 Consolidated Stable;
- approve Package 1.1;
- pass Gate 2;
- complete Phase 1;
- open Phase 2;
- authorize Live.
```

## Package 1.1 consequence

```text
ADR-015:
  Approved (v0.3, 2026-08-03) — vẫn LÀ controlling authority cho 21/22 module + toàn bộ
  dependency graph NGOÀI phần decision-engine đã amend; byte-identical, KHÔNG đổi bởi
  transaction này, blob 37f2712aa0b204dcc6c58687226a4adcbeaa2f4f. Cho PHẦN decision-engine
  taxonomy classification/dependency edge liên quan: ADR-016 v0.8 nay controlling (Mechanism
  A, xem "Approved ADR-015 amendment scope").

ADR-016:
  Approved (v0.8, 2026-08-04, Product Owner) — Candidate B (split) selected. Trigger 1-5 đều
    SATISFIED.

Package 1.1:
  vẫn Draft/candidate/blocked — KHÔNG đổi bởi approval này;
  vẫn KHÔNG thể Consolidated Stable — taxonomy decision NAY Approved (ADR-016 v0.8), NHƯNG
    Package 1.1 ARTIFACT (module-registry.yaml/system-decomposition.md) chưa phản ánh quyết
    định đó (byte-identical, chưa sửa) — cần một bounded correction transaction riêng biệt +
    Review A/B + consolidation decision (xem "Required sequence after any future approval",
    bước 3-5, CHƯA thực hiện) TRƯỚC KHI Consolidated Stable.
```

KHÔNG downstream package nào được authorize coi Package 1.1 artifact (module-registry.yaml/system-decomposition.md) như đã cập nhật theo quyết định này cho tới khi correction transaction hoàn tất — taxonomy decision Approved, artifact CHƯA correct.

## Parallel-work boundary

```text
Package 1.3-C CHỈ có thể được authorize cho bounded exploratory architecture work — CHÍNH XÁC
để sinh ra responsibility evidence còn thiếu tại §"Required deferral trigger" mục 1-2 — KHÔNG
gì khác.

Exploratory work đó KHÔNG được:
  giả định hybrid đã approve;
  giả định split đã approve;
  sửa Package 1.1 baseline như thể một quyết định đã final;
  authorize implementation.
```

**Xác nhận governance hiện có KHÔNG tự động cho phép việc này:** `phase-1-plan.md` §6 (Authoring sequence & parallelism) khóa "1.3-B chờ 1.3-A; 1.3-C chờ 1.3-B... KHÔNG author 'trước' bằng cách giả định output của package chưa Consolidated Stable" — KHÔNG có điều khoản nào tường minh cho phép "bounded exploratory work trong khi Package 1.1 unresolved". Do đó, ADR-016 **KHÔNG tự authorize** Package 1.3-C exploratory work tại transaction này — chỉ ghi nhận đây là một **Product Owner decision khả dĩ trong tương lai** (qua Decision Workflow, Chapter 0 §3, có thể cần narrow amendment cho `phase-1-plan.md` §6 nếu Product Owner muốn phép ngoại lệ này), KHÔNG phải quyết định đã có hiệu lực.

**Cập nhật (v0.5, 2026-08-04):** đúng như dự liệu ở trên, Product Owner đã authorize narrow amendment `phase-1-plan.md` §6.1 (v0.3, Approved v0.4) — thiết lập Gate 1 (exploratory authoring authorization)/Gate 2 (normal package authorization, vẫn `blocked`) — và mở Gate 1 (2026-08-04). Package 1.3-C exploratory evidence (v0.2) đã được author dưới Gate 1 và hoàn tất Review A/Independent Review B CLEAN — xem §"Resume-trigger accounting (v0.5)". Gate 2 **vẫn `blocked`**, không đổi bởi cập nhật này.

**Cập nhật (v0.8, 2026-08-04):** taxonomy decision nay Approved (Candidate B, xem §"Approved taxonomy decision (v0.8)") — Gate 1's exploratory evidence purpose ĐÃ hoàn thành mục đích của nó (evidence dẫn tới approval này). Gate 2 (normal Package 1.3-C authorization) **vẫn `blocked`**, không đổi — approval taxonomy KHÔNG tự động pass Gate 2, KHÔNG unblock Package 1.1 artifact edit ngoài đúng "Required sequence after any future approval" bước 3-5.

## Chapter 7 condition 1 re-evaluation (đóng `ADR016-A-MAJ-01`, đầy đủ — thay thế hoàn toàn atomicity-only argument của v0.1)

**KHÔNG assert condition 1 chỉ dựa authoritative-append atomicity** (v0.1 đã sai ở đây). Đánh giá đầy đủ theo mười bốn tiêu chí task yêu cầu:

```text
Semantic cohesion:            TRUNG BÌNH cho split — "đề xuất candidate" và "ghi nhận chính
                               thức" là hai HÀNH ĐỘNG khác nhau về bản chất (tính toán vs cam
                               kết/authority), tương tự nhiều domain khác tách "calculate" khỏi
                               "commit". KHÔNG phải lý do quyết định một mình.
Transactional cohesion:       KHÔNG còn ủng hộ Alternative A một cách dứt khoát như v0.1 tuyên
                               bố — Chapter 8's Append-and-Revalidate pattern TỰ NÓ là bằng chứng
                               một khoảng hở evaluation→append là CHẤP NHẬN ĐƯỢC khi xử lý đúng
                               (không phải bị cấm).
Idempotency:                  ỦNG HỘ Alternative B — Decision Evaluation Engine (đề xuất) là
                               idempotent-by-design (pure deterministic compute), khớp chính xác
                               định nghĩa Compute Engine (Chapter 7 §7.1).
Failure recovery:             CẢ HAI khả thi — Alternative B cần recoverable-append-gap pattern
                               (đã có precedent: risk.md's RiskEvaluationAttemptRecorded →
                               RiskEvaluation, C5-MAJ-01); Alternative A có một failure domain
                               duy nhất, đơn giản hơn nhưng KHÔNG "an toàn hơn" về bản chất.
Proposal identity/retention:  CHI PHÍ THẬT của Alternative B — cần contract MỚI (identity/
                               version/retention policy cho "evaluation proposal") mà Alternative
                               A không cần. Đây là governance overhead thực, không phải giả định.
Latency:                      Ủng hộ nhẹ Alternative A — split thêm ít nhất một cross-module hop.
Replay determinism:           CẢ HAI khả thi nếu thiết kế đúng — Alternative B cần pin exact
                               proposal artifact (cùng nguyên tắc Referenced Authoritative
                               Artifact, Chapter 8 §8.1.1) để Replay tái dựng đúng; Alternative A
                               chỉ cần pin input/config như hiện tại. B tăng complexity, KHÔNG
                               tăng RỦI RO nếu design đúng.
Version pinning:              Alternative B cần pin CẢ evaluation-logic version LẪN input version
                               — chi phí thêm, không phải blocker.
Audit/explainability:         TRUNG LẬP-NGHIÊNG A nhẹ — Alternative A: một điểm trace; Alternative
                               B: hai bản ghi (proposal + append) CÓ THỂ cho audit trail chi tiết
                               hơn (đặc biệt khi proposal bị reject) nhưng thêm độ phức tạp trace.
Testability:                  ỦNG HỘ Alternative B THẬT SỰ — Decision Evaluation Engine test được
                               độc lập, không cần toàn bộ Decision Authority machinery. v0.1 đã
                               ĐÁNH GIÁ THẤP điểm này không công bằng.
Operational complexity:       ỦNG HỘ Alternative A — B thêm một module để deploy/monitor/version.
Dependency complexity:        ỦNG HỘ Alternative A — B thêm một node + một edge.
Security/isolation:           ỦNG HỘ Alternative B THẬT SỰ — Decision Evaluation Engine (read-
                               only compute) có thể chạy least-privilege, không cần write access
                               tới authoritative event log; Decision Authority Service giữ quyền
                               ghi riêng biệt. v0.1 KHÔNG xem xét điểm này.
God-module risk:              HAI HƯỚNG — Alternative A giữ hybrid có kiểm soát (bounded, hai
                               trách nhiệm liên kết chặt); Alternative B tránh hybrid NHƯNG tạo
                               rủi ro NGƯỢC: một module compute_engine mới (Decision Evaluation
                               Engine) có ranh giới KHÔNG rõ ràng với `strategy-plugin-host` đã
                               tồn tại (cả hai đều "compute_engine sinh non-authoritative
                               candidate signal") — nguy cơ hai module mỏng, chồng lấn trách
                               nhiệm, thay vì một module dày nhưng rõ ràng.
```

**Kết luận điều kiện 1 (v0.3 — sửa ngụy biện, đóng `ADR016-A-MAJ-02`):** separation là **KỸ THUẬT KHẢ THI** (Chapter 8 không cấm — atomicity argument của v0.1 đã sai và bị loại bỏ) VÀ có lợi ích thật (idempotency, testability, security-isolation). Alternative B, ĐÚNG NHƯ ĐỀ XUẤT, để lại một câu hỏi kiến trúc CHƯA giải quyết được TRONG PHẠM VI ADR này: ranh giới CHÍNH XÁC giữa `Decision Evaluation Engine` (đề xuất) và `strategy-plugin-host` (đã tồn tại, Chapter 9 §9.1) — cả hai đều compute_engine sinh candidate signal, và phân biệt chúng đòi hỏi mô tả CHI TIẾT Decision algorithm — **đây chính xác là điều ADR này bị cấm author**.

**v0.2 dừng lại SAI ở đây** — suy luận "ranh giới chưa xác định ⟹ điều kiện 1 THỎA (giữ hybrid)" là ngụy biện: thiếu bằng chứng CÓ THỂ tách hợp lý KHÔNG đồng nghĩa có bằng chứng KHÔNG THỂ tách hợp lý. Khác với `risk-gateway` (P11-A-MAJ-01), nơi Chapter 3 §3.1 nêu TƯỜNG MINH ownership (không mơ hồ boundary nào cần giải quyết, nên "giữ nguyên một module" có căn cứ TÍCH CỰC), với `decision-engine` KHÔNG có văn bản Constitution/Domain Contract tương đương xác định RANH GIỚI CHÍNH XÁC giữa "Plugin's candidate signal" và "platform's further deterministic evaluation" theo BẤT KỲ hướng nào — không có căn cứ tích cực cho "không thể tách" (Alternative A) LẪN "nên tách theo ranh giới X" (Alternative B). Trạng thái trung thực là điều kiện 1 **NOT ESTABLISHED**, không phải "thỏa" theo hướng giữ hybrid.

**Do đó: DEFERRED (kết luận tại v0.3/v0.4, thời điểm đó chính xác)** — KHÔNG chọn Alternative A, KHÔNG chọn Alternative B. Bằng chứng hiện tại không đủ cho một trong hai mà không tự phát minh Decision semantics (Decision algorithm-level detail) ngoài phạm vi ADR-016. Xem §"Required deferral trigger" cho điều kiện resume, và §"Package 1.1 consequence"/§"Parallel-work boundary" cho hệ quả cụ thể.

**Cập nhật (v0.5):** deferral trên đã **RESUME** sau khi Package 1.3-C exploratory evidence (v0.2, reviewed) cung cấp Trigger 2/3/4 — xem §"Resume-trigger accounting (v0.5)".

**Cập nhật (v0.8, 2026-08-04):** RESUMED trên đã **APPROVED** — Product Owner đã quyết định "I approve ADR-016 v0.7 with Candidate B — Split under Mechanism A." Candidate B (split) là official taxonomy decision, có hiệu lực từ 2026-08-04 — xem §"Approved taxonomy decision (v0.8)".

**Decision criteria evaluation (task-required, tổng hợp — GIỮ LẠI làm input cho quyết định tương lai khi resume, KHÔNG phải cơ sở cho một kết luận A/B đã chốt tại v0.3):**

```text
Semantic/transactional cohesion:    Trung bình cho cả hai — KHÔNG còn là lý do quyết định một
                                     mình (sửa từ v0.1).
Authority/source-of-truth
  correctness:                      Alternative A/B đều giữ đúng authority (Decision Authority
                                     Service trong B vẫn là authoritative owner duy nhất);
                                     Alternative C vi phạm (evaluation authority rò rỉ sang
                                     Plugin) — loại C, không phải cơ sở phân biệt A/B.
Deterministic replay:               Cả hai khả thi nếu thiết kế đúng (sửa từ v0.1 — không còn
                                     "B không khả thi").
Decision-time visibility:           Không đổi giữa A/B — vẫn tuân Chapter 9 §9.5.
Strategy Plugin isolation:          Alternative A/B đều giữ Plugin Host tách biệt authority;
                                     Alternative C vi phạm trực tiếp — loại ngay.
Failure boundary:                   A: một domain, đơn giản. B: cần recoverable-append-gap
                                     design (có precedent risk.md) — khả thi, không phải rủi ro
                                     cấm split.
Testability:                        ỦNG HỘ B thật sự (sửa từ v0.1 — trước đây bị đánh giá thấp).
Explainability:                     Nhẹ nghiêng A (một điểm trace) — không quyết định.
Dependency/operational complexity:   ỦNG HỘ A — B thêm node/edge/lifecycle thật.
Risk of god-module growth:           HAI HƯỚNG — A: hybrid có kiểm soát; B: rủi ro chồng lấn
                                     compute_engine mới với strategy-plugin-host — ĐÂY LÀ LÝ DO
                                     KHIẾN CẢ A LẪN B chưa đủ bằng chứng chốt tại v0.3 (sửa từ
                                     v0.2 — không còn coi đây là "lý do quyết định cho A", mà là
                                     lý do chính khiến điều kiện 1 NOT ESTABLISHED cho cả hai
                                     hướng).
Future Package 1.3-C
  elaboration:                       CHÍNH package này phải sinh ra evidence còn thiếu (§"Required
                                     deferral trigger" mục 1-2) — quyết định A hay B resume SAU
                                     khi 1.3-C's bounded exploratory work (nếu Product Owner
                                     authorize) làm rõ ranh giới Plugin/Evaluation, không phải
                                     trước.
```

**Independent reviews / Concerns / Risks noted:**

| Reviewer identity | Role at review boundary | Concern | Risk | Recommendation |
|---|---|---|---|---|
| ChatGPT | AI Technical Architect | v0.1's condition 1 argument relied solely on authoritative-append atomicity, ignoring Chapter 8's Append-and-Revalidate precedent | Major (`ADR016-A-MAJ-01`) | Re-evaluate condition 1 across the full fourteen-criteria set, not atomicity alone |
| Independent Review B | AI Technical Architect | v0.2 inferred "boundary not yet defined" as evidence of inseparability — invalid inference from absence of evidence | Major (`ADR016-A-MAJ-02`) | Correct disposition to DEFERRED; require positive evidence for either taxonomy before selecting |
| Independent Review B | AI Technical Architect | v0.3's Alternative A block retained a current-tense claim ("satisfies all four Chapter 7 §7.1 conditions") contradicting the DEFERRED disposition just established | Major (`ADR016-B-MAJ-01`, v0.4 closure) | Condition the claim explicitly on a future authoritative boundary + Approved ADR |
| Independent Review B | AI Technical Architect | v0.5's Mechanism B was declared available but later trigger/consequence wording hardcoded ADR-016 itself as the only executable vehicle | Major (`ADR016-B-MAJ-01`, v0.6 closure) | Make Trigger 1/5 wording and all governance-path sections mechanism-neutral; define Path B1/B2 explicitly |
| Independent Review B | AI Technical Architect | v0.6's Path B2 conflated the authoritative responsibility definition (Trigger 1) with the separate ADR-015 amendment permission | Major (`ADR016-A-MAJ-03`, v0.7 closure) | Split Path B2 into five steps distinguishing Trigger 1/5 satisfaction from the ADR-015 amendment step |

**Final round (v0.7 → v0.8):** tất cả năm finding trên đã RESOLVED qua correction chain (v0.2/v0.3/v0.4/v0.6/v0.7). **Ghi nhận minh bạch:** không có một vòng Review A CLEAN + Independent Review B CLEAN riêng biệt, mới, xác nhận lại toàn văn v0.7 (sau khi đóng `ADR016-A-MAJ-03`) trước khi Product Owner approval này tới — Product Owner đã trực tiếp exercise thẩm quyền Decision Workflow (Chapter 0 §3) trên v0.7 đã fully-corrected, dựa trên correction-chain evidence tích lũy qua toàn bộ lịch sử tài liệu (khác với ADR-015's approval, vốn có một vòng review cuối tường minh trên v0.2 trước khi Approved). Tối thiểu hai reviewer identity khác nhau (ChatGPT, Independent Review B) đã tham gia xuyên suốt lịch sử tài liệu, đúng Chapter 11 §11.5.

**Scale check:**

```yaml
scale_check:
  current_scale: "0 module implementation tồn tại (Phase 1 architecture-only); Decision fact walking-skeleton scale kế thừa từ decision.md (1 Account, vài Strategy Instance hiện tại)"
  expected_scale:
    strategy: 50
    plugin: 500
  decision_still_valid: "YES — sửa v0.8, xem reason"
  reason: >
    Sửa (v0.8): Candidate B (split) nay Approved. Ở scale hiện tại (0 module implementation,
    walking-skeleton, 1 Account/vài Strategy Instance) — chi phí operational/dependency/replay
    complexity thêm của Candidate B (governance overhead thật, xem §"Decision criteria
    evaluation") không phải blocker; ở expected_scale (50 strategy/500 plugin), lợi ích
    least-privilege/testability/security-isolation của split (đúng đánh giá "ỦNG HỘ B THẬT SỰ"
    tại §"Chapter 7 condition 1 re-evaluation") càng có giá trị khi throughput tăng. Decision
    được coi valid tại cả hai scale — KHÔNG PHẢI vì scale tự nó quyết định (chỉ là input phụ
    trợ, đúng nguyên tắc đã pin trước approval), mà vì Product Owner đã cân nhắc evidence đầy
    đủ (Chapter 7 §7.1, split validation, authority guarantees) trước khi approve.
```

**Consequences (v0.8 — Approved, Candidate B/split selected under Mechanism A, 2026-08-04):**

```text
Ngay bây giờ (v0.8, Approved, Candidate B selected):
  ADR-016 APPROVE Candidate B (split) — xem §"Approved taxonomy decision (v0.8)". Trigger 1-5
  đều SATISFIED. ADR-015 vẫn LÀ controlling authority cho 21/22 module + toàn bộ dependency
  graph NGOÀI phần decision-engine đã amend (Mechanism A, xem §"Approved ADR-015 amendment
  scope") — ADR-015.md byte-identical, KHÔNG bị sửa. Package 1.1 decision-engine hybrid entry
  (module-registry.yaml, hiện tại) REJECTED bởi quyết định này NHƯNG artifact CHƯA được sửa —
  vẫn byte-identical cho tới khi correction transaction riêng biệt thực hiện. Package 1.1 vẫn
  Draft/candidate/blocked, KHÔNG thể Consolidated Stable.

Một Approved ADR-level amendment/supersession của ADR-015 (cho đúng phần module
inventory/dependency graph bị ảnh hưởng) ĐÃ tồn tại — chính ADR-016 v0.8 (Mechanism A) mang
hiệu lực đó. Đây LÀ một thay đổi quyết định đã Approved, KHÔNG PHẢI một "bounded follow-up
correction" (đóng pattern lỗi tương tự `P13C-A-MAJ-01`). SAU governance effect này (đã Approved
tại v0.8), Package 1.1 candidate ĐÒI HỎI một correction transaction riêng biệt
(module-registry.yaml/system-decomposition.md) + Review A/B + Package 1.1 consolidation
decision (xem "Required sequence after any future approval", bước 3-5) — CHƯA thực hiện tại
transaction v0.8 này. ADR-016 v0.8 tự nó KHÔNG sửa Package 1.1 artifact — đúng ranh giới "Do
not modify Package 1.1 artifacts" đã pin cho transaction approval này.

Không author tại ADR này (ranh giới bắt buộc, không đổi bởi approval):
  Decision algorithm, field-level Decision schema, API schema, database schema, runtime
  deployment topology, programming framework, source code, Package 1.3-C implementation —
  tất cả defer tới đúng "Required sequence after any future approval" bước 3-5.
```

**Final governance statement (v0.8):** Product Owner đã approve Candidate B (split) dựa trên đầy đủ evidence đã review — mọi finding trong correction chain (`ADR016-A-MAJ-01`/`ADR016-A-MAJ-02`/`ADR016-B-MAJ-01`/`ADR016-A-MAJ-03`) đã RESOLVED. Approval này KHÔNG tự động resolve governance/Domain gap còn tồn tại (dưới) — những gap đó CẦN resolve TRƯỚC KHI Candidate B trở thành implementation-ready, dù taxonomy đã Approved.

**Accepted risks (v0.8, Product Owner, 2026-08-04):**

```text
1. Redundancy/boundary risk với strategy-plugin-host: Decision Evaluation Engine (compute_engine,
   sinh non-authoritative candidate signal) có trách nhiệm gần giống strategy-plugin-host đã tồn
   tại — ranh giới CHÍNH XÁC chưa được authoritative resolve, đòi hỏi author Decision algorithm
   (ngoài phạm vi ADR này) để phân biệt dứt khoát. CHẤP NHẬN — Package 1.3-C architecture design
   (Consolidated Stable transaction tương lai) phải resolve ranh giới này trước implementation.

2. Evaluation-proposal artifact governance/Domain gap: loại artifact MỚI (evaluation proposal,
   identity/version/retention policy) chưa có Domain Contract. CHẤP NHẬN — cần Domain Contract
   correction (decision.md hoặc contract mới) trước implementation, KHÔNG tự động authorize tại
   ADR này.

3. Authority-level proposal rejection thiếu attempt_outcome mapping: decision.md's attempt_outcome
   enum hiện tại (DECIDED/INELIGIBLE/INPUT_UNAVAILABLE/FAILED_BEFORE_EVALUATION) chưa có giá trị
   cho "proposal hợp lệ nhưng bị Decision Authority Service reject (stale/duplicate/invalid)".
   CHẤP NHẬN — cần Domain Contract correction trước implementation; KHÔNG invent giá trị mới tại
   ADR này (cấm tường minh).

4. Operational/dependency/replay complexity cao hơn Candidate A: thêm một module, một node/edge,
   hai điểm idempotency/replay-pin thay vì một. CHẤP NHẬN — trade-off có chủ đích cho least-
   privilege/testability/security-isolation tốt hơn (xem §"Chapter 7 condition 1 re-evaluation").
```

Bốn residual risk trên đều CHẤP NHẬN bởi Product Owner tại approval transaction này, có điều kiện theo đúng "Required sequence after any future approval" — không risk/mitigation nào đổi bởi việc chấp nhận (chỉ ghi nhận, không tự động resolve).
