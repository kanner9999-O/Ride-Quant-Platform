---
id: ADR-041
title: Canonical Exact-Version Resolution for Input Contracts and Stream Registry
version: "0.1"
status: Draft
owner: Product Owner
reviewers: []
approved_by: null
approved_at: null
created_at: "2026-09-09"
last_review: null
next_review: null
depends_on: [ADR-040]
addresses: []
resolves: []
supersedes: [ADR-040]
---

# ADR-041: Canonical Exact-Version Resolution for Input Contracts and Stream Registry

> **Bounded Review A correction**, addressed/remediated pending re-review (not self-closed by the correction executor — closure is a Review A re-review determination, never asserted here): `P3-ADR041-A-MAJ-01` — this Draft concluded its own Decision requires a narrow amendment/supersession of Approved `ADR-040`, but deferred that semantic effect to a later, separate governed transaction while declaring `supersedes: []` — leaving two conflicting Approved authorities (this ADR's own Decision vs. `ADR-040`'s existing text) if `ADR-041` were approved as written, with no ADR actually carrying the correction. Corrected: `depends_on: [ADR-040]`/`supersedes: [ADR-040]` now declared directly in frontmatter, and a new §"`ADR-040` amendment scope" section states, self-contained in this ADR, exactly what narrows at `ADR-041`'s own future approval boundary — the same `depends_on`/`supersedes` + dedicated-scope-section pattern already precedented by `ADR-016`'s Mechanism-A amendment of `ADR-015`. The separate post-approval "narrow cross-reference transaction" Consequences item is removed; no ADR-040 file edit occurs now or later — `ADR-040.md` stays byte-identical/immutable, exactly as `ADR-015.md` did under `ADR-016`. `P3-ADR041-A-MAJ-02` — Option 2 permitted the current/active file to remain freely editable without ever stating that in-place edits sever its usability as authoritative evidence for an already-referenced version. Corrected: a new "Authoritative-use gate" paragraph fixes the minimum invariant — a version snapshot must exist before that version may be referenced by any authoritative event/cursor; the current content used to author that reference must match the snapshot's own content identity; any semantic modification to the current/active file makes it non-referenceable under the previous version identifier; a new version snapshot must be Published before modified content can be used authoritatively again; historical resolution always uses the immutable snapshot, never the current/active file, with no fallback; and Stream Registry activation/version transitions must continue to satisfy Chapter 8 §8.3.5's own lifecycle-boundary semantics unchanged. **No change to:** Option 2's own selection, the two canonical version-snapshot paths, the reused `ADR-039` grammar/fail-closed discipline, the Bootstrap conclusion, `ADR-041`'s own `version: "0.1"`/`status: Draft`, `ADR-037`/`ADR-038`/`ADR-039`/`ADR-040` (unchanged, unedited, immutable), Locked Constitution, or any Feature EVID/QG/gate/LIVE state. No migration, no implementation, no Review B, no Product Owner approval.
>
> `P3-ADR041-A-MAJ-01: REMEDIATED — PENDING BOUNDED REVIEW A RE-REVIEW`
> `P3-ADR041-A-MAJ-02: REMEDIATED — PENDING BOUNDED REVIEW A RE-REVIEW`

**Context:** [Chapter 8](../constitution/08-event-model.md) §8.5.1 fixes the canonical Replay Cursor as pinning `input_contract_ref {contract_id, contract_version}` and `stream_registry_version` — exact, immutable historical identities. §8.1.1 requires every Referenced Authoritative Artifact (Input Contract and Stream Registry named explicitly, §8.3.1/§8.3.4) to be versioned (rule 1: "mọi thay đổi tạo version mới, không sửa tại chỗ"), immutable once referenced (rule 2), and persistently resolvable through the committed horizon (rule 4) — but fixes only the *requirement*, not the *mechanism* for either artifact family. `ADR-039` (Approved) already resolved this exact problem for Event Contracts with a per-version-file direct-path resolver; it does not cover Input Contracts or Stream Registry, and this ADR does not redesign or revisit it.

**Ground truth, independently confirmed at this boundary:** Input Contract artifacts (`docs/architecture/input-contracts/*.yaml`) and the Stream Registry (`docs/architecture/stream-registry.yaml`) are each realized as a **single file, edited in place**, with `contract_version`/`registry_version` as an internal scalar field, not a per-version filename. Direct `git log` inspection of `feature-swing-distance-input.yaml` shows its own document `version:` field advancing `"0.1"` → `"0.4"` across four commits (real `frontier_policy` mechanism content changes each time) while `input_contract_ref.contract_version: v1` stayed the same literal value throughout — the artifact's own governed identity was never bumped despite genuine content change, a direct violation of §8.1.1 rules 1/2 already latent in the repository today. `stream-registry.yaml` has not yet exhibited this drift empirically (two commits: Draft authoring + a mechanically-identical Approval edit), but no artifact establishes a mechanism preventing or governing it structurally once a genuine `registry_version` transition is ever needed (Chapter 8 §8.3.5 fixes the activation-boundary *event* model, never the file-level resolution of a past `registry_version`'s own exact bytes). `authority_resolver.py`'s existing profile-based resolver (`ADR-037`'s own explicitly-cited mechanism) resolves whichever content currently exists at these fixed paths — never the exact historical bytes a past `computation_cursor` may have pinned. No current artifact — Locked Chapter 8, Approved `ADR-037`, Approved `ADR-040`, or the Input Contract/Stream Registry files themselves — supplies a deterministic mapping from an exact historical `{contract_id, contract_version}`/`registry_version` identity to content guaranteed to still be that exact historical content.

**Scope classification — `ADR Required`.** [Chapter 0 §4b](../constitution/00-governance.md)'s disjunctive triggers are satisfied independently of each other: this is the same class of decision `ADR-039` already was for Event Contracts (a canonical resolution/versioning mechanism for a named Referenced Authoritative Artifact family) — hard to reverse (a chosen per-version scheme constrains every future historical reference; changing it later requires migrating every prior pin) and platform-wide in principle (any current or future consumer of Input Contract/Stream Registry authority, not merely `feature-engine`). `G-ADR-004`'s inflation check (below) confirms no existing alignment already resolves this. `G-ADR-003`: this ADR is not authored merely because a prior transaction (Feature's own ADR-037 Replay-preparation implementation) left work open — the gap is derived directly from Chapter 8 §8.1.1 rules 1/2/4 applied to an artifact family `ADR-039` never covered, exactly parallel to how `ADR-039` itself was derived independently from Chapter 8, not from `ADR-037`/`038`'s own gaps. `ADR-037` remains the exposing use-case only, unrelated by `depends_on`. `depends_on: [ADR-040]`/`supersedes: [ADR-040]` are declared instead for a different, narrower reason (fixes `P3-ADR041-A-MAJ-01`): this ADR's own Decision requires a scoped amendment of `ADR-040`'s existing Input-Contract/Stream-Registry characterization (see §"`ADR-040` amendment scope" below) — a genuine ADR-to-ADR governance relationship, the same class already precedented by `ADR-016`'s `depends_on`/`supersedes: [ADR-015]`, not evidence that this decision's own content is derived FROM `ADR-040`.

**ADR inflation/scope check (`G-ADR-004`):**

```text
1. Existing authority/alignment resolves this without a new decision? NO --
   ADR-039's per-version-file mechanism is explicitly scoped to
   docs/architecture/event-contracts/** only (its own Decision/Boundaries);
   nothing extends it to Input Contracts/Stream Registry. ADR-040 only
   characterizes Input Contract/Stream Registry retention against their
   CURRENT single-path resolver -- it does not decide exact-historical-
   version resolution, the gap this ADR fixes.
2. Genuine architecture decision, not cheaply reversible? YES -- same
   class Chapter 0 §4b already treats as ADR-required (see above).
3. Chapter 0 §4b trigger fires independently? YES, disjunctively:
   platform-wide in principle AND hard-to-reverse.
4. Authored merely to "complete" a prior transaction (G-ADR-003)? NO --
   derived directly from Chapter 8 §8.1.1 rules 1/2/4, Locked before
   ADR-037/ADR-040 existed; those are the exposing use-case only.
Result: ADR REQUIRED confirmed independently.
```

**Decision question:** what canonical version lifecycle + exact-resolution model keeps every referenced Input Contract and Stream Registry version deterministically resolvable at current repository authority throughout the committed horizon?

**Decision (Option 2 — current/active path preserved, plus immutable per-version snapshot artifacts):**

- **Current/active path unchanged, still governs fresh (non-replay) resolution.** `docs/architecture/input-contracts/*.yaml` and `docs/architecture/stream-registry.yaml` remain exactly as Chapter 8 §8.3.1 already locks (Stream Registry's own path is Locked Constitution text — this ADR does not touch or reinterpret it) and as `ADR-040` already characterizes them — freely editable in place, resolved by `authority_resolver.py`'s existing mechanism for callers that need "whichever content is current." No change to this path, its content, or its own governance.
- **New: an immutable per-version snapshot artifact, captured at Publish time, for exact-historical resolution.** When a `contract_version`/`registry_version` is formally pinned for real authoritative reference (a fact's `computation_cursor` may legitimately name it), its exact bytes are captured into a dedicated, frozen file at:
  - Input Contract: `docs/architecture/input-contract-versions/<contract_id>/<contract_version>.yaml`
  - Stream Registry: `docs/architecture/stream-registry-versions/<registry_version>.yaml` (no `contract_id` segment — Chapter 8 §8.3.1 already fixes Stream Registry as a single, unnamed lineage, "Authoritative source duy nhất")
  Resolution is `ADR-039`'s own canonical grammar, reused by direct citation, not redefined: pure literal path substitution, `v<major>.<minor>`, no leading zeros except literal `0`, no aliasing/normalization/case-folding, no path separators/traversal in either token, malformed input fails closed before any path is constructed or any filesystem lookup occurs. No alias, registry/service, or git-history-search fallback exists (`ADR-040`'s own already-Approved prohibition, reused by citation, not re-litigated).
- **Historical/replay resolution always uses the version-snapshot path, never the current/active file.** Any consumer resolving a fact's own `computation_cursor.input_contract_ref`/`.stream_registry_version` (e.g. `feature-engine`'s Replay preparation) must resolve at the version-snapshot path for that exact identity — resolving the current/active file instead is a resolution-mechanism defect this ADR's own Decision does not authorize.
- **Immutable-after-reference; identifier non-reuse.** Once a version snapshot exists, it is frozen byte-for-byte forever (Chapter 11 §11.3 discipline, already applied identically to ADRs and Event Contract version-artifacts, reused by citation) — no in-place edits, no reassigning a `contract_version`/`registry_version` identifier to different content ever again. A content change always mints a **new** version snapshot; the current/active file may still be edited freely in between Publish events, but a *Published* snapshot itself never is.
- **Human version identifier vs. immutable content identity — same split `ADR-039` already fixed, reused by citation.** `contract_version`/`registry_version` is an ordering/legibility identifier only; the version-snapshot file's own git blob hash is its separate, verifiable content identity (§8.1.1 rule 5). Neither substitutes for the other.
- **Current/active vs. historical is a single, uniform distinction, not per-artifact-type variance.** "Current" always means the mutable, in-place file at its already-established path; "historical" always means an immutable snapshot at its own canonical version path — the same two-tier model applies identically to both Input Contracts and Stream Registry, closing the whole artifact-family problem once, not per-instance.
- **Fail-closed behavior — same principle `ADR-039`/`ADR-040` already establish, reused by citation.** A missing, malformed, or mismatched version snapshot; a malformed `contract_version`/`registry_version` token; or a content-identity mismatch on recomputation is a hard resolution failure — no nearest-version substitution, no degraded continuation, no silent fallback to the current/active file.
- **Relationship to `ADR-040`:** version-snapshot artifacts are new Class G members under `ADR-040`'s already-Approved retention model (git-repository-resident; within-horizon commitment = canonical path stays live at `HEAD`; git history is content-identity/audit evidence only, never a fallback resolver) — this ADR adds members to that already-established treatment, exactly as `ADR-039`'s own version-artifacts already did; it does not modify `ADR-040`'s text or its past-horizon archival deferral. The current/active files' own existing lifecycle (Draft, in-place-editable) is unaffected and unrevisited.
- **Bootstrap: existing bare `v1` identities do not carry over.** The Ground-truth finding above shows `input_contract_ref.contract_version: v1` has already denoted at least four distinct byte-states over time — it was never a single, stable, resolvable historical identity, so there is nothing genuine to preserve continuity with. No real authoritative (persisted, Live) fact has ever referenced any of those byte-states (`feature-engine` has never run outside tests). A **separate, future, governed bootstrap transaction** must capture each artifact's then-current content as a fresh **first** version snapshot under this ADR's grammar (`v1.0`, mirroring `ADR-039`'s own "first version starts at v1.0" rule, reused by citation) — this ADR decides that such a transaction is required and what it must produce; it does not itself perform that capture (no migration in this transaction, per explicit instruction).
- **Authoritative-use gate (fixes `P3-ADR041-A-MAJ-02`) — the current/active file's own editability does not, by itself, make its content authoritatively usable under an already-referenced version.** The minimum invariant this Decision fixes, for both artifact families uniformly:
  - Before any `contract_version`/`registry_version` may be named by an authoritative event's/cursor's own reference, its immutable version snapshot MUST already exist at its canonical path — an identifier with no corresponding snapshot is never a valid authoritative reference target.
  - The current/active content actually used to author that authoritative reference MUST match the version snapshot's own content identity exactly (§8.1.1 rule 5) — a reference minted against current content that was never captured into a matching snapshot is malformed.
  - Any semantic modification to the current/active file, after a version's own snapshot has been captured, makes that current/active file **non-referenceable** under the now-superseded version identifier — the current/active file's continued in-place editability (this Decision's own first bullet) is a drafting/fresh-work convenience, never a license to silently redefine an already-Published version's own meaning.
  - Modified current/active content may only become authoritatively usable again once a **new** version snapshot is Published for it, under a new `contract_version`/`registry_version` — identifier non-reuse (above) applies without exception; there is no "patch the same version in place" path.
  - Historical/replay resolution always uses the exact immutable snapshot for the identity named — **never** a fallback to whatever the current/active file happens to contain, even when the two are believed to still agree; agreement is proven by content-identity comparison (§8.1.1 rule 5), never assumed.
  - Stream Registry activation/version transitions remain governed entirely by Chapter 8 §8.3.5's own lifecycle-boundary model (the canonical Lifecycle Stream, `activation_boundary`, Genesis Registry exception, protected-stream carve-outs) — this Decision adds an exact-version-snapshot resolution layer underneath that model, it does not redefine, bypass, or gate any part of §8.3.5's own activation-boundary semantics.

**Alternatives considered:**

1. **(1) Immutable per-version canonical artifacts only, no current/active file (à la `ADR-039`, applied without change).** Rejected for this artifact family specifically: Chapter 8 §8.3.1 already Locks Stream Registry's own canonical path as the single fixed `/docs/architecture/stream-registry.yaml` — replacing it outright with a per-version-only scheme would directly contradict that Locked text, forcing a Constitution amendment this ADR is not authorized to make. Applying Option 1 to Input Contracts alone (no Locked path conflict there) while using Option 2 for Stream Registry would fragment one artifact-family problem into two different mechanisms, violating this ADR's own anti-proliferation mandate to close the family once.
2. **(2) Current/active path plus immutable historical version artifacts (chosen).** Satisfies §8.1.1 rules 1/2/4 for exact historical resolution without touching or reinterpreting Stream Registry's Locked current-path text, and without changing anything about `ADR-040`'s own already-Approved current-path characterization — purely additive. Reuses `ADR-039`'s own reviewed grammar/fail-closed discipline rather than inventing a new one.
3. **(3) Version/content-index based resolution (a central manifest mapping `{contract_id, contract_version}` → content hash/path).** Rejected — introduces a genuinely new authority surface (the index itself needs its own versioning/immutability/integrity guarantees, an unresolved question this option does not answer for free) for no benefit Option 2 does not already provide directly; more mechanism for an equivalent guarantee.
4. **(4) Git-history lookup.** Rejected outright — `ADR-040` (Approved) already establishes git commit/blob history as content-identity/audit evidence only, never a fallback resolver; adopting it here would directly contradict that already-Approved, already-Locked-in-spirit principle rather than merely be superseded by a better option.

No materially better option was identified beyond these four; Option 2 was chosen specifically because it is the only one requiring zero conflict with already-Locked/Approved authority while still fully satisfying §8.1.1's exact-resolution requirement for both artifact types uniformly.

**Authority compatibility:** `REQUIRES_FOLLOW_ON_SUPERSESSION/AMENDMENT` — narrow scope, not Constitution-level, and self-contained in this ADR (fixes `P3-ADR041-A-MAJ-01`: no separate future transaction is required to carry the effect). `ADR-040`'s own existing text characterizes Input Contracts'/Stream Registry's single current-path resolver as already satisfying §8.1.1's retention/resolvability requirement in full; this ADR's own Ground-truth finding shows that characterization conflates *current-active* resolution with *exact-historical-version* resolution, which are now distinct under this Decision. `ADR-040`'s file itself is never modified — not now, not later (Chapter 11 §11.3 forbids editing an Approved ADR; `ADR-040.md` stays byte-identical/immutable exactly as `ADR-015.md` did under `ADR-016`'s own Mechanism-A precedent). Instead, `ADR-041` itself — via `depends_on: [ADR-040]`/`supersedes: [ADR-040]` (frontmatter) plus the dedicated scope section immediately below — carries the exact, scoped narrowing at its own future Product Owner approval boundary; no ADR-042 or any other follow-on ADR is needed to make the correction effective. No conflict with Locked Chapter 8 was identified: Stream Registry's own current-path text stays untouched and unreinterpreted by this Decision.

### `ADR-040` amendment scope (scoped — takes effect at this ADR's own approval boundary, not before)

While `ADR-041` remains `Draft`, `ADR-040` is unaffected and fully controlling, exactly as it is today. **At the boundary this ADR is itself Approved** (never before), `ADR-041` amends `ADR-040`'s controlling effect — **scoped, not total**, the same discipline `ADR-016` v0.8 already applied to `ADR-015`:

```text
Amended (becomes controlling via ADR-041, at ADR-041's own approval only):
  - ADR-040's Class G characterization of Input Contracts'/Stream Registry's
    retention/resolvability treatment -- specifically the claim that their
    single current-path resolver already satisfies §8.1.1 rule 4 in full.
    ADR-041 narrows this to: the current-path resolver satisfies §8.1.1 for
    CURRENT/fresh (non-replay) resolution only; EXACT-HISTORICAL-VERSION
    resolution additionally requires the version-snapshot mechanism this
    ADR establishes (see Decision above).

NOT amended (ADR-040 remains controlling, unchanged, no narrowing applies):
  - ADR-040's own two-tier model (retention-policy scope vs. storage-
    realization classes), its Class G/Class R definitions, its treatment of
    Event Contract version-artifacts (already exact-version-resolvable per
    ADR-039, untouched by this ADR), its Compatibility Result/Policy
    unclassified-scope treatment, its past-horizon archival deferral, and
    every other retention/archive semantic ADR-040 fixes.
  - ADR-040.md ITSELF -- byte-identical, not edited, still Approved/
    immutable (Chapter 11 §11.3). This is a GOVERNANCE relationship
    (frontmatter depends_on/supersedes, same pattern as ADR-014/ADR-003/
    ADR-016/ADR-015), never a text edit into ADR-040's own file.
```

This ADR does not itself modify `docs/adr/ADR-040.md`, does not change `ADR-040`'s own `version`/`status`/`approved_by`, and asserts no effect at all while `status: Draft` — the amendment is declared now (frontmatter + this section) so that, if and when `ADR-041` is Approved, the correction is already fully carried without requiring any additional governed transaction.

**Independent reviews / Concerns / Risks noted:**

| Reviewer principal | Role at review boundary | Execution ID | Review boundary | Independence mode | Isolation attestation | Concern | Risk | Recommendation |
|---|---|---|---|---|---|---|---|---|
| | AI Technical Architect | | | | | | | |
| | AI Technical Architect | | | | | | | |

> Trước approval phải có tối thiểu hai independent review execution đủ điều kiện, đúng Chapter 0 §3 / Chapter 11 §11.5 / [ADR-031](./ADR-031.md) §5. No review has been performed against this Draft yet — this ADR is not yet ready for Product Owner decision.

**Scale check:**

```yaml
scale_check:
  current_scale: "Phase 3 — Core Backend; zero version-snapshot artifacts exist today for either family; three Input Contract instances and one Stream Registry lineage currently resolve via their single current/active path only"
  expected_scale:
    strategy: 0
    exchange: 0
    plugin: 0
  decision_still_valid: true
  reason: >
    Cost scales with the number of distinct {contract_id, contract_version}/
    registry_version pairs ever Published platform-wide, never with event
    volume or strategy/exchange/plugin count -- the identical
    volume-independence property ADR-039 already established for Event
    Contract version-artifacts. Growth in future Input Contract instances
    or Stream Registry transitions increases how many version-snapshot
    files eventually exist, never this resolution mechanism's own shape,
    cost, or validity.
  reason_if_no: ""
```

**Consequences:** This ADR fixes the canonical exact-version resolution architecture for Input Contracts and Stream Registry only, including the scoped `ADR-040` amendment it itself carries at its own future approval boundary (see §"`ADR-040` amendment scope" above — no separate follow-on ADR/cross-reference transaction is required for that narrowing). It does not itself create any version-snapshot artifact, does not migrate any existing Input Contract or Stream Registry file, does not modify Chapter 8, `ADR-037`/`ADR-038`/`ADR-039`/`ADR-040` (`ADR-040.md` itself is never edited, at this boundary or any future one), or any Published Event Contract, and does not modify production source/tests/tooling — `output_contract_resolver.py`/`prepare_replay_evidence()` remain exactly as they are; the two open Review-A Major findings against them (`P3-FEATURE-EVID05B-IMPL-A-MAJ-01`/`-MAJ-02`) and `P3-FEATURE-QG-EVID-05(b)` are not closed by this ADR. Bounded, separate follow-on work remains, in order: (0) Review A/Independent Review B/Product Owner decision on this Draft; (1) if Approved, the governed bootstrap transaction that captures each artifact's current content as its first `v1.0` version snapshot (see Bootstrap above); (2) only then, `feature-engine`'s own `prepare_replay_evidence()` correction resolving `P3-FEATURE-EVID05B-IMPL-A-MAJ-02` against this newly-available authority, alongside `P3-FEATURE-EVID05B-IMPL-A-MAJ-01`'s independent-per-`contract_id`-version-lineage fix; (3) a bounded Review-A re-review closing both findings and, if warranted, `P3-FEATURE-QG-EVID-05(b)`. This ADR does not change Feature Chapter 13 QG evidence, Feature module approval, the Phase 3 Approval Gate, or `LIVE` authorization — all remain exactly as they stood before this transaction.

**Accepted risks:** Not yet applicable — this Draft has not yet been reviewed; no Product Owner decision has been made.
