---
id: ADR-045
title: "Delegated Technical Resolution — Exception-Based Product Owner Decision Model"
version: "0.3"
status: Approved
owner: Product Owner
reviewers: [ChatGPT]
approved_by: Product Owner
approved_at: "2026-09-23T14:02+07:00"
created_at: "2026-09-23"
last_review: "2026-09-23"
next_review: null
depends_on: [ADR-042]
addresses: []
resolves: []
supersedes: [ADR-042]
---

# ADR-045: Delegated Technical Resolution — Exception-Based Product Owner Decision Model

> **Bounded Review A correction**, addressed/remediated pending re-review (not
> self-closed by the correction executor — closure is Review A's own re-review
> determination, never asserted here). Review A (ChatGPT, `AI Technical
> Architect`) reviewed v0.1 (reviewed blobs: this file
> `e5e7357f16220777922b07bd4be29ef0cfe19007`; Chapter 0 v1.5 candidate
> `169821b2c9d6719956e86dafdc5fb1ca8ff07c37`; Global Execution Rules v0.7
> candidate `7bc43913391f3d7b827e8603dbd841525b5ec286`) and returned verdict
> `REVISION_REQUIRED — 0 Blocker / 2 Major / 1 Minor`, Risk `R2`.
>
> **`MAJOR-01`** — v0.1 declared this ADR "extends `ADR-042` — does not
> supersede it," but `ADR-042` (Approved) fixes a single target workflow
> (`Executor -> Review A -> Risk Classification -> Product Owner Decision ->
> Execution`) that this ADR intentionally changes for eligible R0/R1
> technical resolutions — two current Approved ADRs cannot coexist with
> contradictory routing semantics for the same decision class. Corrected:
> `supersedes: [ADR-042]` (frontmatter); "Decision — target principle" and a
> new "Authority compatibility — `ADR-042` supersession scope" section below
> make `ADR-045` the explicit successor, carrying forward every still-valid
> `ADR-042` semantic unchanged (Review A mandatory where review is required
> and independent; R0/R1/R2 definitions; R0/R1 default `NO CROSS-CHECK`; R2
> remains Product-Owner-only with an advisory, non-mandatory, non-veto,
> non-persisted optional cross-check; peer reviewers, no veto; historical
> decisions non-retroactive), with the intentional delta stated narrowly and
> explicitly. `ADR-042.md` is never modified — Chapter 11 §11.3 forbids
> editing an Approved ADR file; it stays byte-identical/immutable, exactly as
> `ADR-042` itself never modified `ADR-031.md`. The future activation bundle
> now explicitly includes marking `ADR-042` `Superseded` in MANIFEST with
> reverse `superseded_by: ADR-045` — not performed by this correction, only
> described as a future activation step.
>
> **`MAJOR-02`** — the Chapter 0 v1.5 candidate left the old unconditional
> single-route diagram live in §3 alongside a new Route A/Route B addendum
> below it, producing two apparently-competing normative routing
> descriptions once activated. Corrected: §3 now contains exactly ONE future
> controlling routing model (a single branching workflow: Review A where
> required -> Risk Classification -> routing, with PO-reserved/R2/
> `ADR_REQUIRED`/non-eligible decisions going to Product Owner Decision and
> R0/R1 decisions satisfying D1–D12 going to `DELEGATED TECHNICAL
> RESOLUTION`, each with its own distinct downstream step — Delegated
> Technical Resolution never produces an `Approved`/`Locked`/Phase Approved/
> Module Approved/LIVE state); the previous `ADR-042` single-route workflow
> is preserved as explicit historical text only, clearly labeled as no
> longer the live model once activated; the candidate explicitly states
> `v1.4` remains controlling until activation and the new branching model is
> the sole current §3 routing model thereafter.
>
> **`MINOR-01`** — `D10` and its escalation-rule counterpart protected only
> governing-artifact reservation, not a Product Owner's own direct call-in
> of a specific decision. Corrected: `D10` and the matching escalation rule
> now cover both — a governing authority explicitly reserving the decision
> class, OR the Product Owner explicitly reserving/calling in this specific
> decision — either fails delegation eligibility closed to Product Owner,
> and no AI may override an explicit Product Owner call-in. `G-DELEGATE-001`
> (Global Execution Rules v0.7 candidate) updated to match.
>
> `MAJOR-01: CLOSED — REVIEW A VALIDATED` (v0.2 re-review, boundary
> `bced025c6df3ca36f46313862fddac2c5c3dc8c1` — see below)
> `MAJOR-02: CLOSED — REVIEW A VALIDATED` (v0.2 re-review, same boundary)
> `MINOR-01: CLOSED — REVIEW A VALIDATED` (v0.2 re-review, same boundary)

> **Review A (round 2), CLEAN, then optional R2 independent cross-check —
> substantive defects found, accepted, consolidated into ONE correction
> (CORR-002).** Review A (ChatGPT, `AI Technical Architect`) reviewed v0.2
> (reviewed blob `033c2a95d372c8cb93ef1f3495699dc95e925384`, boundary
> `bced025c6df3ca36f46313862fddac2c5c3dc8c1`), confirmed round-1
> `MAJOR-01`/`MAJOR-02`/`MINOR-01` genuinely remediated (`CLOSED — REVIEW A
> VALIDATED`, recorded above — NOT reopened by this correction), and
> returned `CLEAN — 0 Blocker / 0 Major / 1 Minor`, Risk `R2`. At R2, Review
> A recommended the optional advisory cross-check; the Product Owner
> elected to use it. The independent cross-check returned `DEFECT FOUND —
> 0 Blocker / 2 Major / 5 Minor`. ChatGPT independently re-verified all
> findings against live repository authority and accepted `0 Blocker / 2
> Major / 5 Minor` — the prior Review-A provenance Minor is absorbed into
> `X-MAJ-02`. Effective correction input: `0 Blocker / 2 Major / 5 Minor`.
> All findings consolidated into ONE internally complete correction rather
> than sequential mini-corrections:
>
> - **`X-MAJ-01`** — Review A independence (existing `G-REV-003`) means
>   independent verification / non-inheritance; it does NOT require
>   `Review A principal != Executor/Author principal`. That separation was
>   previously supplied only by `ADR-031`'s retired Mode-A machinery; under
>   the old (superseded) workflow Product Owner remained the terminal
>   decider regardless, but under Delegated Technical Resolution, removing
>   the Product Owner as terminal decider creates a real self-resolution
>   loop unless principal separation is explicit. Corrected: `D8`
>   strengthened (numbering unchanged, D1-D12 preserved) to require, for a
>   Delegated Technical Resolution only, BOTH (1) independent verification
>   returning CLEAN — 0 Blocker / 0 Major, AND (2) Review A principal
>   identity distinct from every principal that authored or executed the
>   underlying semantic work being resolved; if that identity cannot be
>   reliably resolved, `D8` fails and the case routes to Product Owner
>   Decision. This is a narrow DTR-only safeguard — it does not restore
>   mandatory Review B, does not require two reviewers, does not change
>   ordinary ADR/Approval-Gate Review-A semantics; there is still exactly
>   one Review A. Vacated wording claiming existing Review-A rules already
>   prohibit self-review for DTR (they do not — only `G-REV-003`'s
>   independent-verification requirement existed). Mirrored exactly in
>   `G-DELEGATE-001` (Global Execution Rules v0.7 candidate). No
>   `team.yaml` change required — registered `ChatGPT`/`Claude` principals
>   are already distinct identities.
> - **`X-MAJ-02`** — `ADR-045` supersedes `ADR-042` but did not carry the
>   complete operative R0/R1/R2 definitions into its own text; live current
>   artifacts (Chapter 0, Chapter 11 §11.5/§11.9, the ADR template, and
>   lower-tier parenthetical references) still point to `ADR-042` as the
>   definition source, which becomes stale once `ADR-042` is `Superseded`.
>   Corrected: `ADR-045`'s own "Decision — risk-classified routing" section
>   now carries the FULL operative R0/R1/R2 definitions (mechanical/no
>   semantic risk; bounded semantic/normal implementation risk; high
>   semantic/architectural risk, including every `ADR-042` R2 trigger:
>   Platform Invariant/Event Schema changes, authority/source-of-truth
>   semantics, identity/lifecycle/replay/versioning/retention/activation
>   semantics, significant cross-module contract/dependency blast radius,
>   difficult/expensive-to-reverse migration, destructive/irreversible
>   state transition, conflicting authority/evidence, unresolved ambiguity,
>   materially low reviewer confidence, repeated semantic correction/
>   root-cause uncertainty, Product-Owner-requested second opinion) —
>   unchanged in substance, neither weakened nor broadened. `ADR-045`
>   becomes the current definition authority after activation. Chapter 0
>   v1.5, a new Chapter 11 v2.4 successor candidate, the ADR template, and
>   Global Execution Rules v0.7 all corrected to resolve future/
>   post-activation Risk Classification definition source to `ADR-045`
>   (pre-activation, `ADR-042` remains the source, unchanged) — see each
>   artifact's own correction below. The previous Review-A-round-2
>   provenance Minor (a stale claim that this v0.2 candidate's ground-truth
>   description of `G-ORCH` was accurate) is folded into this same
>   correction, absorbed here since both concern authority-source accuracy:
>   `docs/governance/execution-rules.md`'s `G-ORCH` is not merely
>   transaction/review mechanics — `G-ORCH-002`/`G-ORCH-003` already
>   contain explicit Product-Owner-reserved lifecycle authority
>   (`Approved`/`Consolidated Stable`/`Accepted` state transitions require
>   an explicit Product Owner decision); "Ground truth" corrected
>   accordingly.
> - **`X-MIN-01`** — the PO-reserved/non-delegable list omitted document
>   `Accepted`/`EFFECTIVE` acceptance transitions and package/artifact
>   `Consolidated Stable` transitions where governing authority reserves
>   them to Product Owner. Corrected: both explicitly added to "Approval —
>   reserved to Product Owner," the "Delegated Technical Resolution ...
>   NOT" list, Chapter 0's reserved-decision-class list, and
>   `G-DELEGATE-002`. `D10` remains the generic fail-closed backstop for
>   any decision class not itemized here.
> - **`X-MIN-02`** — the pending-work rule did not explicitly state that a
>   pre-activation Review A verdict cannot itself satisfy `D8` for a
>   post-activation Delegated Technical Resolution. Corrected: new explicit
>   statement — a pre-activation Review A verdict may remain valid
>   historical evidence, but does NOT by itself satisfy `D8`; `D8` for a
>   delegated closure must be performed/reconfirmed after activation,
>   against the exact subject/evidence being resolved, at the fresh
>   resolution boundary, by the required distinct principal. This means
>   `Candidate-005`'s existing `CLEAN` review is historical input only —
>   `Candidate-005` is NOT resolved by this correction.
> - **`X-MIN-03`** — MANIFEST's current-state rows for
>   `governance/execution-rules.md` (stale at `0.5`/`EFFECTIVE`, actual
>   controlling `0.6`/`EFFECTIVE`) and `governance/phases/phase-3-rules.md`
>   (stale at `0.2`/`CANDIDATE`, actual `0.3`/`EFFECTIVE`) were pre-existing
>   I-12 bookkeeping defects, independently fresh-verified this
>   transaction. Corrected: both rows mechanically reconciled to actual
>   frontmatter in this same transaction (deterministic bookkeeping folded
>   into an already-required transaction, `G-TXN-003`); Execution Rules row
>   also retains an accurate `v0.7 CANDIDATE` preview pointer.
>   `docs/governance/phases/phase-3-rules.md` itself verified byte-
>   unchanged — no semantic edit.
> - **`X-MIN-04`** — `ADR-045`'s Review-A provenance table was empty with
>   stale "not yet reviewed" wording. Corrected: table now records both
>   review rounds (round 1: boundary `fee5c582dc89efcd0eec8c537d56cd0
>   151eea673`, reviewed v0.1 blob `e5e7357f16220777922b07bd4be29ef0cfe
>   19007`, `REVISION_REQUIRED — 0 Blocker / 2 Major / 1 Minor`, ChatGPT,
>   `AI Technical Architect`, R2; round 2: boundary `bced025c6df3ca36f4631
>   3862fddac2c5c3dc8c1`, reviewed v0.2 blob `033c2a95d372c8cb93ef1f349569
>   9dc95e925384`, `CLEAN — 0 Blocker / 0 Major / 1 Minor`, R2, optional
>   Product-Owner-selected cross-check `DEFECT FOUND — 0 Blocker / 2 Major
>   / 5 Minor` — advisory only, not represented as an approval
>   prerequisite). History kept compact; the cross-check's full transcript
>   is not pasted here.
> - **`X-MIN-05`** — no generic recording rule existed for how a Delegated
>   Technical Resolution is represented in a legacy candidate schema
>   (e.g. a `product_owner_state` field), risking either a fabricated PO
>   action or an ambiguous record. Corrected: new "Delegated closure
>   recording — legacy schema mapping" subsection defines the generic
>   fields every delegated closure must persist
>   (`resolution_authority: DELEGATED_TECHNICAL_RESOLUTION`,
>   `delegated_resolution_state: RESOLVED`, plus the normal
>   `G-DELEGATE-002` audit fields) and the legacy-schema rule: never write
>   `APPROVED`, never fabricate PO action; either map the legacy PO-facing
>   field to an explicitly non-PO value (e.g.
>   `NOT_REQUIRED_DUE_TO_DELEGATED_TECHNICAL_RESOLUTION`) or use an
>   equivalent unambiguous mapping; if the existing schema cannot represent
>   the result without ambiguity, `D12` fails and the case cannot delegate
>   until a bounded schema alignment exists. Mirrored in `G-DELEGATE-002`.
>   `Candidate-005`'s own artifact is NOT edited by this correction — its
>   mapping applies only if/when it later receives an actual Delegated
>   Technical Resolution.
>
> `X-MAJ-01: CLOSED — REVIEW A VALIDATED` (final Review A, v0.3 boundary
> `e54159aa8355cfd1d3d309d43969007b645246ed` — see below)
> `X-MAJ-02: CLOSED — REVIEW A VALIDATED` (same boundary)
> `X-MIN-01: CLOSED — REVIEW A VALIDATED` (same boundary)
> `X-MIN-02: CLOSED — REVIEW A VALIDATED` (same boundary)
> `X-MIN-03: CLOSED — REVIEW A VALIDATED` (same boundary)
> `X-MIN-04: CLOSED — REVIEW A VALIDATED` (same boundary)
> `X-MIN-05: CLOSED — REVIEW A VALIDATED` (same boundary)

**Status: Approved (Product Owner, `2026-09-23T14:02+07:00`) — ACTIVATED
atomically together with Chapter 0 v1.5, Chapter 11 v2.4, and Global
Execution Rules v0.7 at reviewed semantic boundary
`e54159aa8355cfd1d3d309d43969007b645246ed`, via
`GOV-DELEGATION-MODEL-ADR-045-ACTIVATION-001`.** This ADR's delegation
model is now binding governance authority. It does not itself grant any
credit or resolve any pending candidate — `Candidate-005` remains
`167/170`, unresolved; its pre-activation `CLEAN` Review A does not, by
itself, satisfy `D8` for a post-activation Delegated Technical
Resolution (`X-MIN-02`) — a separate, subsequent, fresh post-activation
`D1`–`D12` determination is required before it can close under this
model. Per Chapter 0 §5.2 / Chapter 11 §11.3, this file is now immutable;
any future change requires a superseding ADR.

## Context

`ADR-042` (Approved) retired the mandatory-two-independent-review gate,
replacing it with `Executor -> Review A -> Risk Classification (R0/R1/R2)
-> Product Owner Decision -> Execution` — Review A remains the single
mandatory formal technical review; R2 recommends, never requires, an
optional advisory cross-check chosen by the Product Owner. That change
reduced review-gate ceremony but left every governed decision — regardless
of risk class — routed to an explicit Product Owner Decision as the final
step before execution.

Recent Feature Engine `P3-FEATURE-QG-EVID-03` governance traffic (the
`ADR-044`/Chapter 13 §13.8.1 correction chain; four sequential Condition-2
§4.1(b) reclassification candidates, `candidate-002` through `-005`, each
requiring its own Product Owner Decision to apply an *already-approved*
mechanism to a new bounded evidence cohort) demonstrates a recurring
pattern: a large share of Product Owner decision traffic is not a policy,
architecture, or product-direction judgment — it is confirming that an
already-governed, already-approved mechanism was correctly and boundedly
applied to a new, individually-verifiable technical case. Product Owner has
instructed reducing this routine traffic while preserving Product Owner
authority over genuinely important decisions: policy, product direction,
architecture/governance change, approval gates, and meaningful risk.

## Ground truth, independently confirmed at this boundary

`docs/constitution/00-governance.md` v1.4 (Locked) §3 currently routes
every decision through Review A -> Risk Classification -> Product Owner
Decision, with no exception for R0/R1 decisions applying already-approved
authority. §4b's ADR Scope Rule (unchanged by this decision) already
distinguishes `ADR Required`/`ADR Optional`/`ADR Not Required` classes but
says nothing about what happens to a non-ADR-Required decision's own
approval routing. `docs/governance/execution-rules.md` v0.6 (EFFECTIVE)
contains no delegation rule — every governed decision today reaches a
Product Owner Decision step regardless of risk class or repetition. Its
`G-ORCH` section is **not** merely transaction/review mechanics
(`X-MAJ-02` correction): `G-ORCH-002` ("KHÔNG auto-approval — mọi
Approved/Consolidated Stable/Accepted state transition đòi hỏi một
Product Owner decision tường minh") and `G-ORCH-003` ("KHÔNG
auto-consolidation") already contain explicit Product-Owner-reserved
lifecycle authority over `Approved`/`Consolidated Stable`/`Accepted`
state transitions — directly relevant to this ADR's own PO-reserved list
(see `X-MIN-01`). `docs/constitution/11-adr-process.md` §11.5/§11.9
currently point to `ADR-042` as the Risk Classification definition
source, which would go stale once `ADR-042` becomes `Superseded`
(`X-MAJ-02`) — Chapter 11 therefore requires an in-place successor
candidate (v2.4) as part of this ADR's own migration, scoped narrowly to
that definition-source pointer; ADR approval itself remains
Product-Owner-only, unaffected in substance. `docs/constitution/
12-approval-gates.md` correctly reserves Approval-Gate decisions to
Product Owner specifically, claims no blanket authority over every
technical governance outcome, and requires no amendment. `docs/
templates/adr-template.md` similarly points to `ADR-042` for Risk
Classification and requires a narrow before/after-activation bridge
reference, without making ADR approval itself delegable.
`docs/governance/phases/phase-3-rules.md` v0.3 (EFFECTIVE) §11 Gate-path
already defers its review-gate prerequisite to "đúng mô hình đang
controlling tại Chapter 0 §3/Chapter 11 §11.5, KHÔNG hard-code một con số
reviewer cụ thể" and ends with an explicit, unchanged `Product Owner
Phase 3 Approval Gate decision` step — fresh inspection found no live
normative statement there that contradicts a delegation model for
non-Approval-Gate decisions; it is therefore not amended by this ADR (see
"Scope / non-goals").

## Scope classification — `ADR Required`

Chapter 0 §4b lists "Governance/Approval process" changes as explicitly
`ADR Required` — this decision changes how a class of governed decisions is
routed to closure. This is exactly that category.

**ADR inflation/scope check (`G-ADR-004`):**

```text
1. Existing authority/alignment resolves this without a new decision? NO --
   Chapter 0 §3 and execution-rules.md currently route every decision,
   regardless of risk class, through an explicit Product Owner Decision;
   no existing risk-tiered delegation exception exists.
2. Genuine architecture/governance decision, not cheaply reversible? YES --
   this changes who may close a class of governed decisions platform-wide;
   a wrong eligibility boundary could silently under-govern a decision that
   should have reached Product Owner.
3. Chapter 0 §4b trigger fires independently? YES -- "Governance/Approval
   process" change is explicitly listed as ADR Required.
4. Authored merely to "complete" a prior transaction (G-ADR-003)? NO --
   directly requested by Product Owner decision direction as a standalone
   governance-efficiency change, not derived from a gap left by ADR-044's
   own chain (Candidate-005's pending state is illustrative context only,
   not the reason this ADR exists -- see "Candidate-005 worked example").
Result: ADR REQUIRED confirmed independently.
```

## Decision question

What governed routing lets Review A close a bounded, already-governed
technical decision directly — without weakening Product Owner authority
over policy, architecture, approval gates, or genuine risk?

## Decision — target principle

```text
Product Owner approves policy, product direction, architecture/governance
change, approval gates, and meaningful risk.

An eligible AI Technical Architect may resolve routine bounded
applications of already-approved policy when strict delegation
eligibility is satisfied.
```

`ADR-045` **supersedes** `ADR-042` (`MAJOR-01` correction). `ADR-042`
fixed a single target workflow — `Executor -> Review A -> Risk
Classification -> Product Owner Decision -> Execution`, with Risk
Classification always followed by a Product Owner Decision — and this ADR
intentionally changes that routing for a closed subset of decisions;
`ADR-045`, not `ADR-042`, is therefore the single current authority for
the combined model, as of this ADR's activation. This is not a rewrite of `ADR-042`'s
own Decision: every `ADR-042` semantic that remains valid is carried
forward unchanged (see "Authority compatibility — `ADR-042` supersession
scope" below) — only the step *after* Risk Classification changes, and
only for the closed subset of decisions satisfying `D1`–`D12`.

### Authority compatibility — `ADR-042` supersession scope

`ADR-042`'s entire operative content — Review A's mandatory/independent
status, the R0/R1/R2 definitions, R0/R1's `NO CROSS-CHECK` default, R2's
Product-Owner-only routing, the optional R2 cross-check's advisory/
non-mandatory/non-veto/non-persisted semantics, peer-reviewer no-veto,
and non-retroactivity — remains valid and is carried forward into
`ADR-045` **unchanged in substance**. The only intentional delta is
narrow and explicit:

```text
ADR-042 (superseded routing):
  ... -> Risk Classification -> Product Owner Decision -> Execution
  (unconditional — every decision reaches Product Owner)

ADR-045 (this ADR's routing, effective as of activation):
  ... -> Risk Classification -> routing:
    eligible R0/R1 + D1-D12 satisfied -> DELEGATED TECHNICAL RESOLUTION
    otherwise                          -> Product Owner Decision
```

`ADR-042`'s file is **never modified** — Chapter 11 §11.3 forbids editing
an Approved ADR; `ADR-042.md` stays byte-identical/immutable, exactly as
`ADR-042` itself never modified `ADR-031.md`. Its own definitions (Review
A independence, R0/R1/R2, optional cross-check semantics) are not
declared incorrect — they are the historical origin of the unchanged
concepts carried forward here. **`X-MAJ-02` correction:** unlike the
prior v0.2 candidate (which only summarized these definitions and left
live artifacts pointing back to `ADR-042` as the active source),
`ADR-045`'s own "Decision — R0/R1/R2 definitions" section below now
carries the FULL operative text forward, unchanged in substance — as of
activation, `ADR-045` is the current definition authority for R0/R1/R2
platform-wide, and `ADR-042` is no longer relied upon as a live
definition source anywhere in the repository (Chapter 0, Chapter 11, the
ADR template, and Global Execution Rules were each corrected accordingly
— see their own sections/artifacts). As of activation, `ADR-042`'s
current lifecycle state is `Superseded` (by `ADR-045`), recorded at
MANIFEST per I-12, mirroring the same relationship `ADR-042` itself
established with `ADR-031` — see "Activation" below for the completed
transition record.

### Approval — reserved to Product Owner, unchanged

At minimum, all of the following remain exclusively Product Owner
authority; no AI may perform them under this ADR:

```text
- ADR approval/rejection (every ADR, Chapter 11);
- Phase Approval Gate decisions (Chapter 12 §12.2);
- Module/Product approval gate decisions;
- product direction, scope, priority, and value tradeoffs;
- governance or approval-process changes (this ADR itself included);
- Platform Invariant / Event Schema / module-taxonomy/dependency changes;
- any decision requiring an ADR under Chapter 0 §4b;
- every R2 decision;
- explicit risk acceptance (Chapter 0 §3's "Chấp nhận rủi ro" requirement);
- LIVE authorization, if/when a future governing authority permits it;
- document `Accepted`/`EFFECTIVE` acceptance transitions where governing
  authority reserves them to Product Owner (`X-MIN-01`; `G-ORCH-002`);
- package/artifact `Consolidated Stable` transitions where governing
  authority reserves them to Product Owner (`X-MIN-01`; `G-ORCH-003`);
- any decision a governing artifact explicitly reserves to Product Owner;
- any decision the Product Owner explicitly calls in/reserves for
  themself (`D10`).
```

Delegation narrows routine operational decision traffic. It does not
transfer Product Owner ownership of the product, and does not create a
second approval authority.

### Delegated Technical Resolution — a distinct, narrower concept

A **Delegated Technical Resolution** is a technical governance outcome
reached under already-approved authority. It is explicitly **NOT**:

```text
- an ADR approval;
- a Phase/Module approval;
- product acceptance;
- Product Owner risk acceptance;
- a new architecture decision;
- a new governance rule;
- an Approved/Locked lifecycle transition;
- a document `Accepted`/`EFFECTIVE` acceptance transition (`X-MIN-01`);
- a package/artifact `Consolidated Stable` transition (`X-MIN-01`).
```

The exact terminology `DELEGATED TECHNICAL RESOLUTION` is used everywhere
this outcome is recorded. It is never called "AI approval," and no
artifact created under this model may write `approved_by: AI` or any
fabricated Product Owner decision text.

## Decision — closed delegation eligibility predicate (D1–D12, conjunctive)

A decision may use Delegated Technical Resolution **only when ALL of the
following hold** — no partial satisfaction, no "mostly satisfies":

```text
D1.  ADR Scope Rule = ADR_NOT_REQUIRED.

D2.  Risk Classification is R0 or R1. R2 is NEVER delegated.

D3.  An existing Approved/Locked/EFFECTIVE authority already defines the
     rule/mechanism being applied.

D4.  The transaction only applies that existing rule to a bounded,
     individually-verifiable technical/evidence case.

D5.  It creates no new product semantics, architecture semantics,
     governance semantics, contract semantics, invariant, schema, module
     dependency, or policy.

D6.  It does not alter product scope, priority, roadmap direction,
     user/business acceptance criteria, or a Product-Owner-owned value
     tradeoff.

D7.  It does not accept a new residual risk on behalf of Product Owner.

D8.  For a Delegated Technical Resolution specifically (`X-MAJ-01`
     correction), BOTH of the following hold — this is a narrow DTR-only
     safeguard: it does NOT restore mandatory Review B, does NOT require
     two reviewers, does NOT change ordinary ADR/Approval-Gate Review-A
     semantics, and there is still exactly one Review A:
     1. Review A independently verifies the exact subject/evidence and
        returns CLEAN: 0 Blocker / 0 Major. Non-blocking Minor findings
        may remain under existing Semantic Sufficiency rules
        (execution-rules.md).
     2. Review A principal identity is DISTINCT from every principal that
        authored or executed the underlying semantic work being
        resolved. If executor/author principal identity cannot be
        reliably resolved, `D8` FAILS -> Product Owner Decision.

D9.  The exact evidence/authority/boundary needed for the resolution is
     fresh-verified per G-VERIFY / P3-VERIFY where applicable.

D10. Neither of the following is true (`MINOR-01` correction — both are
     independent, either alone fails this condition):
     (a) a governing artifact explicitly reserves this decision class to
         Product Owner; or
     (b) the Product Owner has explicitly called in/reserved this
         specific decision. No AI may override an explicit Product Owner
         call-in.

D11. The delegated outcome is bounded and deterministic enough that
     another eligible reviewer applying the same authority/evidence
     should reach the same governance disposition.

D12. Delegation eligibility itself is unambiguous. If eligibility is
     uncertain, FAIL CLOSED TO PRODUCT OWNER.
```

## Decision — R0/R1/R2 definitions (self-contained, `X-MAJ-02` correction)

**As of this ADR's activation, this section is the current definition
authority for the R0/R1/R2 risk taxonomy** (see "Activation"). The
definitions below were copied forward from `ADR-042`'s own Decision,
**unchanged in substance** — neither weakened nor broadened — so that
`ADR-045` is self-contained and does not depend on `ADR-042` as a live
definition authority, now that `ADR-042` is `Superseded`:

```text
R0 — Mechanical / no semantic risk. Typo/formatting; deterministic
  bookkeeping; mechanical factual correction; test-expectation alignment;
  refactor proven not to alter behavior/contract; generated/index/
  reference synchronization. Default: NO CROSS-CHECK. Use deterministic
  verification/tests as appropriate instead.

R1 — Bounded semantic / normal implementation risk. Implementation
  change within one module; bounded bug fix; bounded semantic
  correction; internal algorithm change that does not alter external
  contract/authority; reversible change with limited blast radius.
  Default: NO CROSS-CHECK -- Review A is sufficient unless Review A or
  the Product Owner escalates the specific case to R2 because actual
  uncertainty/risk is materially higher than the nominal category
  suggests.

R2 — High semantic / architectural risk. Classified when one or more
  materially applies: Platform Invariant or Event Schema change;
  authority / source-of-truth semantics; identity, lifecycle, replay,
  versioning, retention, or activation semantics; cross-module contract/
  dependency change with significant blast radius; migration difficult
  or expensive to reverse; destructive/irreversible state transition;
  Review A finds conflicting authority/evidence or unresolved ambiguity;
  Review A's own confidence is materially low; repeated semantic
  correction on the same artifact indicates the root cause may still be
  misunderstood; or the Product Owner explicitly requests a second
  opinion. R2 behavior: RECOMMEND OPTIONAL INDEPENDENT CROSS-CHECK — R2
  MUST NOT automatically require the cross-check. For an R2 case, Review
  A must briefly tell the Product Owner (a) why the case is R2, and (b)
  what specific uncertainty/risk a cross-check could reduce. The Product
  Owner then chooses CROSS-CHECK or PROCEED WITHOUT CROSS-CHECK. Absence
  of a cross-check must never make the approval mechanically ineligible.
```

**Optional R2 cross-check semantics (unchanged from `ADR-042`):** when a
cross-check is chosen (R2 only, Product Owner's choice, never automatic),
it independently inspects the exact candidate/repository authority
(never inheriting Review A's conclusion as ground truth); is advisory
only, with no veto; is not an approval prerequisite under any
circumstance; requires no persisted transcript/report, no standalone
Review-B evidence file, no session/execution-ID persistence, no Mode
A/Mode B bookkeeping; and need not appear in the ADR/MANIFEST/CHANGELOG
record of the decision.

## Decision — risk-classified routing

Routing addendum only — the R0/R1/R2 definitions above are unchanged by
this section:

```text
R0: pure deterministic bookkeeping is not a semantic decision and
    continues under existing G-TXN rules unchanged; if an actual governed
    technical resolution exists at R0 (not mere bookkeeping), it may use
    the delegated lane when D1-D12 pass.

R1: Review A required. If D8's two conditions both hold (independent
    CLEAN verdict AND distinct-principal Review A) AND D1-D12 (all
    twelve) pass: DELEGATED TECHNICAL RESOLUTION — no Product Owner
    decision required for this decision.

R2: NEVER delegated, no exception. Routes to Product Owner exactly as
    defined above. The optional independent cross-check semantics at R2
    are unchanged by this ADR.
```

Review A independence (Chapter 0 §3, `G-REV-003`) is not weakened by this
ADR in any way — a Delegated Technical Resolution still requires the same
independent, non-inherited verification Review A already performs, plus
the `D8` distinct-principal safeguard above; this ADR only changes what
happens *after* a genuinely CLEAN, genuinely eligible R0/R1 verdict.

## Decision — AI Technical Architect delegated authority

When D1–D12 pass, an eligible Review A principal may issue:

```text
DELEGATED TECHNICAL RESOLUTION — CLEAN
```

Per `D8` (`X-MAJ-01` correction), the issuing principal must be distinct
from every principal that authored or executed the underlying semantic
work being resolved — this is a `D8`-specific DTR safeguard, not a claim
that ordinary Chapter 0 §3/`G-REV-003` Review-A independence already
contains a self-review prohibition (it does not; `G-REV-003` requires
independent verification/non-inheritance, not distinct principal
identity — that distinction is introduced here, narrowly, for DTR only).
The result authorizes: the bounded technical disposition itself;
deterministic state/bookkeeping updates directly caused by it;
progression to the next governed technical step. It does **not**
authorize any Product-Owner-reserved state transition — it cannot set
`Approved`, `Locked`, Phase Approved, Module Approved, document
`Accepted`/`EFFECTIVE`, package/artifact `Consolidated Stable`, or LIVE
Authorized (`X-MIN-01`), and cannot itself waive an ADR-Required
determination.

## Decision — escalation rules (fail closed to Product Owner)

Escalate to Product Owner if **any** of the following applies:

```text
- Risk Classification resolves to R2;
- ADR Scope Rule resolves to ADR_REQUIRED;
- Review A has any unresolved Blocker/Major;
- a product/business tradeoff exists;
- the architecture/governance mechanism itself must change (as opposed to
  applying an existing one);
- a new contract/invariant/schema/module dependency would be introduced;
- explicit risk acceptance is required;
- the exact governing authority conflicts or is ambiguous;
- delegation eligibility (D1-D12) cannot be proven;
- a governing artifact explicitly reserves the decision to Product Owner
  (`MINOR-01`); or
- the Product Owner has explicitly called in/reserved this specific
  decision (`MINOR-01`) — no AI may override that request.
```

Review A may also voluntarily escalate a nominal R1 case to Product Owner
if the bounded classification understates the case's actual risk — this
mirrors `ADR-042`'s own R1-escalates-to-R2 provision and is never
prohibited by this ADR.

## Decision — persistence model: persist outcome, not ceremony

For a Delegated Technical Resolution, persist only what is needed for
auditability:

```text
- resolution_authority: DELEGATED_TECHNICAL_RESOLUTION (X-MIN-05);
- delegated_resolution_state: RESOLVED (X-MIN-05);
- decision/resolution identity;
- exact subject;
- governing authority/mechanism (the already-approved rule applied);
- reviewed boundary;
- Review A principal;
- Review A verdict;
- distinct-principal confirmation (D8, X-MAJ-01);
- Risk Classification;
- delegation eligibility = satisfied (D1-D12);
- resulting bounded state transition;
- remaining unresolved items, if any.
```

Do **not** require: fake Product Owner approval text; a long approval
script; optional cross-check evidence for R0/R1 (none exists — cross-check
is R2-only, unchanged); redundant ceremony artifacts. No `approved_by:
AI`. No fabricated Product Owner decision of any kind.

### Delegated closure recording — legacy schema mapping (`X-MIN-05`)

Before the first Delegated Technical Resolution ever closes a case, this
generic recording rule applies to any existing (legacy) candidate/
evidence artifact schema — required so a delegated closure never
fabricates or implies a Product Owner action inside a schema that
predates this ADR:

```text
- A delegated closure MUST persist resolution_authority:
  DELEGATED_TECHNICAL_RESOLUTION and delegated_resolution_state: RESOLVED,
  plus the normal audit fields above.
- For a legacy schema containing a PO-specific field (e.g.
  product_owner_state), a Delegated Technical Resolution MUST:
    - never write APPROVED, or any value implying Product Owner action;
    - never fabricate a Product Owner decision of any kind;
    - either set the field to an explicitly non-PO value (e.g.
      NOT_REQUIRED_DUE_TO_DELEGATED_TECHNICAL_RESOLUTION), OR use an
      equivalent unambiguous schema mapping that preserves the historical
      fact that no Product Owner approval occurred.
- If the existing schema cannot represent the Delegated Technical
  Resolution result without ambiguity (i.e. no non-PO value exists that
  clearly distinguishes it from an actual Product Owner action): D12
  FAILS -- the case cannot delegate until a bounded schema alignment
  exists.
```

This rule is defined here and mirrored exactly in `G-DELEGATE-002`
(Global Execution Rules). It is definitional only — no legacy candidate
artifact, including `Candidate-005`, is edited by this correction; the
mapping applies only if/when a candidate later actually receives a
Delegated Technical Resolution.

## Decision — non-retroactivity and the pending-work rule

Historical completed Product Owner approvals remain valid and are never
rewritten. This model applies **only** to resolution decisions made
*after* `ADR-045`'s own atomic activation.

**Pending-work rule (required so already-authored candidates are not
frozen out, without retroactively rewriting their authoring history):** an
unresolved candidate authored **before** activation MAY use the delegated
model **after** activation if, and only if:

```text
- no Product Owner decision has already completed for it;
- its resolution decision itself occurs after activation;
- its evidence/authority/boundary are fresh-re-verified at the resolution
  boundary (not merely inherited from its authoring-time verification);
- D1-D12 all pass at the resolution boundary.
```

**A pre-activation Review A verdict cannot itself become the
post-activation closing review (`X-MIN-02` correction).** A pre-activation
Review A verdict may remain valid historical evidence of that case's
prior state, but it must **NOT**, by itself, satisfy `D8` for a
post-activation Delegated Technical Resolution. `D8` for a delegated
closure must be performed/reconfirmed:

```text
- after ADR-045 activation;
- against the exact subject/evidence being resolved;
- at the fresh resolution boundary;
- by the required distinct principal (D8, X-MAJ-01).
```

This means, for example, `Candidate-005`'s existing `CLEAN` Review A
(recorded pre-activation) is historical input only — it does not, by
itself, satisfy `D8` for any future delegated closure of `Candidate-005`;
a fresh, post-activation, distinct-principal `D8` verification would still
be required at that future resolution boundary. This ADR does not itself
resolve any such pending candidate, and does not predetermine that any
specific pending candidate will pass `D1`–`D12` at a future resolution
boundary — that determination is made fresh, at resolution time, under
whatever authority is controlling then.

## Candidate-005 worked example — NON-NORMATIVE, illustrative only

```text
Candidate-005 (docs/governance/mutation-baseline-evidence/feature-engine-
mutation-material-gap-reclassification-candidate-005.json):
- existing §4.1(b) mechanism (already Approved, already exercised for 167
  prior rows across candidate-002/003/004);
- ADR Scope Rule = ADR_NOT_REQUIRED (fresh-run at authoring time);
- Review A: CLEAN (reported at boundary 31fc6f5dda1834916f68c55656bd2fa
  4893bdcb8, candidate blob e217bda50f17b71792429e196a81583ae02633e8);
- Risk Classification: R1;
- bounded 2-row evidence application, both rows individually verified;
- no new semantics; no risk acceptance.

As of this ADR's activation, `ADR-045` is now Approved and its model is
binding authority. Candidate-005 is therefore now within scope of the
pending-work rule: it MAY become eligible for DELEGATED TECHNICAL
RESOLUTION only if a FRESH, post-activation D8 verification (independent
CLEAN verdict AND distinct-principal Review A, X-MAJ-01) is performed
against its exact subject/evidence at a future resolution boundary, AND
D1-D12 (all twelve) pass at that boundary. The Review A CLEAN verdict
recorded above (pre-activation, boundary 31fc6f5d...) is historical
input only — per the pending-work rule's X-MIN-02 correction, it does
NOT by itself satisfy D8 for any post-activation delegated closure. This
activation transaction does not itself perform that fresh D8/D1-D12
determination, does not resolve Candidate-005, and does not grant it any
credit. Condition 2 remains `167/170`. Candidate-005's status moves from
`PAUSED — awaiting governance delegation-model resolution` to `READY FOR
FRESH POST-ACTIVATION DTR ELIGIBILITY REVIEW` — a separate, subsequent,
bounded transaction is required to perform that review; it is not
rejected, not approved, and not resolved by this ADR's own activation.
```

## Scope / non-goals

**In scope:** the delegation eligibility predicate (D1–D12, including the
D8 distinct-principal safeguard), the self-contained R0/R1/R2 definitions,
the Approval/Delegated-Technical-Resolution distinction, R0/R1/R2 routing
addendum, escalation rules, the legacy delegated-closure recording/schema-
mapping rule, persistence model, non-retroactivity and pending-work rule
(including the pre-activation-Review-A-cannot-satisfy-post-activation-D8
rule), and the atomic activation bundle below.

**Explicitly out of scope / non-goals:**

```text
- this ADR's own Decision content does not itself perform Candidate-005's
  (or any other pending candidate's) fresh post-activation D8/D1-D12
  determination;
- does not resolve Candidate-005 or any other pending candidate;
- does not change Condition 1/2/3 state for P3-FEATURE-QG-EVID-03;
- does not weaken Review A's mandatory/independent status (Chapter 0 §3,
  G-REV-003 -- both unchanged in substance);
- does not weaken the optional R2 cross-check semantics carried forward
  from ADR-042;
- does not change Chapter 0 §4b's ADR Scope Rule table in substance;
- authors a narrow Chapter 11 v2.4 successor candidate (X-MAJ-02) scoped
  ONLY to redirecting the future/post-activation Risk Classification
  definition-source pointer from ADR-042 to ADR-045 in §11.5/§11.9 -- ADR
  approval itself remains Product-Owner-only, unchanged in substance;
  Chapter 11 does not govern DTR outcomes generally, it remains the
  ADR-process authority;
- does not modify Chapter 12 (Approval Gates remain Product-Owner-only --
  no contradiction found on fresh inspection);
- authors a narrow before/after-activation bridge correction to
  docs/templates/adr-template.md's Risk Classification source reference
  (X-MAJ-02) -- ADR approval remains Product-Owner-only there, unchanged,
  no new role introduced, ADR approval is never made delegable;
- does not modify /team/team.yaml (no new role is introduced by this ADR
  -- delegation is exercised by the existing `AI Technical Architect` role
  under a narrower eligibility predicate, not a new role; registered
  ChatGPT/Claude principals are already distinct identities for D8);
- does not modify docs/governance/phases/phase-3-rules.md -- fresh
  inspection (this transaction) found no live normative statement there
  that contradicts this model; its Gate-path already defers to whichever
  model controls at Chapter 0 §3/Chapter 11 §11.5, and its Product Owner
  Phase 3 Approval Gate decision step is unchanged and unaffected;
- does not authorize LIVE, approve Feature Engine, or touch any Quality
  Gate result.
```

## Alternatives considered

1. **Status quo — every decision reaches Product Owner regardless of risk
   class.** Rejected — this is precisely the disproportionate routine
   traffic the Product Owner decision direction identifies, evidenced by
   four sequential near-identical §4.1(b) candidate approvals.
2. **Delegate R2 as well, with mandatory cross-check as a substitute
   safeguard.** Rejected — contradicts the Product Owner's explicit
   instruction to preserve Product Owner authority over "meaningful risk,"
   and would silently reintroduce a mandatory-gate failure mode ADR-042
   already retired for a different reason.
3. **A scored/weighted eligibility system instead of a closed predicate.**
   Rejected — mirrors ADR-042's own rejection of "scoring systems, forms,
   evidence registries, or additional ceremony"; a closed, conjunctive
   D1–D12 predicate is simpler to apply and to fail closed on than a
   score.
4. **A new "Delegate" role distinct from `AI Technical Architect`.**
   Rejected — no new role is needed; the existing role's Review A
   eligibility (Chapter 0 §2) already carries the necessary technical
   authority, and a new role would itself be an unnecessary governance
   addition contrary to `G-ADR-001`'s "no gap -> ADR reflex."
5. **Retroactively apply the delegated model to already-completed Product
   Owner decisions.** Rejected — explicitly prohibited by the
   non-retroactivity principle (mirrors `ADR-042` §"historical evidence
   unaffected"); historical decisions keep the approval validity they were
   made under.
6. **Bounded exception-based delegation for R0/R1 only, R2 and all
   PO-reserved classes explicitly excluded (CHOSEN).** Narrowest rule that
   reduces routine traffic without touching any decision class the
   Product Owner instruction identified as requiring continued Product
   Owner authority.

## Review A / Risk Classification / Concerns / Risks noted

| Reviewer principal | Role at review boundary | Review boundary | Concern | Risk | Recommendation |
|---|---|---|---|---|---|
| ChatGPT | AI Technical Architect | `fee5c582dc89efcd0eec8c537d56cd0151eea673` (v0.1, blob `e5e7357f16220777922b07bd4be29ef0cfe19007`) | `MAJOR-01` (ADR-042 authority conflict — "extends" vs. supersedes), `MAJOR-02` (Chapter 0 dual normative workflow), `MINOR-01` (D10 PO call-in) | R2 | `REVISION_REQUIRED — 0 Blocker / 2 Major / 1 Minor` — remediated in v0.2, CLOSED — REVIEW A VALIDATED at round 2 |
| ChatGPT | AI Technical Architect | `bced025c6df3ca36f46313862fddac2c5c3dc8c1` (v0.2, blob `033c2a95d372c8cb93ef1f3495699dc95e925384`) | Review A: none (`CLEAN`). Optional R2 cross-check (Product-Owner-selected, advisory, not an approval prerequisite): `X-MAJ-01` (DTR lacks distinct-principal independence), `X-MAJ-02` (R0/R1/R2 authority migration incomplete), `X-MIN-01` (missing reserved lifecycle classes), `X-MIN-02` (pre-activation Review A cannot become post-activation D8), `X-MIN-03` (MANIFEST stale operational-governance rows), `X-MIN-04` (review provenance stale), `X-MIN-05` (legacy DTR recording mapping) — all 7 independently re-verified and accepted by ChatGPT (the prior round's Review-A-provenance Minor absorbed into `X-MAJ-02`) | R2 | Review A `CLEAN — 0 Blocker / 0 Major / 1 Minor`; cross-check `DEFECT FOUND — 0 Blocker / 2 Major / 5 Minor`; ChatGPT-accepted effective input `0 Blocker / 2 Major / 5 Minor` — remediated in v0.3 (CORR-002), CLOSED — REVIEW A VALIDATED at final round |
| ChatGPT | AI Technical Architect | `e54159aa8355cfd1d3d309d43969007b645246ed` (v0.3, blob `432562e62c26d2922981a6e3d73885be13d78d20`) | Review A: none substantive (`CLEAN`) — confirmed `X-MAJ-01`/`X-MAJ-02`/`X-MIN-01..05` genuinely remediated. One residual non-semantic provenance-wording `Minor` (stale round-1/round-2 review-history labeling) — Product Owner explicitly authorized folding its deterministic cleanup into this activation, no additional cross-check required | R2 | `CLEAN — 0 Blocker / 0 Major / 1 Minor` — **APPROVED**. Product Owner decision (verbatim): "APPROVE ADR-045 v0.3 and its atomic activation. Accept Review A CLEAN — 0 Blocker / 0 Major / 1 Minor, Risk R2. Fold the non-semantic provenance-wording cleanup into activation. No additional cross-check required." — `2026-09-23T14:02+07:00` |

> Trước approval phải có đúng một Review A eligible, đúng Chapter 0 §3 /
> Chapter 11 §11.5 — reviewer giữ role `AI Technical Architect` tại review
> boundary, độc lập kiểm tra trực tiếp candidate/repository authority.
> ChatGPT's final Review A on this exact v0.3 candidate (boundary
> `e54159aa8355cfd1d3d309d43969007b645246ed`) returned `CLEAN — 0 Blocker
> / 0 Major / 1 Minor` — the sole remaining Minor was non-semantic
> provenance wording (stale review-round labeling in the round-1 closure
> banner), not a substantive `D1`–`D12`/Risk-taxonomy/routing/activation/
> authority/recording defect. With zero Blocker and zero Major, the
> Product Owner recorded the approval decision quoted above, explicitly
> authorizing the provenance wording's deterministic cleanup to be folded
> into this same activation transaction rather than requiring a fourth
> correction round, and explicitly declining to require any additional
> cross-check.

**Risk Classification (finalized):**

```text
class: R2
reason: >
  Governance/Approval-process change (Chapter 0 §4b, ADR_REQUIRED) that
  determines, platform-wide, which decision classes may close without a
  Product Owner Decision. A wrong eligibility boundary here could
  systematically under-govern decisions that should have reached Product
  Owner -- exactly this ADR's own R2 criterion ("authority / source-of-
  truth semantics," "cross-module ... significant blast radius").
  Classification is ChatGPT's own determination at v0.3's final Review
  A, matching all three prior rounds' own historical R2 classifications.
```

> R0/R1 default `NO CROSS-CHECK`. At R2, Review A recommends an *optional*
> advisory cross-check; Product Owner chooses whether to use it — never a
> mandatory ADR field, never an approval prerequisite. The Product Owner
> exercised that choice for the v0.2 candidate (row above), and the
> cross-check surfaced genuine, since-remediated defects; for the final
> v0.3 review, the Product Owner explicitly required no additional
> cross-check — this remains the Product Owner's own discretionary choice
> each time, never retroactively mandatory.

**Scale check:**

```yaml
scale_check:
  current_scale: "Phase 3 -- Core Backend; governance/decision-routing change, not a runtime/strategy/exchange/plugin artifact"
  expected_scale:
    strategy: 0
    exchange: 0
    plugin: 0
  decision_still_valid: true
  reason: >
    Decision-routing policy cost scales with the number of governed
    decisions made over time, never with strategy/exchange/plugin count or
    event volume -- this ADR reduces routine Product Owner traffic for a
    closed, conjunctive-eligibility subset of R0/R1 decisions while
    keeping Review A mandatory, R2 fully Product-Owner-routed, and every
    PO-reserved class (ADR approval, Approval Gates, product direction,
    risk acceptance, LIVE) unchanged -- it remains valid regardless of
    platform scale.
  reason_if_no: ""
```

## Consequences

This ADR decides the delegation model; its own approval and the
coordinated Chapter 0/Chapter 11/Global Execution Rules/MANIFEST
amendments landed together as one atomic governance action (see
Activation below), mirroring `ADR-042`'s own atomic-activation
precedent — this ADR's own text does not separately amend those
artifacts, the bundled activation transaction did. It does not resolve
`Candidate-005` or any other pending decision, does not touch Feature
Engine Condition 1/2/3, EVID-03, module approval, or LIVE. Now that
activated: R0/R1 decisions satisfying `D1`–`D12` (including the `D8`
distinct-principal safeguard) close as `DELEGATED TECHNICAL RESOLUTION`
without a Product Owner Decision step; R2 and every PO-reserved class
(including the `X-MIN-01` additions — document `Accepted`/`EFFECTIVE`
and package/artifact `Consolidated Stable` transitions) continue exactly
as originally defined, now fully self-contained in this ADR's own text
(`X-MAJ-02`), with `ADR-042` becoming `Superseded` (per "Authority
compatibility" above); historical decisions keep their existing
validity, unreinterpreted.

## Activation

Per the precedent established by `ADR-042`'s own "single coherent atomic
governance action," this decision's activation was a **single atomic
activation bundle** — no partial activation, all items landed together
at reviewed boundary `e54159aa8355cfd1d3d309d43969007b645246ed`, in one
Product Owner decision (`2026-09-23T14:02+07:00`):

```text
1. Product Owner approved ADR-045. DONE.
2. Chapter 0 v1.5 -> Locked / controlling. DONE.
3. Chapter 11 v2.4 -> Locked / controlling. DONE.
4. Global Execution Rules v0.7 -> EFFECTIVE. DONE.
5. MANIFEST atomically recorded:
   - ADR-045 current: Approved;
   - ADR-042 current: Superseded, reverse relation
     ADR-042 superseded_by: ADR-045 -- ADR-042.md itself preserved
     byte-for-byte, unmodified, as historical Approved evidence
     (Chapter 11 §11.3);
   - Chapter 0 v1.5 current: Locked / controlling (v1.4 -> historical
     predecessor);
   - Chapter 11 v2.4 current: Locked / controlling (v2.3 -> historical
     predecessor);
   - Global Execution Rules v0.7 current: EFFECTIVE (v0.6 -> historical
     predecessor);
   - all relevant current-state identities updated consistently, in the
     same commit. DONE.
6. CHANGELOG records the migration. DONE.
```

`docs/templates/adr-template.md`'s own before/after-activation bridge
wording (`X-MAJ-02`) required no separate activation step — it was
already correct both before and after activation by construction (it
names both `ADR-042` and `ADR-045` explicitly), so it was not edited at
the activation boundary; no partial-activation state was created by it.
`docs/constitution/12-approval-gates.md`, `/docs/team/team.yaml`, and
`docs/governance/phases/phase-3-rules.md` remained unchanged, at
activation and afterward. No `Candidate-005` resolution occurred in the
activation transaction — that remains a separate, subsequent, bounded
transaction requiring its own fresh post-activation `D1`–`D12`
determination (see "Status" above).

Resulting state, as of this activation:

```text
ADR-045              = Approved (current risk-taxonomy / decision-
                        routing authority)
Chapter 0 v1.5        = Locked / controlling (v1.4 = historical
                        predecessor)
Chapter 11 v2.4        = Locked / controlling (v2.3 = historical
                        predecessor)
Global Execution Rules v0.7 = EFFECTIVE (v0.6 = historical predecessor)
ADR-042               = Superseded (by ADR-045); ADR-042.md byte-
                        immutable, unmodified
Candidate-005         = remains 167/170, unresolved, awaiting a fresh
                        post-activation D1-D12 determination
```

## ADR Scope rationale (Chapter 0 §4b — `ADR_REQUIRED`)

```text
Governance/Approval-process change: YES. This decision changes how a
  class of governed technical decisions is routed to closure -- who may
  close it and under what closed predicate -- which is Governance/
  Approval-process semantics (Chapter 0's own domain), not a one-off
  technical correction.
Decision affecting >1 module / reusable: YES. The delegation predicate
  applies platform-wide to any future R0/R1 decision satisfying D1-D12,
  not scoped to Feature Engine or EVID-03 specifically, even though
  Candidate-005 is cited as a non-normative illustration.
Not an internal refactor: this is a new decision-routing RULE, not an
  implementation detail -- a wrong eligibility boundary could silently
  under-govern a decision that should have reached Product Owner.
Conclusion: ADR_REQUIRED. Not downgraded to ADR_OPTIONAL or
  ADR_NOT_REQUIRED by this executor.
```

**Accepted risks:** None recorded — no Concern currently raised requires
Product Owner risk acceptance against a Platform Invariant.
