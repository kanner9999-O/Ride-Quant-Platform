# Reproducible dev/build environment lock — structure-engine.
#
# Generated by: `pip freeze --exclude-editable`, from a FRESH venv built via
#   `python3.13 -m venv .venv && pip install -e ".[dev]"`
# (the project package itself is installed editable and is intentionally
# excluded — it is not a "dependency", it IS this repository).
#
# Exact interpreter used: Python 3.13.6 (CPython, arm64).
# Exact install mechanism used: pip 25.2 (pinned via
#   `pip install --upgrade pip==25.2` before installing this project).
# Platform this lock was generated/verified on: macOS-14.5-arm64-arm-64bit-Mach-O.
#
# This is the FULL transitive dev-dependency state (ruff/mypy/pytest and
# everything they themselves pull in) — not just the three top-level tools
# pinned in pyproject.toml's [project.optional-dependencies].dev. Regenerate
# with the exact commands above whenever pyproject.toml's dev extras change;
# do not hand-edit.
#
# The build backend (setuptools) is pinned separately, exactly, in
# pyproject.toml's [build-system].requires — pip builds it in an isolated
# environment per PEP 517, so it does not appear in this application-level
# freeze.
#
# Zero production runtime dependencies (pyproject.toml's [project].dependencies
# is empty) — this lock covers ONLY the dev/build toolchain, never shipped.

ast_serialize==0.8.0
iniconfig==2.3.0
librt==0.15.0
mypy==2.3.1
mypy_extensions==1.1.0
packaging==26.3
pathspec==1.1.1
pluggy==1.6.0
Pygments==2.21.0
pytest==9.1.1
ruff==0.16.4
typing_extensions==4.16.0
