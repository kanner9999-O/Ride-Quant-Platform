# python/

Root cho mọi module có ngôn ngữ triển khai = Python (ADR-008: Feature Engineering · Strategy · Decision logic · Backtest Engine layer). Quy ước đầy đủ: `docs/engineering/monorepo.md`.

Chưa có module nào build tại đây. Khi Phase 3 bắt đầu build một module Python cụ thể, source directory của nó phải đặt tên đúng `module_id` trong `docs/architecture/module-registry.yaml` (ví dụ `python/feature-engine/`).
